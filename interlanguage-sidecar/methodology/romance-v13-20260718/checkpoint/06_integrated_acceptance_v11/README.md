# Romance four-stage redo — production state v11

This tree is the active Romance interlanguage construction lane. The current successor gate is `qa/ROMANCE_ACCEPTANCE_GATE_v11.json`; the four-stage objective remains **ACTIVE_NOT_COMPLETE**. In particular, 19 provisional controlled-Romance pages are not a translation of the 466-page R823 authority, and neither the language nor its intelligibility has been human validated.

## Current four-stage state

- **Stage A — provenance and branch routing: not complete.** The routing ledger names 61 standards/branches: 11 currently have active bodies and 50 remain explicit zero-body routes. Romansh now has seven active official general-school mathematics bodies across Rumantsch Grischun (four), Putèr (one), Sursilvan (one), and Vallader (one). The 2025 acquisition contributes four full 15-page bilingual papers, 60 physical pages, as one declared translation family. It contributes no specialist-algebra body. Surmiran and Sutsilvan remain zero-body routes, and all newly acquired rights are unresolved and not publication-cleared.
- **Stage B — consolidated corpus: current tranche passes.** Corpus v5 has 153 records: 147 primary unique representations, 6 representation aliases, 71 counting-eligible records, and 70 active bodies. Hashes, logical-source and translation-family status, license/status fields, searchable-text bindings, language/domain coverage, and variety/domain coverage are machine checked. The current corpus is an internal research package with absolute paths and unresolved-rights material; it is not itself a direct public payload.
- **Stage C — sense-aware WordWeb: structurally current, empirically incomplete.** WordWeb v11 has 60 concepts, 106 senses, 39 C2 extension nodes, and 811 evidence records. Those comprise 120 inherited ES/FR core locator claims with zero quotations, 682 reviewed corpus occurrences, and 9 new exact-source semantic-audit records. Accepted support now reaches 78/106 senses; 28 senses remain unsupported. The v11 audit adds exact support for T10-S1, T10-S2, T34-S1, T35-S1, and T55-S1, revalidates T35-S2, and preserves the adverse scope boundaries for T35-S3 and T55-S2.
- **Stage C — relation and crosswalk boundary.** WordWeb v11 contains 406 relation records, but these are **not 406 graph edges**: only 27 relations have valid target IDs, 379 are descriptive/source-gloss records without target IDs, and there are zero invalid target IDs. Adding the 106 concept-to-sense memberships yields 133 ID-resolved references. The preserved production crosswalk still contains 194 rows (101 Spanish, 93 French) and all frozen rows reproduce their own row hashes. The live external French terminology ledger has advanced beyond the v10-pinned snapshot, so v11 does not pretend that the old crosswalk is byte-reproducible from that mutable current path.
- **Stage C — named-cohort access/MII boundary.** Access v11 is the exact 106 × 9 = 954-row grid for the manager's nine canonical reader cohorts, including separate Rumantsch Grischun and regional-idiom cohorts. Every human-result field remains null, all 954 rows remain `pilot_eligible=false`, and human observations, form promotions, and empirical-MII results remain zero. Orthographic proxy values are design diagnostics and do not measure comprehension or intelligibility.
- **Stage D — actual controlled creation: active, provisional.** T001–T008 cover authority body lines 21047–21307 in eight source-keyed units. Their eight build PDFs equal the eight output copies byte-for-byte, and all 19 fresh renders reproduce the 19 pinned QA pages. The cumulative controlled-language profile records 65 grammar decisions, 63 function-word decisions, and 147 terminology decisions; 122 terminology rows have effective WordWeb links, 25 remain explicitly unlinked, and the links contain 148 resolved identifier references. T008 uses invariant `iste`; plural `istes` is rejected. All construction decisions remain provisional, with zero native validation and zero human observations. The next semantic authority line is **21309**, §7.

## Rebuild and validation sequence

1. Run the isolated Romansh intake validator, then `scripts/validate_corpus_branch_package_v3.py`.
2. Run `scripts/build_wordweb_semantic_evidence_v11.py`, then `scripts/validate_wordweb_semantic_evidence_v11.py`. The raw source audit contains absolute paths, quotations, and unresolved-rights material and is not a direct public payload.
3. Run the T007 and T008 local validators; earlier tranche controls remain pinned through the v10 predecessor manifest.
4. Run `scripts/verify_pdf_renders_v12.py`; the expected surface is eight PDFs and 19/19 byte-identical fresh/pinned page renders.
5. Run `scripts/validate_controlled_romance_spec_v3.py`; the expected surface is 58/58 checks and 65/63/147 grammar/function/term decisions.
6. Run `scripts/validate_romance_tranche_v11.py --core-only` before advancing current-state documentation.
7. Run `scripts/validate_romance_tranche_v11.py` twice. The gate, log, and manifest must be byte-stable and all manifest rows must recompute.

## Claim boundary

The current state supports bounded machine claims about source slices, hashes, semantic routing, corpus classification, compiled PDFs, and rendered-page identity. It does not support a pilot, native-validation, scalar-readiness, empirical-MII, public-rights-clearance, lane-completion, or full-R823 Romance translation claim. Stage A, the remaining 28 unsupported senses, human validation, and the translation beyond line 21307 are not complete.
