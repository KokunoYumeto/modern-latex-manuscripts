# Operational decisions

Each JSON file in this directory validates against:

`01_methodology/research_department/OPERATIONAL_DECISION_INTERFACE.schema.json`

These records classify newly recovered forms as held concept-shelf witnesses. They correct inherited zero-witness or no-body states but select no candidate for translation and make no native-review, community-review, or terminology-acceptance claim. Every record preserves the vector of hard readiness gates and sets `auto_promotion_prohibited` to `true`.
