# Concordance: Fable blind atlas × ChatGPT language-only sidecar
2026-07-04. Frozen inputs: [frozen/FABLE_BLIND_ATLAS_v0.md](frozen/FABLE_BLIND_ATLAS_v0.md) (produced before seeing the handoff), [frozen/CHATGPT_LANGUAGE_ONLY_HANDOFF_v0.md](frozen/CHATGPT_LANGUAGE_ONLY_HANDOFF_v0.md). Provenance note: the atlas was written from the codex logs + theory docs only; the handoff arrived afterward and no atlas edits followed it. The convergence below is therefore evidential (two systems, same corpus, independent syntheses), with the shared theory-sidecar ancestry as the one common upstream input.

## Delta requested by the sidecar: what changes / sharpens / confirms / rejects

**Independently confirmed (identical or compatible):**
- One-system view: ladder "source shelf → term spine → proof grammar → script sidecar → review packet → pilot gate" (atlas) ≡ handoff corpus strata + gates. IDENTICAL.
- access_gain_score = operational form of the bridge objective; ledger-not-pseudo-precision boundary. IDENTICAL (handoff's informal formula maps term-for-term; merge table adopted below).
- Source floor is lexicographic, before optimization (atlas F1 ≡ handoff siting rule). IDENTICAL.
- Build-type trichotomy zonal/controlled/script-bridge and "siting question ≠ what language to invent". IDENTICAL.
- Dominance collapse as recurring failure mode with regularizer response. COMPATIBLE (atlas adds the five documented instances + the measured `kolco` case).
- Interslavic logbook first; claim ledger; heuristic register; siting table sequence. IDENTICAL (executed).

**What becomes sharper after the handoff + response:**
- Spine architecture gets names and roles: **C0** intersection core (17; sanity/schema/display only), **C1** broad shared core (32; first comparison set), **C2** stratified core spine (the real object; proof-grammar / curriculum-algebra / Noether-corpus strata; fill-list semantics: witnessed | gap | not-applicable), **C3** outer union/backlog (138+; discovery surface for false-friend candidates and hard rows). ADOPTED.
- Submodularity claim RESTRICTED to the defensible form: benefit layer = weighted graded max-coverage over reader-task universe (monotone submodular, greedy guarantee); risk layer (false friends, dominance, script confusion, pair-interactions) = constraints/vetoes/penalties outside the coverage function. CLM-MARG-001 updated accordingly.
- Marginal access gain formalized with an equity term: MAG(x,c) = Σ_g w_g (I_g(x,c) − I_g(d(c),c)) − ρ·Var_g(I_g(x,c)) — total gain over dominant baseline minus concentration penalty. This is the rigorous form of Floris's selection rule and subsumes HEU-MARG-INT-001 + HEU-NONDOM-001.
- F10 audit gets a typed schema: witness vector W(t)=(E, W_S, S, I, X) + flags F10-0 clean / F10-1 missing witness / F10-2 East-heavy vs broad claim / F10-3 dominance-risk / F10-4 authority-needed; plus safe_to_show_external per term. Defect ≠ E(t)>0; defect = broad-Slavic claim with East-heavy vector and no compensating I/X rationale. ADOPTED as the audit spec.
- Freeze-then-fix (Floris): pre-retrofit spine v1 and the un-backfilled dataset are locked as the historical stratum; the retrofit diff ("AI with East-Slavic sources only" vs "AI with family-breadth sources") becomes a measured result, not a cleanup.
- Script hygiene: path-parameterization + run manifests (inputs, hashes, counts) before public packaging. QUEUED.
- ID material narrowed to corpus diagnostics with genre/length/language/model/estimator controls; new operational hypothesis: good bridge registers reduce cross-family task description length (fewer exceptions, fewer per-language patches). COMPATIBLE with HEU-ID-001 (status stays test).

**What I now reject / correct in my own earlier output:**
- "200-concept pilot table" as a flat object (inherited from the sidecar's own CLM-ILO-001 draft): rejected in favor of C2 stratified fill-list. (Both systems converged on the rejection after F9.)
- Calling 3-AI triangulation anything near review: the standing phrase is **"internal triangulation before external review"** — never "validation". (Atlas already said this; now normative wording.)
- My union-spine md/json as a living file: v1 is frozen; v2+ are new artifacts.

**Tensions (minor, tracked):**
- Handoff's "Immediate P0.2" wanted CLAIM_LEDGER/HEURISTIC_REGISTER/INTERSLAVIC_GATE_MAP strictly from the logbook; I built them corpus-wide (logbook + glossaries + world-family logs). Judged compatible — the wider base is strictly more evidence — but flagged for the record.
- Handoff proposes UNION_TERM_SPINE.csv; current implementation is json+md. CSV export added to the v2 build for spreadsheet review. No conflict of substance.

## Triangulation hit log (evidence that cross-system review catches real defects)

- 2026-07-04 **Ränderung prediction CONFIRMED**: ChatGPT, reading only artifact summaries, predicted the coarse gloss-linker had falsely mapped "ring" to *Ränderung* (bordering). Verified in data: substring matching had linked "ring" to the gloss "bordering operation under Clebsch's transfer principle". Also found unpredicted sibling defect: "reducible" linked into "irreducible" (semantic opposite). Fix: word-boundary + plural-tolerant matching in both linkers; coverage honestly deflated (28→25 gloss concepts, 40→34 spine matches); *Ränderung* added to ledger as `false_friend_or_collision_risk`, `do_not_merge_with: ring` — first `do_not_use`-class row.
- Adopted from same sidecar pass: layer renamed **dependence-corrected witness weighting on a language-family tree** (ensemble-dependence framing: many witnesses from one branch ≠ independent evidence mass — the "many votes from one model" problem in an accepted empirical field); three-status discipline **linked_to_concept ≠ witnessed_for_branch ≠ reviewed_for_bridge_use** now encoded in spine v2 Slavic entries; match_type vocabulary (exact / near / historical_Noether_specific / compound_or_phrase / not_in_current_spine / ambiguous / false_friend_or_collision_risk) adopted in the seed schema; branch-weighting measurement plan = three states (pre-backfill baseline [frozen run1] / post-concept-alignment / post-W-S-backfill).

- 2026-07-04 (later) **Missing-QF-row catch CONFIRMED (ChatGPT→Fable)**: v3 master reported that "quotient field" — a review-priority row — was absent from Fable's 138-row marker table v1. Verified true (v1 had quotient/quotient group/quotient module but not quotient field; the union-spine keying dropped it). v3's inclusion is correct.
- 2026-07-04 (later) **Label-contamination catch (Fable→ChatGPT)**: v3 master's label columns carry sentence/fragment source cues in place of lemmas in ~25% of rows (isv 54, isv_cyr 45, uk 41, ru 42 of 212; e.g. ring.isv = a full sentence, lemma kolco). Repaired in v3.1 (cues moved to *_source_cue columns; lemmas restored from ledger labels 10–16 per column; rest honest-blank). Defect report filed for v4 extraction fix. **The mutual-catch pattern is now three-for-three across directions** (Ränderung: C→F; missing QF: C→F; label contamination: F→C).

## Concordance entries (sidecar schema; cross-referenced to CLAIM_LEDGER.md)

| claim_id | fable_blind_statement | chatgpt_sidecar_statement | agreement | status | next_action |
| --- | --- | --- | --- | --- | --- |
| CLM-001 | one production system, one ladder (atlas §1) | one research map over three strata | identical | supported | done (atlas §2 table) |
| CLM-002 | access_gain_score ≡ theory objective, 3-way table (atlas §5, CLM-OBJ-001) | informal best_bridge formula ≡ ledger | identical | supported | scoring-table reconstruction in siting v1 |
| CLM-003 | dominance collapse recurs both substrates (F6/F10, `kolco`) | dominance penalty + harm floor needed | compatible | supported (5 instances + 1 measured) | MAG with variance penalty; post-retrofit shift measurement |
| CLM-004 | source availability is THE gate (F1) | source floor lexicographic before optimization | identical | supported | siting table v1 |
| CLM-005 | union spine = missing keystone (F4) | UNION_TERM_SPINE.csv build | identical | established (v1 built; 138/17/32/83) | v2 with Slavic + csv export |
