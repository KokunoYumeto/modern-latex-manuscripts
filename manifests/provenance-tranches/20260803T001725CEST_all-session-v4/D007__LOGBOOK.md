# D007 work log

## 2026-08-02 — no-overwrite opening

- Opened D007 only after D006 reached exact local French/English/side-by-side EOF closure and its semantic/recovery records were made coherent.
- Bound the complete 25-page IAS scan as source authority. The smaller collected-paper split is retained for comparison but does not override the selected IAS image evidence.
- Bound the inherited French and English TeX drafts as fallible working predecessors, not source proof. Each wording, formula, note, cross-reference, and diagram remains subject to direct authority replay.
- Bound both existing GPU-OCR page batches. OCR is locator/drafting evidence only; no new OCR, OCR extraction, or agent transcription is authorized.
- Source-detail review will use targeted images at approximately 1,100–9,000 dpi according to the represented detail, with the higher end reserved for small symbols, primes, accents, arrowheads, attachments, crossings, and ambiguous typography. No low-resolution image will carry a source-reading decision.
- The canonical deliverable remains separate source-aligned French and English TeX/readers plus a genuine French-left/English-right synchronized reader. Stable semantic unit IDs and explicit dependency rows will be generated from admitted source structure as the paper advances; this is preparatory indexing, not formalization.
- To protect the unstable workstation, this opening used only small metadata/hash reads and direct text edits. No rendering, image loading, compilation, agent work, or parallel task was started.
- Copied the two inherited TeX drafts byte-for-byte into this no-overwrite root. The French copy remains 42,537 bytes / SHA-256 `BA349A71D6E201F37E8246230515D2B05171BB3BFD61C42B9E9502A620A9B75F`; the English copy remains 41,257 bytes / SHA-256 `03D90E1797AFE5BBB8509BFBEF9DABAB50DE8D5E74F850FD33C97A8A502F080E`. No TeX byte has yet been changed.
- Added `controls/PAGE_STRUCTURE_BASELINE.csv` from the inherited TeX section markers and existing OCR metadata only. It maps the title, Sections 1--6, and bibliography to authority pages 1, 1, 3, 4, 10, 13, 21, and 24. Every row remains explicitly locator-only until direct source replay; the OCR strings do not establish spelling or mathematics.

## 2026-08-02 — authority page 1 header

- Inspected one bounded full-width page-1 header band directly from the selected IAS authority at 1,100 dpi; no page batch or whole-volume render was made. The scan places `Séminaire BOURBAKI` alone on the first line, then places `22e année, 1969/70, n° 376` on the left and `Mai--Juin 1970` on the right of the second line.
- The inherited French and English drafts instead put the date on the first line. Corrected both editions so the date is right-aligned on the second line, preserving the source hierarchy while translating only the English wording. This is action `D007.source.action.001` in `controls/SOURCE_ALIGNMENT_ACTIONS.csv`.
- The inspection establishes only the opening header. The direct sequential cursor remains on authority page 1 at the title and summary; no later page is claimed.
- A separate bounded 1,100-dpi crop confirms the printed title `TRAVAUX DE GRIFFITHS` and attribution `par Pierre DELIGNE`. The French transcription is exact. Retained English `GRIFFITHS'S WORK`: here `work` is the conventional mass noun for a scholar's body of mathematical results, whereas plural `works` would misleadingly suggest a bibliography of separate publications. This retained choice is logged as `D007.source.action.002`.
- The next full-width, vertically narrow 1,100-dpi band confirms the complete two-sentence summary, printed heading `1. Structures de Hodge.`, and opening sentence defining the ambient real vector space. French is exact. Retained English `This talk` for Bourbaki `Cet exposé`, and retained both the positive scope (fundamental results on families of Hodge structures) and explicit exclusion (results on algebraic cycles) without compression; action `D007.source.action.003`.
- Definition 1.1 was reviewed in a separate full-width 1,100-dpi band. The inherited drafts had compressed the source's two successive displayed equalities into one line; restored the complexification and bigrading as two displays in both editions (`action.004`).
- The same band shows deliberate typewritten underlining on the defining clauses, `Les nombres de Hodge`, and `poids`. Restored literal underlining in French and on the corresponding English clauses/terms (`action.005`). This is a source-visible emphasis layer, not mathematical notation and not an editorial invention; literal underline is used instead of silently converting the source to italics.
- An overlapping 1,100-dpi band confirms Definition 1.2 and equation (1.2.1). Restored the printed clause-by-clause underlining in both editions (`action.006`). Corrected the conjugation scope from an overline around the entire evaluated expression to the printed bar on the filtration $F$ itself (`action.007`).
- The source places tag (1.2.1) on the left, while the inherited amsmath default would place tags on the right. Logged this as `D007.open.001`; a whole-paper equation-number-side policy will be applied only after representative later tags confirm consistency, avoiding a premature paper-wide layout mutation.
- The terminal page-1 band contains only the exact transition `On passe de 1.1 à 1.2 en posant`; equation (1.2.2) starts on authority page 2. Retained French and English transition wording as `action.008`. Authority page 1 is therefore sequentially closed without inferring any page-2 formula from OCR.
- The page-2 top band confirms equations (1.2.2) and (1.2.3). Corrected the bar in (1.2.3) to apply to the filtration $F$ itself (`action.009`). The source's tags (1.2.1)--(1.2.3) are consistently left-numbered across the page boundary, so closed `D007.open.001` by applying the document-level `leqno` option to both language editions (`action.010`); this avoids equation-by-equation layout patches.
