# SET THE ACTIVE GOAL NOW

This branch is failing if you only read instructions.

Your first action is to set the active goal / pursuit goal / persistent task to the literal complete contents of:

`SET_THIS_AS_YOUR_GOAL_VERBATIM_20260705.md`

Then execute it.

Do not push acknowledgement.

Do not push status-only output.

Do not push governance-only output.

Do not touch `main`.

The next push must contain literal source bodies, usable source-checked translation/transcription output, concrete repair artifacts, or a real package containing those things.
