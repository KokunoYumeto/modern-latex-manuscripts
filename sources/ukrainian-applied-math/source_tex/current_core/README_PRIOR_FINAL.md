# prior build final staging note

This package stages the full current auxiliary local run output and useful Spark output.

Use `pdf/ukrainian_session07_review_final.pdf` for stable review. It is a valid PDF built from the prior build integrated reference plus a prior build addendum. The TeX source tree contains prior build chapters 44--51 and the full incoming auxiliary local run/Spark payloads.

The sandbox killed long monolithic XeLaTeX runs while rebuilding every chapter into one PDF. The source is preserved; local Codex can run build repair on the monolithic `src/main.tex` later.
