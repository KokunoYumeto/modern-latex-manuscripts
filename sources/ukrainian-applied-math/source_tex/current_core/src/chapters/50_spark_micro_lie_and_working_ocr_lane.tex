\chapter{Spark: micro Lie fragments і приватна OCR-доріжка}
\label{chap:spark-session07}

\sourcebox{Spark output: \texttt{codex 5.3 Spark.zip}. Корисні частини: частковий переклад \texttt{P0\_arxiv\_1812.01537} і скомпільований OCR/LaTeX старт для Blackman chapter 4.}

Spark у цьому запуску поводився саме як дешевий механічний worker. Це не заміна глибокому перекладу, але корисний спосіб спалювати окремий пул токенів на дрібних файлах.

\section{Micro Lie translation progress}

Spark створив українські TeX-фрагменти для:
\begin{itemize}[leftmargin=*]
\item abstract;
\item intro;
\item derivation rules;
\item composite manifolds;
\item прикладів $S^1$, $S^3$, $SO(3)$;
\item diff-drive self-calibration;
\item conclusion.
\end{itemize}

Важливий статус: raw-файли корисні, але не всі одразу компілюються в оригінальному paper preamble. У Spark log зазначено проблему з \texttt{hyperref} driver option \texttt{pdftex} під XeLaTeX. У цій сесії зроблено окремий wrapper, який компілює selected fragments і залишає проблемні raw-файли в source tree для подальшого ремонту.

\section{Що це дає основному ядру}

Micro Lie є ключовим для станів на $SO(3)$ і $SE(3)$:
\begin{equation}
  X\oplus\delta = X\Exp(\delta),\qquad X_2\ominus X_1=\Log(X_1^{-1}X_2).
\end{equation}
Правила диференціювання зводять складні похідні до блоків Якобіанів і chain rule. Це безпосередньо потрібно для ESKF, visual-inertial odometry, SLAM і factor graphs.

\section{Blackman OCR/LaTeX}

Spark також узяв working OCR gap з Blackman chapter 4 і зробив компільований OCR/LaTeX draft. Це варто зберігати, але не зливати у source-checked release без окремого рішення. Призначення цього файлу: локальний repair/testbed для OCR, формул і термінології dynamic target models.

\section{Наступний prompt для Spark}

Найкращий короткий prompt:
\begin{quote}
Take one untranslated or partially translated TeX fragment from \texttt{P0\_arxiv\_1812.01537}. Translate only prose to Ukrainian. Preserve all math, labels, refs, citations, and macro names. Then patch only compile errors in the local wrapper. Do not summarize. Output updated \texttt{\_ua.tex}, a status row, and compile log.
\end{quote}

Після micro Lie наступні Spark-задачі: PySDR RST-to-TeX, Correll figure placeholders/cref repair, Labbe notebook markdown-to-TeX skeletons.


