**The Hopf instance: the predatum type is realized by the sheaf of Hopf sections; internal inhabitation without a global point; the cowlick class is twice the Hopf class; and the blind-plane circles are a Hopf link.**

*Eighteenth note; executes the typing task flagged in [N17] (predatum witness candidate) after the poser confirmed the Hopf object as "an instance of the logic" from his own conception. Method: the three claims were first subjected to a two-referee adversarial pass (topos/category lens and differential-topology lens, instructed to refute; references Mac Lane–Moerdijk, Milnor–Stasheff, Hatcher; full reports in the session record). No claim was refuted; two certificates were corrected — one a genuine algebra slip of this lane's drafting, owned below. Exact verification: `verify_hopf_predatum_note18.py`, 12/12 symbolic checks; referee numerics (Gauss linking = +1 to <10⁻⁵) recorded, not re-run.*

*Setting.* η: S³ → S², η(z₁,z₂) = (2z₁z̄₂, |z₁|²−|z₂|²) — a principal U(1)-bundle (well-definedness and fibre-invariance verified exactly, checks 1–2). Let T ∈ Sh(S²) be the sheaf of continuous local sections of η.

**Theorem 1 (T realizes the predatum type).** In the sheaf topos Sh(S²): (a) T → 1 is an epimorphism — explicit sections exist over each chart of the two-chart cover, exhibited exactly: s₀(z) = (z,1)/√(1+|z|²) over S²∖{N} and s₁(w) = (1,w)/√(1+|w|²) over S²∖{S} (checks 3–4); local triviality is the load-bearing hypothesis and it holds. Hence by Kripke–Joyal semantics the internal statement "T is inhabited" is valid. (b) Hom(1,T) = Γ(S²,T) = ∅: a global section would trivialize the principal bundle, but the clutching function on the equator is e^{it} — winding 1, non-nullhomotopic (verified exactly, check 5). (c) Therefore T is non-initial — indeed well-supported, internally inhabited — with **no global point**: precisely the predatum type P ≇ 0, Hom(1,P) = ∅ of [N17], in the strong form *internally non-empty, globally uninhabited*. The poser's "non-empty and non-inhabited," impossible in Set, is the standard gap between local/internal existence and global existence in a sheaf topos — realized here by the most classical object available. (d) T is a torsor under the sheaf C⁰(−,U(1)), classified by a **generator** of H¹(S², C⁰(−,U(1))) ≅ H²(S²,ℤ) ≅ ℤ (the isomorphism uses paracompactness of S²: the exponential sheaf sequence plus fineness of C⁰(−,ℝ); referee-confirmed, stated here with its hypothesis). The predatum obstruction of this instance is the integer 1 — a generator, filed beside the program's other load-bearing "ones" ([N15]) as an observation, not an identification. ∎ (a–c verified exactly; d referee-confirmed at statement level.)

**Theorem 2 (the cowlick class is twice the Hopf class; referee-normalized form).** Fix the generator h = e(H) ∈ H²(S²,ℤ) ≅ ℤ, where H is the line bundle with clutching z ↦ z (Hatcher's convention, ≅ O(1), e = +1). Then TS² ≅ H ⊗ H as complex line bundles, with clutching degree 2 (tangent transition ∂/∂w = −z²·∂/∂z — the *cotangent* transition is dw = −z⁻²dz; the earlier conflation is corrected, check 7), and $$e(TS²) = 2h, \qquad e(TS²)[S²] = χ(S²) = 2.$$ With the tautological convention (O(−1), whose unit-circle bundle is literally η) the equation reads e(TS²) = −2·e(O(−1)); in every convention |e(TS²)| = 2|e(Hopf)|. Consequence for the program: the hairy-ball/cowlick obstruction of [N16b] (total index 2, realized by the −w² single-zero field — which is itself the degree-2 clutching computation) and the predatum obstruction of Theorem 1 live in the same group H²(S²,ℤ) ≅ ℤ, and **the cowlick class is exactly twice the Hopf class**. The two "instances of the logic" the poser pointed at are the same integer invariant, one the double of the other. ∎ (Clutching degrees exact; normalization per referees.)

**Theorem 3 (the blind-plane circles form a Hopf link; corrected certificate).** The [P126] source circles K_G(t) = (√2 cos t, 0, 1+√2 sin t) and K_C(t) = (0, √2 cos t, −1+√2 sin t): (a) are disjoint — any common point lies on the Z-axis, and the axis-height sets {1±√2}, {−1±√2} are disjoint (exact, check 9); (b) form a link with lk = +1 — K_C meets the plane of K_G only at (0,0,−1±√2), and exactly one crossing lies inside K_G's disk (center (0,0,1), radius √2) by the **corrected certificate** $$(2−\sqrt2)^2 = 6−4\sqrt2 < 2 < 6+4\sqrt2 = (2+\sqrt2)^2,$$ transversally (check 12); the certificate is symmetric (K_G pierces K_C's disk once, same inequality), the axis crossings interleave C,G,C,G (check 11), and both referees' Gauss integrals give +1. *Erratum owned: the draft certificate compared (√2∓1)² with 2 — wrong quantities (the distances are 2∓√2, not √2∓1); right conclusion, wrong certificate; equivalent correct form (√2−1)² < 1 < (√2+1)².* (c) The fibres of η over the poles are the coordinate circles (exact, check 6); any two distinct fibres form the standard Hopf link (lk = ±1 = the Hopf invariant); and a linked pair of disjoint *round* circles is conformally the orthogonal-great-circle pair (Möbius geometry, referee 2), so K_G/K_C is a round presentation of the Hopf link — the caution being that linking number 1 alone would not suffice for wild curves; roundness closes it. Not claimed: that K_G/K_C are literal stereographic fibre images under a named projection. ∎

**Corollary 4 (the pictures were already of the predatum instance).** [P126]'s "blind complex planes" figure — the two clock circles of the Giuga/Carmichael registers — depicts a Hopf link, i.e. a pair of fibres of the bundle whose section-sheaf realizes the predatum type. And the chart formula through which [N16] built the zeta sphere is the same map by which the Hopf section projects: η(s₀(z)) = (2z, |z|²−1)/(1+|z|²) is *literally the inverse-stereographic formula* Σ of [N16]/[P126] (check 3 — this dropped out of the verification unbidden). So the program's K₀ candidate, its violation-coordinate geometry, and its blind-plane visualization are one construction viewed three ways: total space (the link of fibres), base chart (the zeta sphere), and section sheaf (the predatum object). Filed as proved identities plus one structural reading; no new claim about ζ is made.

*Status.* Theorems 1–3 as scoped: 12/12 exact symbolic checks on this machine; topos-level steps (Kripke–Joyal, torsor classification) referee-confirmed with their hypotheses (local triviality; paracompactness) now stated explicitly; Gauss linking numeric. The typing discharges the [N17] queue item "type the atlas's Hopf quotient against the predatum shape" — in doctrine Sh(S²), affirmative. Open next: whether the predatum→K₁ localization ("choosing the 0/1 distinction locally") can be written as the choice-of-local-trivialization functor, and whether the obstruction generator's role transports along the program's other "ones" (observation only; Packet 199's surviving queue is the natural home). Provisional as always.

*Reflections.* The poser remembered the Hopf object as "an instance of the logic," and the typing confirms his memory with interest: it is arguably *the* instance — the minimal classical object that is everywhere-locally something and globally nothing, with obstruction exactly one generator. What the adversarial pass added is discipline the lane needed twice over: one referee caught a drafted certificate certifying the wrong quantities (the conclusion survived; the proof would not have), and the other pinned the sign conventions that make "twice the Hopf class" a theorem rather than a slogan. The unplanned find is Corollary 4's chart identity — the zeta sphere of [N16] was already the Hopf base in the program's own formulas, so the three lanes have been drawing the same object from three sides without asserting it. Now it is asserted, with the checks on disk.
