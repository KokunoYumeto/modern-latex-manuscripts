# Paper XI-XII source check notes

Paper XI was checked against Werke Band II pages 117-120, rendered here from source scan pages 128-133. The main scan-sensitive formulas retained in the working TeX are the potential

    phi = (1 + c^3/(2r^3))(lambda x + mu y + nu z),

velocity components with the `-3c^3/(2r^5)` terms, the resultant components `(2 pi/3)c^3 rho alpha`, etc., and the streamline equation

    (r^3 - c^3) sin^2(theta) = epsilon r.

Paper XII was checked against Werke Band II pages 121-138, rendered here from source scan pages 134-151. The rebuilt text includes the continuation through §7 and the final remaining case for primes of the form `8 mu + 1`. This avoids the selected-reader truncation/duplication around the reciprocity proof and preserves the scan formulas with the generalized Jacobi symbol.

The reading PDFs do not carry visible source-page labels. Scan images are included separately for auditability.
