# Machine-readable index layer

This tranche uses TeX as the lossless typesetting source and JSONL/CSV as the unit-alignment layer.

Unit pattern: `dirichlet.v1.p04b.<type>.<serial>` for French and `dirichlet.v1.p04b.en.<type>.<serial>` for English. Inventories are explicit even when null.
