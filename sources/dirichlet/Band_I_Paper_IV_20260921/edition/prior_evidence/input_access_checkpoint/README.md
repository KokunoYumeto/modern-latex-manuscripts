# Prompt 01: input-access checkpoint, not completion

**Prompt 01 is incomplete. The next cursor remains 1. Accepted author coverage is zero.**

The Project's 00_READ_FIRST.md, governing instructions 01, source hierarchy 03,
initial-state JSON 04, output contract 09 and Prompt 01 were read as searchable
text. Files 02 and 05-08 were not retrieved. No original project input files were
mounted in the execution filesystem. In particular, the named input-package ZIP,
controlling exact-scope PDF, original full-volume PDF and inherited R27/R28 ZIPs
were not available as bytes.

The archive name follows the requested session naming pattern, but this is only
a complete collection of this response's administrative state. It is NOT the
contract-complete cumulative edition. No original source bytes or inherited bytes
have been preserved here; no French or English edition, apparatus or reader has
been generated, compiled, rendered or accepted.

## What the evidence means

- evidence/retrieved_text contains reconstructed text representations returned by
  file search, not byte-identical copies of the named input files. Their measured
  hashes apply only to those representations.
- Expected source hashes in ledgers/REQUIRED_INPUTS.tsv are reported values from
  the source hierarchy, not successful hash checks.
- The topology and copy-matter ledgers record the instructions' expected mapping.
  They do not confirm the scan or replace 05_PAGE_MAP.tsv.
- SOURCE_BACKED_DIFF.tsv has only its required column header. No comparison took
  place, so no correction and no NONE result is asserted.
- Historical PASS claims in the retrieved packet-validation receipt were not
  accepted as current validation.

## Resume

Attach DIRICHLET_PAPER_IV_INPUT_PACKAGE.zip directly to this conversation,
including the original governing files 00-09, both authority PDFs and both
inherited ZIPs. Finish the prerequisite instruction read and byte validation,
then execute the remaining Prompt 01 work. Do not advance to Prompt 02.

## Non-circular integrity layout

The three sibling sidecars are CHECKPOINT.json, MANIFEST.tsv and
SOURCE_BACKED_DIFF.tsv. MANIFEST.tsv covers every ZIP member exactly once.
CHECKPOINT.json binds the ZIP, manifest, diff and every member with their exact
byte lengths and uppercase SHA-256 values. It also embeds the same cursor state
as state/CURSOR.json. The manifest and checkpoint remain outside the ZIP so that
neither must contain its own hash. CHECKPOINT.json does not hash itself.

Package CRC, member safety, manifest exactness and checkpoint binding checks apply
only to the generated access-checkpoint package, never to the unavailable sources
or unperformed manuscript audit. The standalone checkpoint records their results.
