# Continuation Prompt - Dirichlet Lane

You are continuing the Dirichlet source-first translation lane. Use the attached `Dirichlet_Round01_Stability_and_Audit_20260601` package plus the original starter packet.

Work standard:

- Do not use `Dirichlet - Selected Works.pdf` as source truth. It is a scaffold only.
- For every paper, render the source scan pages first from `dirichlet_werke_vol1_1889.pdf` or `dirichlet_werke_vol2_1897.pdf`.
- Rebuild editable original-language TeX from the scan, preserving formulas, footnotes, source titles, and references.
- Only then translate into English, preserving all mathematics.
- Return cumulative TeX/PDF and a short status note. Do not report every few pages; work in substantive blocks.
- If a formula is uncertain, keep the best TeX candidate, mark `[check scan p. ...]`, and continue. Do not invent or summarize.

Immediate next task:

Band II, Paper II: `Sur un moyen général de vérifier l'expression du potentiel relatif à une masse quelconque, homogène ou hétérogène`, source printed pages 9-16. Render the corresponding scan pages, produce source-checked French TeX, and produce a faithful English translation. Include both PDFs and the scan-page audit note.

Known current-reader hazards from Paper I:

- Formula terms can be missing, not merely OCR-misread.
- Equality/inequality can be wrong.
- Signs can be wrong (`-\lambda^2` appeared where the scan has `\lambda^2`).
- Broken footnote markers appear as red missing boxes in the current reader.

Treat every displayed formula as requiring visual confirmation against the scan.
