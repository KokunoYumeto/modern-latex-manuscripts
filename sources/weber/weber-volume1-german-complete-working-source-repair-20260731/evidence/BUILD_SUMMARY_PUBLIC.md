# Build summary

The producer built the supplied TeX with MiKTeX pdfTeX 1.40.29. The retained producer log has:

- 41,295 bytes
- SHA-256 `DD9DB34C805FE33D262BDA3113F1F386F013901DCA2806346E417C39789458C9`
- zero build errors
- zero undefined references
- zero ordinary LaTeX warnings
- zero overfull boxes
- zero underfull boxes
- zero missing-character diagnostics
- one nonblocking amsmath `foreign \over` warning

The released PDF is 420 A4 pages. Independent structural replay found no text-empty pages, no raster image XObjects, 26 font resources, and no Type 3 fonts. The reader currently has no internal link annotations; reference linking is not claimed by this checkpoint.

The cleaned public TeX was then rebuilt in isolation for three passes. The fresh PDF has 420 pages and 2,275,193 bytes. All 420 extracted-text pages, decoded page-content streams, and page geometries match the released reader exactly. Its SHA-256 differs only because pdfTeX wrote fresh creation and modification timestamps. The exact machine receipt is `ISOLATED_REBUILD_VALIDATION.json`.
