# Cumulative translation-method update through Paper 35

The cumulative branch retains the previous terminology normalizations: `dominio íntegro` / `整域`, `base íntegra` / `整基底`, division-ring-aware handling of noncommutative `Körper`, and class/residue-ring vocabulary from Papers 19-34.

Paper 35 adds the Hilbert-14 denominator-saturation vocabulary.  `Maximalbereich` is not treated as a maximal ideal.  `ganzzahlig` is context-sensitive: in the title and function-domain definitions it is translated as integer-coefficient language (`funciones de coeficientes enteros` / `整係数関数`); in `relativ-ganze Funktionen` it is integrality language (`funciones relativamente enteras` / `相対的に整な関数`).

Global layout refinement: the two consecutive Paper 02 table pages in the cumulative PDFs are now standard A4 portrait pages.  The table cells and formulas are unchanged; only the prior landscape page wrappers were removed and the tables were fitted with `adjustbox` on ordinary pages.  This is a layout cleanup, not a textual or mathematical change.
