diff --git "C:\\Users\\Floris\\Documents\\interlanguage\\03_projects\\language_management\\cjk\\05_independent_checking\\noether_chinese\\paper35\\intake\\frozen_producer_package\\build\\zh-Hant-controlled\\Noether_Paper35_Chinese_CurrentAuthority_zh-Hant-controlled_v001.tex" "C:\\Users\\Floris\\Documents\\interlanguage\\03_projects\\language_management\\cjk\\05_independent_checking\\noether_chinese\\paper35\\candidate\\zh-Hant-controlled\\Noether_Paper35_Chinese_CurrentAuthority_zh-Hant-controlled_checker_candidate_v002.tex"
index daeca47..dd5e921 100644
--- "C:\\Users\\Floris\\Documents\\interlanguage\\03_projects\\language_management\\cjk\\05_independent_checking\\noether_chinese\\paper35\\intake\\frozen_producer_package\\build\\zh-Hant-controlled\\Noether_Paper35_Chinese_CurrentAuthority_zh-Hant-controlled_v001.tex"
+++ "C:\\Users\\Floris\\Documents\\interlanguage\\03_projects\\language_management\\cjk\\05_independent_checking\\noether_chinese\\paper35\\candidate\\zh-Hant-controlled\\Noether_Paper35_Chinese_CurrentAuthority_zh-Hant-controlled_checker_candidate_v002.tex"
@@ -1,10 +1,11 @@
-% Noether Paper 35 controlled-generic zh-Hant producer translation.
+% Noether Paper 35 controlled-generic zh-Hant independent-checker correction candidate.
 % Translation source: published invariant Paper-35 span, Zenodo 21699405,
 % LF snapshot SHA-256 DAED6EF21C297425F018C0AE6B23BC5BDD05C0B86984B3FC25FB5937DCBEBD6A.
 % Binder: NOETH-DE-BINDER-P35-20260804-001; decision ZH-D120.
-% Controlled-generic script transport only; every independent check pending.
+% Checker-owned candidate applying frozen findings ZHCHK-P35-F001--F011.
 % Controlled generic Traditional script only; not zh-Hant-TW/HK/MO prose.
-% Hans wording remains the lexical base; independent Hant checking is pending.
+% Hans wording remains the lexical base; no regional localization is claimed.
+% Controlled Hant normalizations apply frozen tooling findings ZHCHK-P35-F012 and F014.
 % No source, semantic, formula-content, terminology, visual, regional,
 % publication, archive, approval, or certification check is claimed.
 \documentclass[11pt]{article}
@@ -46,7 +47,7 @@
 \begin{document}
 \setcounter{footnote}{0}
 
-\section*{35. 關於整數係數函數構成的極大域。}
+\section*{35. 關於整數係數函數構成的極大整環。}
 
 \begin{center}
 艾米·諾特（哥廷根）撰。\par
@@ -58,24 +59,24 @@
 
 以下研究與相對整函數的希爾伯特問題\textsuperscript{1)}密切相關；其緣起是 E.\ Hecke 偶然提出的一個問題，他曾通過直接計算解決了一個自己用作輔助引理的特殊情形\textsuperscript{2)}。
 
-問題如下：給定域 $\mathfrak{Z}$ 內，由 $x_1,\ldots,x_n$ 的整數係數函數 --- 多項式、冪級數或它們的商 --- 構成的一個 \textit{極大域}，是指不能再通過加入整數分母而擴大的域；也就是說，只要 $a \cdot g(x)$ 屬於它 --- 其中 $g(x)$ 在 $\mathfrak{Z}$ 中 ---，就總能推出 $g(x)$ 也屬於它，這裡 $a$ 指非零有理整數。$\mathfrak{Z}$ 中的每個整環 $\mathfrak{I}$ 都唯一生成一個極大域 $\mathfrak{M}(\mathfrak{I})$；它由 $\mathfrak{Z}$ 中所有滿足下述條件的函數 $g(x)$ 構成：至少存在一個 $a \neq 0$，使得 $a \cdot g(x)$ 屬於 $\mathfrak{I}$。
+問題如下：給定整環 $\mathfrak{Z}$ 內，由 $x_1,\ldots,x_n$ 的整數係數函數 --- 多項式、冪級數或它們的商 --- 構成的一個 \textit{極大整環}，是指不能再通過加入整數分母而擴大的整環；也就是說，只要 $a \cdot g(x)$ 屬於它 --- 其中 $g(x)$ 在 $\mathfrak{Z}$ 中 ---，就總能推出 $g(x)$ 也屬於它，這裡 $a$ 指非零有理整數。$\mathfrak{Z}$ 中的每個整環 $\mathfrak{I}$ 都唯一生成一個極大整環 $\mathfrak{M}(\mathfrak{I})$；它由 $\mathfrak{Z}$ 中所有滿足下述條件的函數 $g(x)$ 構成：至少存在一個 $a \neq 0$，使得 $a \cdot g(x)$ 屬於 $\mathfrak{I}$。
 
-所要問的是：當原來的整環 $\mathfrak{I}$ 是有限生成的 --- 即（d.\,h.）它由 $\mathfrak{Z}$ 中有限多個函數 $f_1(x),\ldots,f_r(x)$ 的所有整數係數多項式構成 --- 時，關於這個極大域 $\mathfrak{M}(\mathfrak{I})$ 的分母 $a$ 能說些什麼。這裡取 $\mathfrak{Z}$ 為 $x_1,\ldots,x_n$ 的所有整數係數多項式或冪級數，或者（bezw.）為這樣的商：除 $f(x)$ 中出現的分母及其冪以外，不含其他分母。
+所要問的是：當原來的整環 $\mathfrak{I}$ 是有限生成的 --- 即它由 $\mathfrak{Z}$ 中有限多個函數 $f_1(x),\ldots,f_r(x)$ 的所有整數係數多項式構成 --- 時，關於這個極大整環 $\mathfrak{M}(\mathfrak{I})$ 的分母 $a$ 能說些什麼。這裡取 $\mathfrak{Z}$ 為 $x_1,\ldots,x_n$ 的所有整數係數多項式或冪級數，或者為這樣的商：除 $f(x)$ 中出現的分母及其冪以外，不含其他分母。
 
 我證明 --- 在冪級數或其商的情形，還須附加假設各 $f(x)$ 之間只有代數依賴關係 ---，總可以選取這些 $a$，使其中只出現有限多個不同的素數，不過一般而言這些素數的任意次冪都可能出現。
 
-後一點是立即清楚的：如果 $a\cdot g(x)$ 屬於 $\mathfrak{I}$，但對任何指數 $\varrho$，$a^{\varrho-1}g(x)^{\varrho}$ 都不屬於 $\mathfrak{I}$，那麼所有冪 $a^{\varrho}$ 都會出現（例如（z.\,B.）$\mathfrak{I}=[2x]$；$x^{\varrho}=(2x)^{\varrho}/2^{\varrho}$）。
+後一點是立即清楚的：如果 $a\cdot g(x)$ 屬於 $\mathfrak{I}$，但對任何指數 $\varrho$，$a^{\varrho-1}g(x)^{\varrho}$ 都不屬於 $\mathfrak{I}$，那麼所有冪 $a^{\varrho}$ 都會出現（例如 $\mathfrak{I}=[2x]$；$x^{\varrho}=(2x)^{\varrho}/2^{\varrho}$）。
 
 有界性問題只能表述如下：能否給出 $\mathfrak{M}(\mathfrak{I})$ 的一個一般為無限的生成元系，使相應分母中各素數的指數保持有界？由於所涉及的素數只有有限多個，這個問題根據已知論證\textsuperscript{3)}，與 $\mathfrak{M}(\mathfrak{I})$ 的有限性問題相同，因而也就是整數係數表述下相對整函數的希爾伯特問題 --- 這個問題我無法用這裡所採用的方法判定\textsuperscript{4)}。
 
-為了證明分母 $a$ 中只出現有限多個不同的素數 $p$，所依據的是：對於每個這樣的 $p$，生成 $\mathfrak{I}$ 的各 $f(x)$ 之間至少存在一個模 $p$ 關係，而這個關係取整數係數時並不作為 $x$ 的恆等式成立。換言之，令 $\mathfrak{o}$ 表示以未定元 $u_1,\ldots,u_r$ 構成的整數係數多項式域，則在對應 $u_i \to f_i(x)$ 下，$\mathfrak{I}$ 是 $\mathfrak{o}$ 的同態像；這一同態由 $\mathfrak{o}$ 中的一個素理想 $\mathfrak{a}$ 給出（即（d.\,h.）$\mathfrak{I}$ 與剩餘類環 $\mathfrak{o}/\mathfrak{a}$ 同構）。相應地，$\mathfrak{I}_p$ 是 $\mathfrak{o}_p$ 的同態像 --- 下標表示轉到模 $p$（mod.\ $p$）的剩餘類；除 $\mathfrak{I}$ 的分母中出現的有限多個素數外，這一轉化均可進行。--- 這個同態同樣由 $\mathfrak{o}_p$ 中的一個素理想 $\mathfrak{c}_p$ 給出，並且它包含 $\mathfrak{a}_p$。當且僅當 $\mathfrak{c}_p$ 成為 $\mathfrak{a}_p$ 的真上理想（理想意義下的真因子）時，$p$ 才會出現在 $\mathfrak{M}(\mathfrak{I})$ 的分母中。這只有在下列兩種情形之一才可能發生：$\mathfrak{I}_p$ 相對於 $\mathfrak{I}$ 的超越次數降低，或者 $\mathfrak{a}_p$ 喪失其素理想性質\textsuperscript{5)}。這隻會對有限多個 $p$ 發生，原因是模 $p$ 的代數依賴關係同樣會導致函數行列式消失（\S\,1），而且 $\mathfrak{a}$ 還可證明是絕對素理想，因此它只會模有限多個 $p$ 喪失其素理想性質\textsuperscript{6)}。
+為了證明分母 $a$ 中只出現有限多個不同的素數 $p$，所依據的是：對於每個這樣的 $p$，生成 $\mathfrak{I}$ 的各 $f(x)$ 之間至少存在一個模 $p$ 關係，而這個關係取整數係數時並不作為 $x$ 的恆等式成立。換言之，令 $\mathfrak{o}$ 表示以未定元 $u_1,\ldots,u_r$ 構成的整數係數多項式環，則在對應 $u_i \to f_i(x)$ 下，$\mathfrak{I}$ 是 $\mathfrak{o}$ 的同態像；這一同態由 $\mathfrak{o}$ 中的一個素理想 $\mathfrak{a}$ 給出（即 $\mathfrak{I}$ 與剩餘類環 $\mathfrak{o}/\mathfrak{a}$ 同構）。相應地，$\mathfrak{I}_p$ 是 $\mathfrak{o}_p$ 的同態像 --- 下標表示轉到模 $p$ 的剩餘類；除 $\mathfrak{I}$ 的分母中出現的有限多個素數外，這一轉化均可進行。--- 這個同態同樣由 $\mathfrak{o}_p$ 中的一個素理想 $\mathfrak{c}_p$ 給出，並且它包含 $\mathfrak{a}_p$。當且僅當 $\mathfrak{c}_p$ 成為 $\mathfrak{a}_p$ 的真上理想（理想意義下的真因子）時，$p$ 才會出現在 $\mathfrak{M}(\mathfrak{I})$ 的分母中。這只有在下列兩種情形之一才可能發生：$\mathfrak{I}_p$ 相對於 $\mathfrak{I}$ 的超越次數降低，或者 $\mathfrak{a}_p$ 喪失其素理想性質\textsuperscript{5)}。這只會對有限多個 $p$ 發生，原因是模 $p$ 的代數依賴關係同樣會導致雅可比行列式消失（\S\,1），而且 $\mathfrak{a}$ 還可證明是絕對素理想，因此它只會模有限多個 $p$ 喪失其素理想性質\textsuperscript{6)}。
 
 還應指出，如果用某個（有限）代數數域的整數及其素理想代替有理整數，那麼上述全部論證經少量修改仍然成立（\S\,4）。
 
 % -----------------------------------------------------------
 \bigskip
 \S~\textbf{1.}
-\textbf{特徵為 $p$ 的係數域上的函數行列式。}
+\textbf{特徵為 $p$ 的係數域上的雅可比行列式。}
 
 \smallskip
 
@@ -85,10 +86,10 @@
 於是，與特徵為零的情形一樣，有：
 
 \noindent\textbf{定理。}
-\textit{如果各 $f(x)$ 之間存在一個係數取自 $\Omega$ 的代數依賴關係，那麼函數矩陣 $(\partial f_i/\partial x_k)$（$i=1,\ldots,t$；$k=1,\ldots,n$）的秩小於 $t$}\textsuperscript{7)}。
+\textit{如果各 $f(x)$ 之間存在一個係數取自 $\Omega$ 的代數依賴關係，那麼雅可比矩陣 $(\partial f_i/\partial x_k)$（$i=1,\ldots,t$；$k=1,\ldots,n$）的秩小於 $t$}\textsuperscript{7)}。
 
 \noindent\textit{證明。}
-在多項式域 $\Omega[u_1,\ldots,u_t]$ 中，以 $\mathfrak{m}$ 表示所有滿足 $G(f) = 0$ 關於 $x$ 恆等成立的多項式 $G(u)$ 所構成的素理想。根據假設，$\mathfrak{m}$ 不同於零理想；從 $\mathfrak{m}$ 中取一個次數最低的多項式 $G(u) \neq 0$（從而 $\neq {const.}$），由此特別可知 $G(u)$ 不可約。於是像通常那樣得到：
+在多項式環 $\Omega[u_1,\ldots,u_t]$ 中，以 $\mathfrak{m}$ 表示所有滿足 $G(f) = 0$ 關於 $x$ 恆等成立的多項式 $G(u)$ 所構成的素理想。根據假設，$\mathfrak{m}$ 不同於零理想；從 $\mathfrak{m}$ 中取一個次數最低的多項式 $G(u) \neq 0$（從而 $\neq {const.}$），由此特別可知 $G(u)$ 不可約。於是像通常那樣得到：
 \[
 \begin{aligned}
 \left(\frac{\partial G}{\partial u_1}\right)_{f}\frac{\partial f_1}{\partial x_1}
@@ -100,18 +101,18 @@
 \left(\frac{\partial G}{\partial u_t}\right)_{f}\frac{\partial f_t}{\partial x_n}&=0,
 \end{aligned}
 \]
-由此可知，要麼函數矩陣的秩小於 $t$，要麼所有 $\left(\partial G/\partial u_i\right)_{f}$ 都消失。但由於已假定 $G(u)$ 是次數最低的多項式，因而可以推出所有 $\partial G/\partial u_i$ 都消失。
+由此可知，要麼雅可比矩陣的秩小於 $t$，要麼所有 $\left(\partial G/\partial u_i\right)_{f}$ 都消失。但由於已假定 $G(u)$ 是次數最低的多項式，因而可以推出所有 $\partial G/\partial u_i$ 都消失。
 
 於是得到：$G(u)=H(u_1^p,\ldots,u_t^p)$；又因為已假定係數域 $\Omega$ 是代數閉域，從而是完美域，所以進一步有：$G(u)=H(u_i^p)=(T(u))^p$；這與 $G(u)$ 的選取相矛盾。--- 這一證明自然也適用於特徵為零的情形，此時不需要最後一步推論。
 
 \medskip\noindent\textbf{2.} \textbf{推論。}
 
-設 $F_1(x_1,\ldots,x_n),\ldots,F_t(x_1,\ldots,x_n)$ 是整數係數函數（多項式、冪級數或它們的商），其函數矩陣的秩恰為 $t$。選取素數 $p$，使它既不出現在 $F(x)$ 的分母中，也不同時整除函數矩陣的所有 $t$ 階行列式。
+設 $F_1(x_1,\ldots,x_n),\ldots,F_t(x_1,\ldots,x_n)$ 是整數係數函數（多項式、冪級數或它們的商），其雅可比矩陣的秩恰為 $t$。選取素數 $p$，使它既不出現在 $F(x)$ 的分母中，也不同時整除雅可比矩陣的所有 $t$ 階行列式。
 
 那麼 $F_1(x),\ldots,F_t(x)$ 模 $p$ 後仍然代數無關。
 
 \noindent\textit{證明。}
-以 $P$ 表示模 $p$ 的剩餘類域，並以 $f_1(x),\ldots,f_t(x)$ 表示 $P$ 中的函數，它們由 $F_1(x),\ldots,F_t(x)$ 把整數係數替換為其模 $p$ 剩餘類而得到。由於 $p$ 不出現在 $F_i(x)$ 的分母中，$f_i(x)$ 是有定義的；其函數矩陣 $(\partial f_i/\partial x_k)$ 同樣由 $(\partial F_i/\partial x_k)$ 把整數係數替換為其模 $p$ 剩餘類而得到 --- 在 $(\partial F_i/\partial x_k)$ 中不會出現 $F(x)$ 所沒有的其他分母。--- 根據 $p$ 的選取，$(\partial f_i/\partial x_k)$ 的秩仍為 $t$；因此各 $f(x)$ 在 $\Omega$ 中代數無關，從而更在 $P$ 中代數無關。
+以 $P$ 表示模 $p$ 的剩餘類域，並以 $f_1(x),\ldots,f_t(x)$ 表示 $P$ 中的函數，它們由 $F_1(x),\ldots,F_t(x)$ 把整數係數替換為其模 $p$ 剩餘類而得到。由於 $p$ 不出現在 $F_i(x)$ 的分母中，$f_i(x)$ 是有定義的；其雅可比矩陣 $(\partial f_i/\partial x_k)$ 同樣由 $(\partial F_i/\partial x_k)$ 把整數係數替換為其模 $p$ 剩餘類而得到 --- 在 $(\partial F_i/\partial x_k)$ 中不會出現 $F(x)$ 所沒有的其他分母。--- 根據 $p$ 的選取，$(\partial f_i/\partial x_k)$ 的秩仍為 $t$；因此各 $f(x)$ 在 $\Omega$ 中代數無關，從而更在 $P$ 中代數無關。
 
 % -----------------------------------------------------------
 \bigskip
@@ -121,29 +122,29 @@
 \smallskip
 
 \noindent\textbf{1.}
-如引言中那樣，以 $\mathfrak{I}$ 表示由 $x_1,\ldots,x_n$ 的整數係數函數構成的一個整環（無零因子環）--- 即整數係數的多項式或冪級數，或者它們的商。--- 在冪級數的情形，可以是形式定義的冪級數，也可以是在某一區域內收斂的冪級數；這裡只用到所出現的函數構成一個整環這一點。
+如引言中那樣，以 $\mathfrak{I}$ 表示由 $x_1,\ldots,x_n$ 的整數係數函數構成的一個整環（無零因子環）--- 即整數係數的多項式或冪級數，或者它們的商。對於冪級數，可以是形式定義的冪級數，也可以是在某一區域內收斂的冪級數；這裡只用到所出現的函數構成一個整環這一點。
 
-以 $\mathfrak{Q}$ 表示 $\mathfrak{I}$ 的分式域；$\mathfrak{Z}$ 表示介於 $\mathfrak{I}$ 與 $\mathfrak{Q}$ 之間的一個整環。如引言所述，如果由 $a \cdot g(x)$ 屬於 $\mathfrak{Z}$ 中的 $\mathfrak{M}$ --- 其中 $a$ 是整數且 $\neq 0$，$g(x)$ 在 $\mathfrak{Z}$ 中 --- 總能推出 $g(x)$ 屬於它，就稱 $\mathfrak{M}$ 在 $\mathfrak{Z}$ 中是 \textit{極大的}。對於 $\mathfrak{I}$，存在唯一一個由 $\mathfrak{I}$ 在 $\mathfrak{Z}$ 中生成的極大域 $\mathfrak{M}(\mathfrak{I})$；它是 $\mathfrak{Z}$ 中所有包含 $\mathfrak{I}$ 的極大域之交 --- 因為 $\mathfrak{Z}$ 本身就是這樣的極大域，而且任意多個極大域的交仍然極大幷包含 $\mathfrak{I}$。$\mathfrak{M}(\mathfrak{I})$ 由 $\mathfrak{Z}$ 中所有滿足下述條件的函數 $g(x)$ 構成：至少存在一個 $a \neq 0$，使 $a \cdot g(x)$ 位於 $\mathfrak{I}$ 中。
+以 $\mathfrak{Q}$ 表示 $\mathfrak{I}$ 的分式域；$\mathfrak{Z}$ 表示介於 $\mathfrak{I}$ 與 $\mathfrak{Q}$ 之間的一個整環。如引言所述，如果由 $a \cdot g(x)$ 屬於 $\mathfrak{Z}$ 中的 $\mathfrak{M}$ --- 其中 $a$ 是整數且 $\neq 0$，$g(x)$ 在 $\mathfrak{Z}$ 中 --- 總能推出 $g(x)$ 屬於它，就稱 $\mathfrak{M}$ 在 $\mathfrak{Z}$ 中是 \textit{極大的}。對於 $\mathfrak{I}$，存在唯一一個由 $\mathfrak{I}$ 在 $\mathfrak{Z}$ 中生成的極大整環 $\mathfrak{M}(\mathfrak{I})$；它是 $\mathfrak{Z}$ 中所有包含 $\mathfrak{I}$ 的極大整環之交 --- 因為 $\mathfrak{Z}$ 本身就是這樣的極大整環，而且任意多個極大整環的交仍然極大並包含 $\mathfrak{I}$。$\mathfrak{M}(\mathfrak{I})$ 由 $\mathfrak{Z}$ 中所有滿足下述條件的函數 $g(x)$ 構成：至少存在一個 $a \neq 0$，使 $a \cdot g(x)$ 位於 $\mathfrak{I}$ 中。
 
-事實上，這些 $g(x)$ 構成 $\mathfrak{Z}$ 中一個包含 $\mathfrak{I}$ 的極大域 $\mathfrak{N}$，因為若 $b \cdot h(x)$ 位於 $\mathfrak{I}$ 中，且 $b \neq 0$、$h(x)$ 在 $\mathfrak{Z}$ 中，就可推出 $ab \cdot h(x)$ 位於 $\mathfrak{I}$ 中且 $ab \neq 0$，因而 $h(x)$ 位於 $\mathfrak{N}$ 中。另一方面，$\mathfrak{Z}$ 中每個包含 $\mathfrak{I}$ 的極大域 $M$ 都包含 $\mathfrak{N}$；因為若 $a \cdot g(x)$ 位於 $\mathfrak{I}$ 中，則由於 $\mathfrak{I}$ 位於 $\mathfrak{M}$ 中，$a \cdot g(x)$ 也位於 $\mathfrak{M}$ 中；所以 $g(x)$ 位於 $\mathfrak{M}$ 中。
+事實上，這些 $g(x)$ 構成 $\mathfrak{Z}$ 中一個包含 $\mathfrak{I}$ 的極大整環 $\mathfrak{N}$，因為若 $b \cdot h(x)$ 位於 $\mathfrak{I}$ 中，且 $b \neq 0$、$h(x)$ 在 $\mathfrak{Z}$ 中，就可推出 $ab \cdot h(x)$ 位於 $\mathfrak{I}$ 中且 $ab \neq 0$，因而 $h(x)$ 位於 $\mathfrak{N}$ 中。另一方面，$\mathfrak{Z}$ 中每個包含 $\mathfrak{I}$ 的極大整環 $M$ 都包含 $\mathfrak{N}$；因為若 $a \cdot g(x)$ 位於 $\mathfrak{I}$ 中，則由於 $\mathfrak{I}$ 位於 $\mathfrak{M}$ 中，$a \cdot g(x)$ 也位於 $\mathfrak{M}$ 中；所以 $g(x)$ 位於 $\mathfrak{M}$ 中。
 
 \medskip\noindent\textbf{2.}
-現在假定 $\mathfrak{I}$ 是帶單位元的有限生成整環，以 $f_1(x),\ldots,f_r(x)$ 為整環基；即（d.\,h.）$\mathfrak{I}$ 由各 $f_i(x)$ 的所有整數係數多項式構成。再令 $\mathfrak{Z}$ 由 $x$ 的所有整數係數多項式或冪級數構成，或者（bezw.）由這樣的商構成：除有限多個 $f_i(x)$ 中出現的分母外，不含其他分母；這些分母當然可以取任意次冪。
+現在假定 $\mathfrak{I}$ 是帶單位元的有限生成整環，以 $f_1(x),\ldots,f_r(x)$ 為整性基（即有限代數生成組）；換言之，$\mathfrak{I}$ 由各 $f_i(x)$ 的所有整數係數多項式構成。再令 $\mathfrak{Z}$ 由 $x$ 的所有整數係數多項式或冪級數構成，或者由這樣的商構成：除有限多個 $f_i(x)$ 中出現的分母外，不含其他分母；這些分母當然可以取任意次冪。
 
 再假定至少有一個 $f_i(x)$ --- 一般而言所有 $f_i(x)$ --- 中確實出現 $x$，否則 $\mathfrak{I}$ 與 $\mathfrak{Z}$ 就只由分數 $1/c$ 的所有整數係數多項式構成，並無任何事情需要證明。
 
-若所考慮的是多項式或有理函數，那麼分式域 $\mathfrak{Q}$ 相對於有理數域 $K$ 的超越次數為 $t$，其中 $1 \leq t \leq r$；如果例如 $f_1(x),\ldots,f_t(x)$ 構成一個不可約系統（注\textsuperscript{5)}），則 $\mathfrak{Q}$ 是純超越域 $K(f_1(x),\ldots,f_t(x))$ 的有限代數擴張。同時，$f_1(x),\ldots,f_r(x)$ 的函數矩陣以及 $f_1(x),\ldots,f_t(x)$ 的函數矩陣，其秩都恰為 $t$\textsuperscript{8)}。
+若所考慮的是多項式或有理函數，那麼分式域 $\mathfrak{Q}$ 相對於有理數域 $K$ 的超越次數為 $t$，其中 $1 \leq t \leq r$；如果例如 $f_1(x),\ldots,f_t(x)$ 構成一個代數無關系統（原文稱“不可約系統”；見注\textsuperscript{5)}），則 $\mathfrak{Q}$ 是純超越域 $K(f_1(x),\ldots,f_t(x))$ 的有限代數擴張。同時，$f_1(x),\ldots,f_r(x)$ 的雅可比矩陣以及 $f_1(x),\ldots,f_t(x)$ 的雅可比矩陣，其秩都恰為 $t$\textsuperscript{8)}。
 
-為了使 $\mathfrak{Q}$ 的這一結構在冪級數或其商的情形仍然保持，必須附加一個假設：若 $t$ 是各 $f_i(x)$ 的函數矩陣的秩，並且例如它同時也是 $f_1(x),\ldots,f_t(x)$ 的函數矩陣的秩 --- 從而這些函數按 \S\,1，1.\ 構成一個不可約系統 ---，那麼仍要求 $\mathfrak{Q}$ 是純超越域 $K(f_1(x),\ldots,f_t(x))$ 的有限代數擴張；即（d.\,h.）$f_{t+1}(x),\ldots,f_r(x)$ 應當相對於 $K(f_1(x),\ldots,f_t(x))$ 是代數的\textsuperscript{9)}。這裡仍有 $t \geq 1$，因為 --- 特徵為零！--- 並非所有 $\partial f_i/\partial x_k$ 都消失。
+為了使 $\mathfrak{Q}$ 的這一結構在冪級數或其商的情形仍然保持，必須附加一個假設：若 $t$ 是各 $f_i(x)$ 的雅可比矩陣的秩，並且例如它同時也是 $f_1(x),\ldots,f_t(x)$ 的雅可比矩陣的秩 --- 從而這些函數按 \S\,1，1.\ 構成一個代數無關系統 ---，那麼仍要求 $\mathfrak{Q}$ 是純超越域 $K(f_1(x),\ldots,f_t(x))$ 的有限代數擴張；即 $f_{t+1}(x),\ldots,f_r(x)$ 應當相對於 $K(f_1(x),\ldots,f_t(x))$ 是代數的\textsuperscript{9)}。這裡仍有 $t \geq 1$，因為 --- 特徵為零！--- 並非所有 $\partial f_i/\partial x_k$ 都消失。
 
-同時，這個條件對於整環基中任意 $t$ 個函數所組成的系統也成立，只要其函數矩陣的秩恰為 $t$；因為它們構成一個不可約系統，其餘函數因而代數依賴於它們。
+同時，這個條件對於整性基中任意 $t$ 個函數所組成的系統也成立，只要其雅可比矩陣的秩恰為 $t$；因為它們構成一個代數無關系統，其餘函數因而代數依賴於它們。
 \medskip\noindent\textbf{3.}
 現令 $\mathfrak{o}$ 表示不定元 $u_1,\ldots,u_r$ 上的整係數多項式環，那麼由對應 $u_i \to f_i(x)$ 立即可知，$\mathfrak{I}$ 是 $\mathfrak{o}$ 的環同態像。因此 $\mathfrak{I}$ 與 $\mathfrak{o}/\mathfrak{a}$ 環同構，其中 $\mathfrak{a}$ 是素理想，因為 $\mathfrak{I}$ 是無零因子環；$\mathfrak{a}$ 由所有使 $G(f)$ 關於 $x$ 恆等為零的多項式 $G(u)$ 構成。
 
 正如引言中已經提到的，這裡所涉及的是以下定理：
 
 \medskip\noindent\textbf{定理。}
-\textit{在2.的附加假設下，令 $\mathfrak{o}_p$、$\mathfrak{a}_p$、$\mathfrak{I}_p$ 分別表示由 $\mathfrak{o}$、$\mathfrak{a}$、$\mathfrak{I}$ 通過把整係數替換為它們模 $p$ 的剩餘類而得到的對象。那麼，即使在此情形下，除至多有限多個素數外 --- 為使 $\mathfrak{I}_p$ 有定義，$f(x)$ 的分母中出現的素數也在這些例外之列 ---，從 $\mathfrak{o}_p$ 到 $\mathfrak{I}_p$ 的同態也由 $\mathfrak{a}_p$ 誘導，因而 $\mathfrak{I}_p$ 與 $\mathfrak{o}_p/\mathfrak{a}_p$ 環同構，從而也與 $\mathfrak{o}/(\mathfrak{a}, p\mathfrak{o})$ 環同構。}
+\textit{在第2點的附加假設下，令 $\mathfrak{o}_p$、$\mathfrak{a}_p$、$\mathfrak{I}_p$ 分別表示由 $\mathfrak{o}$、$\mathfrak{a}$、$\mathfrak{I}$ 通過把整係數替換為它們模 $p$ 的剩餘類而得到的對象。那麼，即使在此情形下，除至多有限多個素數外 --- 為使 $\mathfrak{I}_p$ 有定義，$f(x)$ 的分母中出現的素數也在這些例外之列 ---，從 $\mathfrak{o}_p$ 到 $\mathfrak{I}_p$ 的同態也由 $\mathfrak{a}_p$ 誘導，因而 $\mathfrak{I}_p$ 與 $\mathfrak{o}_p/\mathfrak{a}_p$ 環同構，從而也與 $\mathfrak{o}/(\mathfrak{a}, p\mathfrak{o})$ 環同構。}
 
 藉助下述推論，由該定理可知，$\mathfrak{M}(\mathfrak{I})$ 的分母 $a$ 中只出現有限多個素數：
 
@@ -154,10 +155,10 @@
 
 $\mathfrak{o}_p/\mathfrak{a}_p$ 與 $\mathfrak{o}/(\mathfrak{a}, p\mathfrak{o})$ 同構。事實上，按照定義，$\mathfrak{o}_p$ 等於剩餘類環 $\mathfrak{o}/p\mathfrak{o}$，相應地，$\mathfrak{a}_p$ 等於 $(\mathfrak{a}, p\mathfrak{o})/p\mathfrak{o}$。因此 --- 由第一同構定理 ---，$\mathfrak{o}_p/\mathfrak{a}_p$，並且根據假設 $\mathfrak{I}_p$，都與 $\mathfrak{o}/(\mathfrak{a}, p\mathfrak{o})$ 同構。
 
-而這意味着：由\[
-H(f_i(x))\equiv0\ (p)\quad\hbox{于 }\mathfrak{I}
+這意味着：\[
+H(f_i(x))\equiv0\ (p)\quad\hbox{在 }\mathfrak{I}\hbox{ 中}
 \quad\Longrightarrow\quad
-H(u)\equiv0\ (\mathfrak{a},p\mathfrak{o})\quad\hbox{于 }\mathfrak{o}.
+H(u)\equiv0\ (\mathfrak{a},p\mathfrak{o})\quad\hbox{在 }\mathfrak{o}\hbox{ 中}.
 \]
 
 這可以改寫為：由 $H(f_i(x)) = p \cdot g(x)$ [$H(f_i(x))$ 屬於 $\mathfrak{I}$；$g(x)$ 屬於 $\mathfrak{Z}$] 可得 $H(u_i)-pF(u_i)\equiv0\ (\mathfrak{a})$，因而還有 $H(f_i(x)) = pF(f_i(x))$，進而 $g(x) = F(f_i(x))$；所以 $g(x)$ 屬於 $\mathfrak{I}$。
@@ -174,7 +175,7 @@ H(u)\equiv0\ (\mathfrak{a},p\mathfrak{o})\quad\hbox{于 }\mathfrak{o}.
 \noindent\textbf{1.} \textbf{輔助定理：}
 \textit{決定 $\mathfrak{I}$ 與 $\mathfrak{o}$ 之間同態的素理想 $\mathfrak{a}$（\S\,2, 3.）是絕對素理想。}
 
-令 $\mathfrak{o}^*$ 表示不定元 $u_1,\ldots,u_r$ 上以任意代數數 --- 包括分數代數數 --- 為係數的多項式環，$\mathfrak{a}^*$ 表示 $\mathfrak{o}^*$ 中由 $\mathfrak{a}$ 生成的理想；因此，$\mathfrak{a}^*$ 由所有線性組合 $a_1 G_1(u) + \cdots + a_s G_s(u)$ 構成，其中 $a_i$ 是任意代數數，$G_i(u)$ 是 $\mathfrak{a}$ 中的多項式。要證明 $\mathfrak{a}^*$ 是素理想。
+令 $\mathfrak{o}^*$ 表示不定元 $u_1,\ldots,u_r$ 上以任意代數數 --- 也包括非整代數數 --- 為係數的多項式環，$\mathfrak{a}^*$ 表示 $\mathfrak{o}^*$ 中由 $\mathfrak{a}$ 生成的理想；因此，$\mathfrak{a}^*$ 由所有線性組合 $a_1 G_1(u) + \cdots + a_s G_s(u)$ 構成，其中 $a_i$ 是任意代數數，$G_i(u)$ 是 $\mathfrak{a}$ 中的多項式。要證明 $\mathfrak{a}^*$ 是素理想。
 
 只需證明從 $\mathfrak{o}^*$ 到 $\mathfrak{I}^*$ 的同態由 $\mathfrak{a}^*$ 誘導 --- 此處 $\mathfrak{I}^*$ 表示由 $f_i(x)$ 的所有以任意代數數為係數的多項式組成的整環 ---，即當 $H(f_i(x))$ 為零時，$H(u)$ 屬於 $\mathfrak{a}^*$（$H(u)$ 屬於 $\mathfrak{o}^*$）。
 
@@ -197,13 +198,13 @@ a \cdot H(u) = \omega_1 H_1(u) + \cdots + \omega_s H_s(u),
 
 根據前文，關於條件 I、II、III 的例外素數只有有限多個，這是直接可得的。
 
-I 是顯然的：例外素數只可能是 $f_i(x)$ 的分母中出現的有限多個素數；II 源於輔助定理所表明的 $\mathfrak{a}$ 是絕對素理想，因而此性質在模除至多有限多個素數之外的所有素數時仍得以保持（參見注\textsuperscript{6}））。最後，III 已在推論 \S\,1, 2. 中證明，因為根據 \S\,2, 2.，$f_1(x),\ldots,f_t(x)$ 的函數矩陣的秩恰好為 $t$。
+I 是顯然的：例外素數只可能是 $f_i(x)$ 的分母中出現的有限多個素數；II 源於輔助定理所表明的 $\mathfrak{a}$ 是絕對素理想，因而此性質在模除至多有限多個素數之外的所有素數時仍得以保持（參見注\textsuperscript{6)}）。最後，III 已在推論 \S\,1, 2. 中證明，因為根據 \S\,2, 2.，$f_1(x),\ldots,f_t(x)$ 的雅可比矩陣的秩恰好為 $t$。
 
 現在選取一個不同於這些例外素數的 $p$。從 $\mathfrak{o}_p$ 到 $\mathfrak{I}_p$ 的同態由一個理想 $\mathfrak{c}_p$ 誘導；由於 $\mathfrak{I}_p$ 也是無零因子環，$\mathfrak{c}_p$ 是素理想，並且它的超越次數由 $\mathfrak{I}_p$ 的超越次數給出，因而至少為 $t$。事實上，由於 $\mathfrak{I}_p$ 與 $\mathfrak{o}_p/\mathfrak{c}_p$ 同構，$\mathfrak{c}_p$ 的剩餘類域便與 $\mathfrak{I}_p$ 的分式域同構。
 
-由於 $\mathfrak{a}_p$ 也是素理想，並且是 $\mathfrak{c}_p$ 的倍理想（子理想），所以 $\mathfrak{a}_p$ 與 $\mathfrak{c}_p$ 當且僅當二者的超越次數相同時才相同\textsuperscript{10)}。$\mathfrak{a}_p$ 作為 $\mathfrak{c}_p$ 的倍理想，其超越次數同樣至少為 $t$；事實上，$u_1,\ldots,u_t$ 模 $\mathfrak{a}_p$ 的類構成一個不可約系統。需要證明這個超越次數恰好等於 $t$，這樣一切就都得證了。
+由於 $\mathfrak{a}_p$ 也是素理想，並且包含於 $\mathfrak{c}_p$（即 $\mathfrak{a}_p$ 是 $\mathfrak{c}_p$ 的子理想），所以 $\mathfrak{a}_p$ 與 $\mathfrak{c}_p$ 當且僅當二者的超越次數相同時才相同\textsuperscript{10)}。$\mathfrak{a}_p$ 作為 $\mathfrak{c}_p$ 的子理想，其超越次數同樣至少為 $t$；事實上，$u_1,\ldots,u_t$ 模 $\mathfrak{a}_p$ 的類構成一個代數無關系統。需要證明這個超越次數恰好等於 $t$，這樣一切就都得證了。
 
-這裡，在冪級數或其商的情形下，要用到關於 $\mathfrak{I}$ 的分式域 $\mathfrak{Q}$ 之結構的附加假設（\S\,2, 2.）。據此，素理想 $\mathfrak{a}$ 在每一種情形下的超越次數都是 $t$；並且編號的選取方式使得 $u_1,\ldots,u_t$ 模 $\mathfrak{a}$ 的類構成一個不可約系統，而 $u_{t+1},\ldots,u_r$ 的類代數地依賴於該系統。因此，$\mathfrak{a}$ 中包含 --- 整係數且本原的 --- 多項式
+這裡，在冪級數或其商的情形下，要用到關於 $\mathfrak{I}$ 的分式域 $\mathfrak{Q}$ 之結構的附加假設（\S\,2, 2.）。據此，素理想 $\mathfrak{a}$ 在每一種情形下的超越次數都是 $t$；並且編號的選取方式使得 $u_1,\ldots,u_t$ 模 $\mathfrak{a}$ 的類構成一個代數無關系統，而 $u_{t+1},\ldots,u_r$ 的類代數地依賴於該系統。因此，$\mathfrak{a}$ 中包含 --- 整係數且本原的 --- 多項式
 \[
 G_1(u_{t+1};u_1,\ldots,u_t),\ldots,G_{r-t}(u_r;u_1,\ldots,u_t)\tag{1}
 \]
@@ -224,7 +225,7 @@ G^*_1(u_{t+1};u_1,\ldots,u_t),\ldots,G^*_{r-t}(u_r;u_1,\ldots,u_t)\tag{2}
 正如引言末尾已經指出的那樣，如果以一個（有限）代數數域 $K$ 的代數整數代替有理整數，並以該數域的素理想 $\mathfrak{p}$ 代替素數 $p$，那麼所有論證經少量修改後仍然成立。其中，\S\,1 完全不變，導出 \S\,2 中定理表述的論證也同樣不變；在證明（\S\,3, 2.）以及 \S\,2, 3. 的推論中，則需要作若干補充。
 
 \medskip\noindent\textbf{1.}
-在 \S\,3, 2. 中，還須把整除多項式 $G_1(u),\ldots,G_{r-t}(u)$ 的有限多個素理想列為例外素理想，\S\,3, 2., (1))，因為此時不能再假定這些多項式是本原的。這樣便保證了多項式 $G^*_1(u),\ldots,G^*_{r-t}(u)$（\S\,3, 2., (2)）不消失；這構成了條件 III 的加強。條件 II 保持不變，因為關於絕對不可約性的定理沒有改變；條件 I 自然也同樣保持不變。
+在 \S\,3, 2. 中，還須把整除多項式 $G_1(u),\ldots,G_{r-t}(u)$ 的有限多個素理想列為例外素理想（見 \S\,3, 2. 的式 (1)），因為此時不能再假定這些多項式是本原的。這樣便保證了多項式 $G^*_1(u),\ldots,G^*_{r-t}(u)$（\S\,3, 2., (2)）不消失；這構成了條件 III 的加強。條件 II 保持不變，因為關於絕對不可約性的定理沒有改變；條件 I 自然也同樣保持不變。
 
 \medskip\noindent\textbf{2.}
 作為最終結果的 \S\,2, 3. 的推論，應按照該處的最終表述寫成：
@@ -232,14 +233,14 @@ G^*_1(u_{t+1};u_1,\ldots,u_t),\ldots,G^*_{r-t}(u_r;u_1,\ldots,u_t)\tag{2}
 \noindent\textbf{推論。}
 \textit{若 $K$ 中的代數整數 $\alpha$ 不被有限多個例外素理想中的任何一個整除，則只要 $\alpha \cdot g(x)$ 屬於 $\mathfrak{I}$ 且 $g(x)$ 屬於 $\mathfrak{Z}$，總可推出 $g(x)$ 屬於 $\mathfrak{I}$。}
 
-若從 $K$ 中選取代數整數 $\lambda_1,\ldots,\lambda_s$，使每個整數均被某個例外素理想整除，但不被該素理想的平方整除，也不被其餘例外素理想整除，那麼這些 $\lambda$ 的冪乘積足以充當分母 $\alpha$。
+若從 $K$ 中選取代數整數 $\lambda_1,\ldots,\lambda_s$，使每個 $\lambda_i$ 均被一個例外素理想整除，但不被該素理想的平方整除，也不被其餘例外素理想整除，那麼這些 $\lambda$ 的冪乘積足以充當分母 $\alpha$。
 
 \noindent\textit{證明。}
-設 $(\alpha) = \mathfrak{p}_1^{\varrho_1} \cdots \mathfrak{p}_g^{\varrho_g}$ 是 $\alpha$ 的素理想分解，並且 $\mathfrak{p}_1,\ldots,\mathfrak{p}_g$ 不同於例外素理想，因而對每個 $\mathfrak{p}_i$，$\mathfrak{I}_{\mathfrak{p}_i}$ 都同構於 $\mathfrak{o}_{\mathfrak{p}_i}/\mathfrak{a}_{\mathfrak{p}_i}$。從 $K$ 的全體代數整數所成的環出發，通過在分母中加入所有且僅加入與 $(\alpha)$ 互素的代數整數，也就是不被素理想 $\mathfrak{p}_1,\ldots,\mathfrak{p}_g$ 中任何一個整除的代數整數，過渡到由 $(\alpha)$ 所定義的分式環 $\mathfrak{R}$。在 $\mathfrak{R}$ 中，素理想 $\mathfrak{p}_1,\ldots,\mathfrak{p}_g$ 仍是素理想，但變為主理想；其餘所有素理想都變為單位理想；$(\alpha)$ 的分解保持不變\textsuperscript{11)}。因此，當過渡到以 $\mathfrak{R}$ 為係數域時，該推論及其證明仍完全保持 \S\,2, 3. 中的形式。
+設 $(\alpha) = \mathfrak{p}_1^{\varrho_1} \cdots \mathfrak{p}_g^{\varrho_g}$ 是 $\alpha$ 的素理想分解，並且 $\mathfrak{p}_1,\ldots,\mathfrak{p}_g$ 不同於例外素理想，因而對每個 $\mathfrak{p}_i$，$\mathfrak{I}_{\mathfrak{p}_i}$ 都同構於 $\mathfrak{o}_{\mathfrak{p}_i}/\mathfrak{a}_{\mathfrak{p}_i}$。從 $K$ 的全體代數整數所成的環出發，通過在分母中加入所有且僅加入與 $(\alpha)$ 互素的代數整數，也就是不被素理想 $\mathfrak{p}_1,\ldots,\mathfrak{p}_g$ 中任何一個整除的代數整數，過渡到由 $(\alpha)$ 所定義的分式環 $\mathfrak{R}$。在 $\mathfrak{R}$ 中，素理想 $\mathfrak{p}_1,\ldots,\mathfrak{p}_g$ 仍是素理想，但變為主理想；其餘所有素理想都變為單位理想；$(\alpha)$ 的分解保持不變\textsuperscript{11)}。因此，當過渡到以 $\mathfrak{R}$ 為係數環時，該推論及其證明仍完全保持 \S\,2, 3. 中的形式。
 
 這就是說：若 $\alpha \cdot g(x)$ 屬於 $\mathfrak{I}$，則 $g(x) = F(f_i(x))$，其中多項式 $F(u)$ 的係數屬於 $\mathfrak{R}$。若 $\delta$ 是這些係數的一個公分母 --- 即 $\delta$ 與 $\alpha$ 互素 ---，則也可表述為：當 $\alpha \cdot g(x)$ 屬於 $\mathfrak{I}$ 時，$\delta \cdot g(x)$ 屬於 $\mathfrak{I}$，其中 $\delta$ 與 $\alpha$ 互素。因此 $(\delta, \alpha)$ 等於單位理想，從而得到：若 $\alpha \cdot g(x)$ 屬於 $\mathfrak{I}$，則 $g(x)$ 屬於 $\mathfrak{I}$。這就證明了上述推論的第一個斷言。
 
-為證明第二個斷言，令 $\mathfrak{R}^*$ 為 $K$ 中由有限多個例外素理想定義的分式環，並令 $\lambda_1,\ldots,\lambda_s$ 為這些素理想在 $\mathfrak{R}^*$ 中的基元素；可以假定它們是代數整數。在 $\mathfrak{R}^*$ 中，除去 $\mathfrak{R}^*$ 的一個單位因子後，$\alpha$ 是這些 $\lambda$ 的冪乘積。因此，退回到代數整數便得到 $\gamma\alpha = \beta \lambda_1^{\varrho_1} \cdots \lambda_s^{\varrho_s}$，其中 $\gamma$ 和 $\beta$ 都不被任何例外素理想整除。由 $\alpha \cdot g(x)$ 屬於 $\mathfrak{I}$，於是可推出 $\beta \cdot \lambda_1^{\varrho_1} \cdots \lambda_s^{\varrho_s} g(x)$ 屬於 $\mathfrak{I}$，繼而根據推論的第一個斷言，還可推出 $\lambda_1^{\varrho_1} \cdots \lambda_s^{\varrho_s} g(x)$ 屬於 $\mathfrak{I}$。
+為證明第二個斷言，令 $\mathfrak{R}^*$ 為 $K$ 中由有限多個例外素理想定義的分式環，並令 $\lambda_1,\ldots,\lambda_s$ 為這些素理想在 $\mathfrak{R}^*$ 中的生成元；可以假定它們是代數整數。在 $\mathfrak{R}^*$ 中，除去 $\mathfrak{R}^*$ 的一個單位因子後，$\alpha$ 是這些 $\lambda$ 的冪乘積。因此，退回到代數整數便得到 $\gamma\alpha = \beta \lambda_1^{\varrho_1} \cdots \lambda_s^{\varrho_s}$，其中 $\gamma$ 和 $\beta$ 都不被任何例外素理想整除。由 $\alpha \cdot g(x)$ 屬於 $\mathfrak{I}$，於是可推出 $\beta \cdot \lambda_1^{\varrho_1} \cdots \lambda_s^{\varrho_s} g(x)$ 屬於 $\mathfrak{I}$，繼而根據推論的第一個斷言，還可推出 $\lambda_1^{\varrho_1} \cdots \lambda_s^{\varrho_s} g(x)$ 屬於 $\mathfrak{I}$。
 
 \begin{center}
 \rule{4cm}{0.4pt}\\[0.6em]
@@ -254,9 +255,9 @@ G^*_1(u_{t+1};u_1,\ldots,u_t),\ldots,G^*_{r-t}(u_r;u_1,\ldots,u_t)\tag{2}
 
 \noindent\textsuperscript{3)} 参见 Hilbert 上引文献，或 E. Noether 的更详细论述《二元形式的整数不变量系统的有限性》（Gött. Nachr. 1919，第 138--156 页），\S\,1, IV。
 
-\noindent\textsuperscript{4)} 因此，例如在一个基本形式系统的整数（射影）不变量的情形，已经证明它们可以用通常意义下的一个完全不变量系统来表示，而分母中只出现\emph{有限多个}不同的素数 --- 这一结果不能由通常的 $\Omega$-过程方法推出 ---；但这并没有对整数情形本身的有限性作出任何断言，后者迄今只在二元基本形式的情形中为人所知。（参见 3) 中引用的札记。）
+\noindent\textsuperscript{4)} 因此，例如在一个基本形式系统的整数（射影）不变量的情形，已经证明它们可以用通常意义下的一个完全不变量系统来表示，而分母中只出现\emph{有限多个}不同的素数 --- 这一结果不能由通常的 $\Omega$-过程方法推出 ---；但这并没有对整数情形本身的有限性作出任何断言，后者迄今只在二元基本形式的情形中为人所知。（参见注\textsuperscript{3)}所引札记。）
 
-\noindent\textsuperscript{5)} 众所周知，一个系统的“超越次数”是指这样一组代数无关函数的数目：该系统代数地依赖于它们；由代数无关函数组成的系统称为不可约系统。一个素理想的超越次数（维数）是指其剩余类域的超越次数。一个素理想不存在与它具有相同超越次数的真素因子（例如参见 B.\,L.\,v.\,d. Waerden，《论多项式理想的零点理论》，Math. Ann. 96 (1926)，第 183--208 页）。
+\noindent\textsuperscript{5)} 众所周知，一个系统的“超越次数”是指这样一组代数无关函数的数目：该系统代数地依赖于它们；原文把由代数无关函数组成的系统称为“不可约系统”，本文译作“代数无关系统”。一个素理想的超越次数（维数）是指其剩余类域的超越次数。一个素理想不存在与它具有相同超越次数的真素因子（例如参见 B.\,L.\,v.\,d. Waerden，《论多项式理想的零点理论》，Math. Ann. 96 (1926)，第 183--208 页）。
 
 \noindent\textsuperscript{6)} E. Noether，《消去理论与一般理想理论》，Math. Ann. 90 (1923)，第 229--261 页，\S\,7。借助消去理论\textsuperscript{*)}，该定理可由下述较简单的定理推出：一个绝对不可约多项式只有模有限多个素数时才会失去这一性质（A. Ostrowski，《代数量的算术理论》，Gött. Nachr. 1919，第 279--298 页，其中第 296 页。较简单的证明见 E. Noether，《绝对不可约性的一个代数判据》，Math. Ann. 85 (1922)，第 26--33 页，№ 7）。如果 $\mathfrak{I}$ 有一个整性基，其所含函数的个数恰好多于其超越次数\emph{一个} --- 例如 Hecke（注 2）所考察的情形 ---，从而 $\mathfrak{a}$ 成为主理想，那么只需利用这个较简单的定理，而无须借助消去理论。各处的素数都可以由一个（有限）代数数域的素理想代替。当 $\mathfrak{a}$ 是零理想时，该定理显然也成立。
 
@@ -265,7 +266,7 @@ G^*_1(u_{t+1};u_1,\ldots,u_t),\ldots,G^*_{r-t}(u_r;u_1,\ldots,u_t)\tag{2}
 \medskip
 \hrule width 2.7cm
 \smallskip
-\noindent\textsuperscript{*)} 更确切地说，由于 $\mathfrak{a}$ 具有特征为零的系数，这一结论与该文的辅助定理 V 无关。事实上，该辅助定理的证明并不正确（在所给出的代换下，一切都可能恒等消失）；正确的证明直到 B.\,L.\,v.\,d. Waerden 的《Bézout 定理的一种推广》，Math. Ann. 99 (1928)，第 497--541 页，才得以给出。参见该文注 37）。
+\noindent\textsuperscript{*)} 更确切地说，由于 $\mathfrak{a}$ 具有特征为零的系数，这一结论与该文的辅助定理 V 无关。事实上，该辅助定理的证明并不正确（在所给出的代换下，一切都可能恒等消失；正确的证明直到 B.\,L.\,v.\,d. Waerden 的《Bézout 定理的一种推广》，Math. Ann. 99 (1928)，第 497--541 页，才得以给出。参见该文注37）。
 
 该文第 257 页还需要作一个容易看出的修正（定理 XIV 在 \S\,7 中同样没有用到）。原文说，$(\mathfrak{R})$ 中的所有元素都是通过把 $(x_i)$ 中的 $t_{\mu,\nu}$ 特殊化而得到 $(\mathfrak{R}')$ 中的元素；实际上，它们是由一个更一般的线性形式
 \[
@@ -273,9 +274,9 @@ G^*_1(u_{t+1};u_1,\ldots,u_t),\ldots,G^*_{r-t}(u_r;u_1,\ldots,u_t)\tag{2}
 \]
 經特殊化得到的，而該線性形式的指數與 $(x_i)$ 的指數相同。
 
-\noindent\textsuperscript{8)} 關於這個函數行列式定理的純代數證明 --- 這裡討論的是特徵為零的情形 ---，參見 B.\,L.\,v.\,d. Waerden，《微分的代數理論》，Nieuw Archief v. Wiskunde (2) 15 (1926)，第 111--120 頁。
+\noindent\textsuperscript{8)} 關於這個雅可比行列式定理的純代數證明 --- 這裡討論的是特徵為零的情形 ---，參見 B.\,L.\,v.\,d. Waerden，《微分的代數理論》，Nieuw Archief v. Wiskunde (2) 15 (1926)，第 111--120 頁。
 
-\noindent\textsuperscript{9)} 當所討論的是解析函數時 --- 如 Hecke 所考察的情形（注 2） ---，並且 $f_1(x),\ldots,f_t(x)$ 解析無關，而 $f_{t+1}(x),\ldots,f_r(x)$ 代數地依賴於前者時，這一附加假設得到滿足。不過，這一假設被有意表述為形式性的 --- 函數矩陣的秩 ---，從而使它在形式定義的冪級數情形中也具有明確含義。由此可以使所有論證都保持在純粹的形式代數範圍內，而關於解析函數的定理只在特殊情形中為證明該假設得到滿足時才是必要的。
+\noindent\textsuperscript{9)} 當所討論的是解析函數時 --- 如 Hecke 所考察的情形（注 2） ---，並且 $f_1(x),\ldots,f_t(x)$ 解析無關，而 $f_{t+1}(x),\ldots,f_r(x)$ 代數地依賴於前者時，這一附加假設得到滿足。不過，這一假設被有意表述為形式性的 --- 雅可比矩陣的秩 ---，從而使它在形式定義的冪級數情形中也具有明確含義。由此可以使所有論證都保持在純粹的形式代數範圍內，而關於解析函數的定理只在特殊情形中為證明該假設得到滿足時才是必要的。
 
 \noindent\textsuperscript{10)} 例如參見 B.\,L.\,v.\,d. Waerden，《論多項式理想的零點理論》，Math. Ann. 96 (1926)，第 183--208 頁，\S\,3。
 
@@ -287,16 +288,16 @@ G^*_1(u_{t+1};u_1,\ldots,u_t),\ldots,G^*_{r-t}(u_r;u_1,\ldots,u_t)\tag{2}
 \begingroup
 \fontspec{Noto Serif}
 \begin{center}
-{\Large\bfseries 論整數函數的極大域。}\par
+{\Large\bfseries 論整數函數的極大整環。}\par
 \vspace{0.7em}
 {\bfseries 埃米·諾特（哥廷根）。}\par
 \vspace{0.5em}
 \emph{（摘要。）}
 \end{center}
 
-每一個沒有“零因子”的環（整環）$\mathfrak{I}$ 都確定一個同類型的極大域 $\mathfrak{M}(\mathfrak{I})$，它由所有滿足下述條件的函數 $g(x)$ 組成：至少存在一個 $a\ne0$，使得 $a\cdot g(x)$ 屬於 $\mathfrak{I}$。
+每一個沒有“零因子”的環（整環）$\mathfrak{I}$ 都確定一個同類型的極大整環 $\mathfrak{M}(\mathfrak{I})$，它由所有滿足下述條件的函數 $g(x)$ 組成：至少存在一個 $a\ne0$，使得 $a\cdot g(x)$ 屬於 $\mathfrak{I}$。
 
-本文在域 $\mathfrak{I}$ 為有限生成的假設下，考察該極大域 $\mathfrak{M}(\mathfrak{I})$ 的“分母” $a$。文中證明，總可以選擇這些 $a$，使得在其全體中只有有限多個素數作為因子出現。與此同時，當 $g(x)$ 是冪級數或這類級數的商時，假定定義域 $\mathfrak{I}$ 的函數 $f'_1(x),\ldots,f'_r(x)$ 彼此之間只能由代數關係聯繫。
+本文在整環 $\mathfrak{I}$ 為有限生成的假設下，考察該極大整環 $\mathfrak{M}(\mathfrak{I})$ 的“分母” $a$。文中證明，總可以選擇這些 $a$，使得在其全體中只有有限多個素數作為因子出現。與此同時，當 $g(x)$ 是冪級數或這類級數的商時，假定定義整環 $\mathfrak{I}$ 的函數 $f'_1(x),\ldots,f'_r(x)$ 彼此之間只能由代數關係聯繫。
 
 證明的基礎是把該問題歸結為一般理想理論中的若干問題。這一歸結通過如下方式實現：把因子 $p$ 在分母 $a$ 中的出現視為函數 $f_i$ 之間的模 $p$ 關係。
 
@@ -310,4 +311,3 @@ G^*_1(u_{t+1};u_1,\ldots,u_t),\ldots,G^*_{r-t}(u_r;u_1,\ldots,u_t)\tag{2}
 \clearpage
 
 \end{document}
-
