# Noether Paper 6 Korean producer status — T05 / §4

State: **complete producer-draft coverage of §4 in twelve editable units; all targets UNCHECKED and unbuilt**.

- Source: ED0001 lines 4904--4999; 5,258 LF bytes / `FEC0CDF1F652320EC258145E31E3941D8DF6C3FACE9BB000968425F61715E58F`.
- Targets: T05-U35--U46, 12 files / 15,714 bytes. Paper 6 through T05 now has 46 producer targets / 70,295 bytes.
- Binding: pointer v007, 21,580 bytes / `A6A8FC8E5AC24ACAF49DFD55B4B58FA3DA882EF8C3FDD4D136220C8751045156`; ED0001 2,153,565 raw bytes / `D1F06B311F6CBD991DD247D745DD9A72DDE326A20396DF43CFE0C8EDB1593CDB`.
- Next source cursor: line 5000, §5; that line is outside T05.
- Language posture: Hangul-first provisional ko-KR with selective disambiguating Hanja `階數` and `體`. Neither display choice has independent regional validation; ko-KP remains `unverified_do_not_claim`.
- Review: no source/scan comparison, Korean clause or terminology review, formula/proof/citation check, Hanja/regional review, or human validation.
- Build: no compilation, extraction, rendering, visual inspection, assembly, packaging, certification, or approval. Visual/render evidence for T05 is zero because no image or render operation occurred.
- German: no defect claim and no authority/canon mutation.
- Byte hygiene: the final twelve-target freeze has ESC 0, CR 0, BOM 0. U40 nevertheless retains adverse producer history: the first patch introduced ANSI escapes, a bounded invisible-sequence repair did not match, and the file was deleted/recreated with `apply_patch`; the malformed interim identity is unavailable and is not reconstructed.

## Principal holds

- `Rationalbasis` is provisionally `유리기저`, meaning a finite rational generating family, not a linear-algebra basis over `\mathbb Q`; `유리적 기저` and `유리생성기저` remain alternatives.
- `Übertragungsprinzip` remains `전이 원리`, distinct from the operator `Überschiebung` rendered provisionally as `전이연산`; `이행 원리` remains an alternative.
- `Körper` is provisionally `체(體)`, with `대수적 확대체` used where the extension sense is explicit. The Hanja is a disambiguator, not evidence of ko-KR or ko-KP standardization.
- `algebraischer Rang` remains `대수적 계수(階數)`, with false attraction to coefficient `係數`; `대수적 랭크` remains an alternative.
- `Adjunktion` and `Zwischenkörper` are provisionally `첨가` and `중간체`; `부가` and spaced `중간 체` remain checker alternatives.
- `birationale Transformation` is provisionally `쌍유리 변환`; spacing, historical register, and the exact generator-change sense remain held.
- Every algebraic-element and field-extension assertion, elimination relation, rational-generation implication, field tower, primitive-element-style step, Theorem II, function count bound, birational transformation formula, footnote, and citation remains formula/proof/referent debt.
