% NOETHER PAPER 6 — KOREAN TRANSLATION-PRODUCER DRAFT — UNCHECKED
% Unit: T06-U56; current-authority whole lines 5066--5080
% Source slice: 571 LF UTF-8 bytes; SHA-256 D1D32D5AB6270262C02AFBD37A34C63EBEB958179A9208E5FEA961BEDAD1DFF7
% Pointer: NOETH-DE-AUTH-v007-20260804; SHA-256 A6A8FC8E5AC24ACAF49DFD55B4B58FA3DA882EF8C3FDD4D136220C8751045156
% German authority: NOETH-DE-ED-0001; 2,153,565 raw bytes; SHA-256 D1F06B311F6CBD991DD247D745DD9A72DDE326A20396DF43CFE0C8EDB1593CDB
% Producer translation only: no source/Korean/formula review, compilation, or rendering.
% Sense window: durchläuft alle Funktionen means that the quotient ranges over every field function; formal symmetrisch modifies the conjugate expressions rather than asserting a new field symmetry.
% Terminology debt: konjugiert→켤레/공액, formal symmetrisch→형식적으로 대칭, identisch verschwinden→항등적으로 소멸하다/항등적으로 0이 되다 remain checker-held.
% Standardization hold: ko-KR 켤레 and Hanja 체(體) are provisional; ko-KP equivalents remain unresolved.

따라서
\[
f^*(x)=\frac{F^*(x)}{H^*(x)}
\]
가 $\Kfield^*_{\rho\rho}$의 모든 함수를 두루 취하면, 다음과 같은 무한히 많은 관계로 이루어진 계가 성립한다.
\[
\begin{array}{@{}l@{\quad}c@{}}
(6)&\displaystyle \frac{F^*(x)}{H^*(x)}=\frac{F^*(x^{(1)})}{H^*(x^{(1)})}=\cdots=\frac{F^*(x^{(i-1)})}{H^*(x^{(i-1)})};
\end{array}
\]
또는 이를 한데 쓰면 다음과 같다.
\[
F^*(x^{(k)})H^*(x^{(l)})-F^*(x^{(l)})H^*(x^{(k)})=0\qquad(k,l=0,1,\ldots,(i-1));
\]
여기서 $F^*(x^{(k)})$와 $H^*(x^{(k)})$는 각각 $F^*(x)$와 $H^*(x)$에 켤레이며 형식적으로 대칭이므로, 어느 것도 항등적으로 소멸하지 않는다.
