# Source check — Noether Paper 33 Chinese rebase

The complete logical Paper 33 article was checked against sealed P31, not sampled.

Structural parity:

- 2 source footnotes / 2 Hans / 2 controlled Hant;
- 6 source emphasis loci / 6 mapped target loci in each Chinese target;
- 3 source small-cap title loci / 3 mapped title-style loci in each target;
- 26 inline mathematical spans, sequence-identical across German, Hans, and Hant;
- 2 displayed `pmatrix` blocks, sequence-identical in both Chinese targets;
- 9 German guillemet pairs / 9 mapped Chinese quote pairs;
- balanced local group and centered title block.

Reconciliation corrections to the inherited witness:

- reattached *in arithmetischer Auffassung* to both title subjects: `算术观点下的超复量与表示论`;
- restored citation form `S. 71--73`;
- restored source notation `$\mathfrak{G}$` for the represented group and `$\mathfrak{B}$` at all three operator-group loci, replacing inherited plain `$G$` and `$\mathfrak{M}$`;
- restored full emphasis windows `Darstellung einer Gruppe` and `Reduktion einer gegebenen Darstellung`, and removed six target-only bibliographic italics;
- rendered Schur-context `Automorphismenring` / `Automorphismenkörper` as `自同态环` / `自同态除环`, not the inherited `自同构环` / `自同构除环`;
- separated group-ring element `群数（即群环元素）` from group order/cardinality;
- separated `不可约` from `不可分解` and `分解` from `分裂域`;
- controlled historical `完全可约环` with the modern explanatory form `半单环`;
- rendered the radical quotient explicitly as `模去根基（在此即 Jacobson 根）所得商环`;
- replaced trap-prone `算子域` with source-defined `算子系`, avoiding confusion with a coefficient field or an analytic operator domain.

No extra inherited Introduction survives. The old successful two-page proof render is not accepted as current build evidence; fresh P31-reconciled builds and renders control.

All language judgments are internal. No native-speaker, domain-community, Taiwan, Hong Kong, Macao, Singapore, or external certification is claimed.
