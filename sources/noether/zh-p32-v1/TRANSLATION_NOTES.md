# Noether Paper 32 — Chinese producer translation notes

## Scope

Complete Paper 32 was translated from the exact current German interval at lines 16011–16179, SHA-256 `1E1C2E6AA32B606EAB5B57737F60CE7CF649610B490098511C29498BE8CC7611`. The inherited Simplified-Chinese interval, SHA-256 `34655BF638E18A2B62C062D0D34E2CC44CB5FB1B9FC70B4CC748F5281761A813`, was used only as a drafting witness.

Four non-overlapping translation segments were assembled in the order A–D. The complete PRC-oriented Simplified-Chinese target has SHA-256 `762BCAC20CFB3DBCFBE000E16ADBA386DB3561A1BFC594C5C769083D53D28A06`. A controlled-generic Traditional-script derivative was produced mechanically with OpenCC `s2t` plus recorded normalizations; its TeX SHA-256 is `2616DB5841744671F8CA611C126979DBEB46D06B25657C2FC2EC484E03151F13`.

## Producer lexical choices

The translation uses, among others:

- `分裂域` for `Zerfällungskörper`;
- `极小分裂域` for inclusion-minimal `minimaler Zerfällungskörper`, while `次数最小` marks least degree;
- `非交换除环` for historical `nichtkommutativer Körper`;
- `双侧单环`, `极大交换子域`, `超复系统`, `表示类`, and `完全可约`;
- `四元数除环`, `幂等元素`, `本原幂等元素`, and `单侧单理想`;
- `有理表示` for a representation realizable over the stated field $K$;
- `因子系`, `相对范数`, `本原 $p$ 次单位根`, and `分圆域`.

These are producer editorial choices, not validated terminology. The 24-row terminology, adverse-evidence, and CJKV ledgers record an explicit sense window, excluded attractors, alternatives, a provisional lexical-attractor basin, and qualitative Mandarin-Simplified dominance debt for each decision. Japanese and Korean evidence were not consulted and do not authorize the Chinese choices.

## Adverse production evidence for another session

- The assembled Hans draft predominantly uses `四元数除环` for `Quaternionenkörper`, while the segment-C section heading contains `四元数体`. This producer package leaves that variation explicit for independent adjudication rather than silently self-checking it.
- `极小` versus `最小`, `极大` versus `最大`, and the local sense of `正则情形` are trap-prone.
- `非交换除环` competes historically with `非交换体` and `斜域`.
- `有理表示` can be misread as a representation over the rational numbers rather than over the stated field $K$.
- `因子系` and `超复系统` preserve a historical register and are not modernized to cohomological or algebra terminology.
- Controlled Hant is a generic script derivative of PRC-oriented prose. It is explicitly not Taiwan-, Hong Kong-, or Macao-localized language.
- Both final logs contain the expected unavailable-italic-shape warning and summary font-substitution warning. No page was viewed.

## Role boundary

Floris's controlling instruction is: `you do not check - you translate - other sessions CHEWCK`.

This package therefore records translation and mechanical compilation only. It does not claim source collation, source correctness, semantic/formula/terminology correctness, native-reader review, visual QA, regional localization, approval, publication, or certification. If an independent checker identifies a precise possible Noether-source defect, it must deduplicate it and ensure that `4 -nterslav` sees it; this producer did not make or adjudicate a source-defect claim.

SGA remains held pending explicit Floris confirmation.
