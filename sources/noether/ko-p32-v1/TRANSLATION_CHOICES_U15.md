# P32-KO-U15 translation choices

This file records translator choices, not checking or certification.

- Historical `in $K$ rational` is rendered `$K$ 위에서 유리적인`; the checker may prefer `$K$ 위에서 정의된`.
- `ähnliche Matrizen` is rendered `서로 상사인 행렬`; `Transformation` is `변환`.
- `ohne Einschränkung` is rendered `일반성을 잃지 않고`.
- All conjugations, matrix entries, relations, and the sum-of-two-squares conclusion are preserved.
- This is an unchecked ko-KR translation. No ko-KP, source, Korean-review, specialist, or external validation is claimed.
