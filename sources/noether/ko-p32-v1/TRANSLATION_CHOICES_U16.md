# P32-KO-U16 translation choices

This file records translator choices, not checking or certification.

- `in $K$ rational` retains U15's `$K$ 위에서 유리적`; `ähnlich` is `상사인`.
- `Faktorensysteme` retains `인자계`; `Charakterisierung` retains `특성화`.
- All constructed matrices and signs are preserved without normalization.
- This is an unchecked ko-KR translation. No ko-KP, source, Korean-review, specialist, or external validation is claimed.
