# Noether Paper 4 Korean T08--T09 producer choices — UNCHECKED

These are source-local Hangul-first ko-KR producer preferences, not Korean terminology certification. Hanja, ko-KP localization, and pan-Korean equivalence remain unverified. Mandarin-Simplified dominance in the surviving comparison shelf is qualitative evidence debt only, never a readiness score. Chinese and Japanese do not authorize Korean.

## Coupled sense windows

- `Grundfaltung / kogrediente Faltung / Defekt`: `기본 수축 / 동변 수축 / 결손`. The basic contraction is the defect-one generator; `결손` is the source parameter, not an error report.
- `Typus`: `유형`. It labels the contraction family indexed by `\rho`, not a logical type system.
- `Schlußform / Schlußausdruck`: `종결형 / 종결식`. The first is the resulting form, the second the final symbolic expression; they are not proof conclusions.
- `Gewichtsabnahme / Gewichtsindex`: `무게 감소 / 무게지수`, continuous with the paper's historical weight grading.
- `dualer Prozeß`: `쌍대적 과정`; the checker must ensure this does not overstate modern categorical duality.
- `n-reihige Determinante`: `$n$행 행렬식`. `reihig` counts determinant rows here, while `Reihe` elsewhere may mean row, family, or series.
- `höhere Form / normierte Form / Normalform`: `더 높은 형식 / 정규화된 형식 / 정규형`. These three notions must remain distinct.
- `Wertesystem`: `값계`, meaning the indexed integer family `\alpha`, not a value system in an axiological sense.
- `Gitterpunkt / Mannigfaltigkeit`: `격자점 / 다양체` in the local ordering model; no modern topological smoothness is asserted.
- `Modul`: `가군` in §9's finite form collection. This sense differs from §4's source-local `Modul \eps`, which the producer rendered as `부호 \eps`.
- `reduzibel / Reduzent`: `환원 가능 / 환원자`. `환원자` is provisional historical terminology for the reducible form series used as a reduction source.
- `Elementarkovarianten / Polaren / Simultaninvarianten`: `기본 공변식 / 편극 / 연립불변식`.

## Provisional lexical-attractor basins

- Sino-xenic inherited or established technical basin: `수축`, `결손`, `쌍대`, `행렬식`, `정규형`, `가군`, `공변식`, `편극`.
- Modern Sino-xenic coinage/calque basin: `무게지수`, `연립불변식`, `환원 가능`.
- Native/mixed basin: `더 높은 형식`, `값계`, `기본 수축`.
- Mixed/contested: `종결형`, `종결식`, `환원자`, `형식열`.
- Unresolved: whether Korean specialist usage prefers `동시불변식` to `연립불변식`, `감약 가능` to `환원 가능`, or another rendering of `Reduzent`.

## Adverse evidence and checker priorities

- `n-reihig`, `Reihe`, `Formenreihe`, and `Reihenentwicklung` share a German surface family but require four different Korean sense windows. Audit each occurrence against its syntax.
- The three §8 cases and equation (70) establish uniqueness only up to permutation of order; check that Korean scope is not stronger.
- The last §8 paragraph distinguishes equivalence by relations (62.) from equivalence by higher weight decrease. This is a high-risk logical distinction.
- §9's `Modul` may be historically narrower than modern algebraic `가군`; Korean specialist evidence must decide.
- The proof of theorem IX switches among forms/form series named `f`, `g`, `h`, and `H`; check referents and the direction of every expansion independently.
- No formula or German textual token inside a formula was normalized or translated by the T08 producer; their preservation is not validation.

No target was compiled, rendered, source-checked, formula-checked, assembled, or independently linguistically reviewed.
