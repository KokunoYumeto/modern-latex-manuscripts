# Continuation TODO

1. Reopen Noether Paper 4 section 6 against the original print and record the confirmed R823 word-boundary defect `Schluss ausdruecke` versus source compound `Schlussausdruecke`; change downstream bodies only where the target sense is actually wrong.
2. Reopen Paper 4 section 7 and adjudicate the four dotted complementary-symbol occurrences after equation (52), plus the independent `rho<n` versus source `rho<=n` endpoint defect. Patch the German authority and all affected Slavic targets, rebuild, render, and return stable record IDs and hashes.
3. Route the Paper 29 duplicated-footnote apparatus defect and the Paper 39 original-print bibliography error `Transac, 134 (1932)` versus bibliographic volume 34 into the source-governance ledgers without silently changing the diplomatic German print reading.
4. Resume the next non-overlapping German source-critical paper lane after those cross-language authority defects are stabilized. Paper 2 printed pp. 23-57 remain separately open and must not be inferred closed from this package.

The current checkpoint is a live cumulative base, not permission to discard later independently source-backed work. Future merges must use exact content diffs and survival checks rather than package sequence numbers.
