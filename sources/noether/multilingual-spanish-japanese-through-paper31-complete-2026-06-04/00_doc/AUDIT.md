# Audit note: Paper 31 ES/JA

Completed: Paper 31 complete, introduction and §§1--8, in Spanish and Japanese.

No declared translation gaps. All source-visible footnotes are present: 28 in Spanish and 28 in Japanese, matching the recombined German/English controls. Displayed formulas and bracketed ideal decompositions are editable TeX, not images.

Source/control reconciliation: Paper 31 was supplied as two source/control batches. The recombined German and English files in `04_src/` omit only split-batch mechanics (`Continuation` heading and local footnote reset); these are provenance mechanics, not paper text. The full scan witness is included as `04_src/scan/N31_scan.pdf`.

Global refinement: Spanish was globally standardized in this paper to `el discriminante` / `teorema del discriminante` rather than the earlier literal feminine drift from German `die Diskriminante`. This is recorded in cumulative methodology aids for future Takagi/algebraic-number-theory integration. Japanese uses `判別式定理` and `判別式` consistently.

Terminology decisions: `Ordnung` = `orden` / `オーダー`; `Hauptordnung` = `orden principal` / `主オーダー`; `Restklassenring` = `anillo de clases residuales` / `剰余類環`; `Restklassenkörper` = `cuerpo residual` / `剰余体`; `Multiplikationsring` = `anillo de multiplicación` / `乗法環`.

No tables or diagrams occur in Paper 31. Render checks cover standalone heads/tails, cumulative tails, German/English controls, and the scan witness.
