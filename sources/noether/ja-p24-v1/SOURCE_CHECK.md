# Source check — final internal record

## Authority and audit boundary

The complete sealed P31 Paper 24 span, German lines 13631–14119, was compared against the Japanese article span at target lines 257–780. The sealed raw source span is SHA-256 `CA71FFFDAB3CEEE3EC614AB30A13F26EEF7D12D95D13875B8613BFC6B8C77232`; the accepted Japanese raw article span is `C623010EC3232EFECFA1AC9EE8372C66D1A8882BBA194864207455E9D5CDB3BA`. The inherited Japanese block was used only as a translation witness and defect locator.

## Apparatus, structure, and source-visible form

- The separate article title, author line `Emmy Noether in Göttingen`, source-visible rule, journal apparatus, seven numbered sections, Theorems I–XVII, Lemmas I–VIII, dated receipt apparatus, and article-terminal `\clearpage` are present.
- The final target has no mid-article forced page break. The terminal `\clearpage` remains the article boundary.
- Source/target form checks aligned all 66 emphasized objects, all 41 bold objects, and all 24 notes in order and content.
- The representation-aware display inventory maps 47 German source display objects to 54 Japanese target objects. The larger target count reflects documented representation splits or promoted inline material, not omitted or invented mathematics; all target display locators are accounted for in `DISPLAY_CORRESPONDENCE.csv`.
- The Fraktur, barred/unbarred, Greek-letter, and coefficient-field families were checked as mathematical distinctions rather than typographic decoration.

## Mathematical reconciliation

The inherited formula layer was not accepted on count parity. The source-keyed rebase repaired, among other items:

1. The inverse map `\xi_k=s_k(\zeta)` with finite support, `E^{(i)}\zeta_0`, `R^{(i)}=E^{(i)}E_1^{(i)}\cdots E_\sigma^{(i)}`, the norm display, and the barred-zero coordinates and multiplicities in formula (5).
2. Arbitrary isolated-component indices, the source equality and note-13 reference, the successive `E^{(n)}\cdots E^{(i+1)}` proof products, source nonvanishing conditions, and the distinct nonzero `G^{(i+1)}`/`H^{(i+1)}` argument.
3. The barred variable family and denominator field, the arbitrary `x_{i+\lambda}` construction, the three-line `c_\lambda/c_i/T_\mu/\bar h_\mu` argument, Theorem IV's indices and zero coordinates, and the four distinct objects `\bar R`, `R`, `\bar P`, and `P`.
4. The coefficient field, primitivity in `x_{i+1},\ldots,x_n`, the `t_{1i},\ldots,t_{ni}` and `v_1,\ldots,v_i` families, the `\delta=p^f` and later `p^g` conditions, and the correct implication from barred `\bar p` to unbarred `p`.
5. Source `\alpha` in Lemma VI and Theorem X; `v_\lambda=-t_{\lambda i}`; the two successive congruence implications without an invented `r_\lambda` multiplier; Fraktur `\mathfrak L,\mathfrak L_0,\mathfrak M`; `R'`; barred `q` in the third example; and the sealed directions `x_{i-1}^{k}`, `x_i^{l}`, `V_i^\sigma D^{(i)}`, and `x_{i+1}^{m}`.

Findings from the independent early/mid and late-section read-only comparisons were revalidated against the sealed source before repair. A separate final exactness audit passed on semantically identical pre-pagination text; the frozen E597 target differs only by nonbreaking wrappers that do not change mathematical or linguistic content. No remaining mathematical/source divergence was found in the bounded body.

## Terminology and language controls

- `剰余体`, `随伴素イデアル`, `ガロア拡大体`, `単因子`, and explicitly defined `絶対素イデアル` are internally accepted in their recorded senses.
- Historical or work-specific `基本イデアル`, `個別ノルム`, `素関数`, `零点体`, `準素関数`, and `互いに対応する一組の零点系` remain held rather than externally standardized. `準素関数` carries the required immediate sense window `ここでは素関数の冪`.
- Simplified-Chinese evidence does not authorize Japanese. Mandarin-Simplified dominance risk is recorded qualitatively for the relevant lexical-attractor shelves and is never converted into a readiness scalar.
- Residual-language scanning and bilingual review found no impermissible English or German bridge prose in the accepted target; bibliographic names and source citations remain where appropriate.

## German source-defect protocol

The sequence `(Anm. 21));` at sealed German line 14093 was initially routed as a possible source punctuation defect to the persistent task `4 -nterslav`. Original-print comparison established that the first closing mark terminates the note number and the second terminates the enclosing parenthetical. JA-D027 therefore supersedes the preliminary suspicion and requests no source edit. No other genuine German source defect was established during this Paper 24 reconciliation.

Successful internal source checking does not constitute external/community certification or independent human-reader validation.
