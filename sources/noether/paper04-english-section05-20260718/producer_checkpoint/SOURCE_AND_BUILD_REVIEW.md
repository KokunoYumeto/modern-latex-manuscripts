# Source, formula, and build review

The bounded R823 body was checked line by line against the historical print. Relations (32)–(39), the unnumbered product-matrix display, Theorem III and its complete supporting argument, the Clebsch note, and the exact Section 6 boundary were checked.

## Disclosed print/R823 deltas

1. Relation (35): the print uses alpha for both component indices and omits the terminal minus-lambda term in the first complementary `R` index. R823 coordinates both component indices to lambda and adds minus lambda. The target follows R823 and discloses the complete historical reading.
2. Immediately after relation (38): the print reads cogredient folding of defect alpha; R823 reads defect rho. The target follows R823 and discloses the print reading without claiming that alpha is mathematically erroneous.
3. In the linear-form consequence after relation (39): the print uses the identity sign; R823 uses ordinary equality. The target follows R823 and discloses the historical sign without certifying semantic interchangeability.

These are high-confidence editorial deltas, not automatically proved German-source defects.

## Rejected inherited-English regressions

- Relation (33): an ordinary factor multiplied by a separate pairing was collapsed into one pairing.
- Relation (34): the same grouping regression occurred in its first two lines.
- Relation (35): `rho_1` was changed to `rho` in the first complementary `R` subscript.
- Relation (38): a spurious epsilon was inserted.
- Relations (36)–(39): source-literal paired bounds were silently normalized to min/max notation.

The target restores the German controls, preserves the source-literal bounds, and explains their endpoint convention in prose.

Two isolated builds, byte-identical text extraction, complete-page render reproduction, metadata/font inspection, and package-machine validation passed. This review is automated and source-audit based; it is not independent human peer review or mathematical certification.

