# Final open Noether source-critical lane

Work only on Paper 2, printed pp. 50-57, starting from the cumulative in this checkpoint.

## Required authority

- Required base TeX SHA-256: `6FCBF5DB4E4378032B7074442C181E3FCFE975275319E49B284CE3B868EE0D5D`.
- Original Paper 2 publication scan decides every reading.
- OCR, inherited TeX, package labels, and earlier ledgers are locators or claims, never authority.

## Required method

1. Open every complete original source page at native and enlarged detail.
2. Check every prose line, formula, exponent, prime, accent, hat span, relation, operator, continuation mark, note call/text, emphasis span, list label, matrix/table cell, and page boundary.
3. Reopen pp. 55 and 57 as complete pages. Their source-local `*)` notes are already repaired, but those exact repairs do not certify the rest of either page.
4. Keep every source-local note marker independent of the global numeric footnote counter.
5. Promote only source-confirmed changes into a patched cumulative. Return an exact predecessor-to-successor diff and one disposition row per printed page.
6. Compile twice, render every changed output page, and visually reopen each render.
7. Compare the whole 466-page output against this predecessor and account for every changed page before sealing.

## Required return

- patched cumulative TeX and PDF;
- exact diff;
- eight-row complete-page disposition ledger;
- confirmed-fix, no-patch, hard-math, correction-origin, and source-apparatus rows;
- labeled native source pages and only genuinely useful enlarged crops;
- two-pass build logs and changed-page render evidence;
- zero-open-page closure result or an exact unresolved locus.

Do not call Paper 2 or the author complete until all eight pages are source-adjudicated on this exact head and the whole-author 753-key validation is rerun with zero open keys.

