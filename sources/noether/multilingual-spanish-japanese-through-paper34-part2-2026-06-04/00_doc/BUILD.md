# Build summary

- Spanish standalone `N34p2_ES.pdf`: 12 pages, built with `pdflatex`.
- Japanese standalone `N34p2_JA.pdf`: 11 pages, built with `xelatex`/`xeCJK`.
- German source/control `N34p2_DE.pdf`: 11 pages, built with `pdflatex` from the batch source TeX.
- English control `N34p2_EN.pdf`: 11 pages, built with `pdflatex` from the batch control TeX.
- Source scan witness `N34p2_scan.pdf`: 17 pages, copied from the paper-level source-scan packet.
- Cumulative Spanish through Paper 34 §14 `N34p2_cum_ES.pdf`: 328 pages, built with `pdflatex`.
- Cumulative Japanese through Paper 34 §14 `N34p2_cum_JA.pdf`: 293 pages, built with `xelatex`/`xeCJK`.

Log check: the German, English, Spanish, Japanese, and both cumulative logs report zero `Overfull` and zero `Underfull` diagnostics. Render checks were produced for standalone first/last pages and cumulative tail pages.
