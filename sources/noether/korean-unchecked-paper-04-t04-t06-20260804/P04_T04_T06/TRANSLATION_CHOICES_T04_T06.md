# Noether Paper 4 Korean T04--T06 producer choices — UNCHECKED

These are source-local Hangul-first ko-KR producer preferences, not Korean terminology certification. Hanja, ko-KP localization, and pan-Korean equivalence remain unverified. Mandarin-Simplified dominance in the surviving comparison shelf is qualitative evidence debt only, never a readiness score. Chinese and Japanese do not authorize Korean.

## Coupled sense windows

- `Zerlegungsidentität / zerlegbar / unzerlegbar`: `분해 항등식 / 분해 가능한 / 분해 불가능한`. The source distinguishes an identity that decomposes a row from an identity that itself becomes decomposable when component rows have independent meaning.
- `Schlußausdruck`: `종결식`. It is the final expression of the symbolic transformation, not a logical conclusion sentence.
- `Modul \eps`: provisionally `부호 \eps` because the displayed value is `(-1)^\delta`. Algebraic `가군` and absolute-value `모듈러스` are adverse attractors.
- `Polar / Polarprozeß / Polaroperation`: `편극 / 편극 과정 / 편극 연산`. This is classical invariant-theory polarization; modern analytic polarity is outside the window.
- `Aggregat`: `결합식`, not a set. It can be a linear combination of determinant, matrix-product, or polar operations depending on context.
- `explizite Darstellung / expliziter Schlußausdruck`: `명시적 표현 / 명시적 종결식`. Explicitness means dependence only on the aggregate rows or uniquely derived rows.
- `Produktsatz`: `곱정리`, scoped to determinants/matrices in this paper.
- `Elementarkovarianten`: `기본 공변식`. `초등 공변식` remains an unresolved Korean alternative.
- `kogrediente Faltung`: `동변 수축`; `Defekt`: `결손`. The defect is the source parameter, not a generic rank defect or error.
- `Formenreihe`: `형식열`, meaning the finite family of coefficient-linear invariant constructions, not an ordinary numerical series.
- `Differentiationsprozeß / Differentialsymbol`: `미분 과정 / 미분기호`.
- `äquivalent` in the Clebsch definition: `동치`, not literal equality.
- `Gesamtgewicht / Gewichtsabnahme`: `전체 무게 / 무게 감소`, continuous with §1's historical weight definition.

## Attractor controls

- Provisional modern Sino-xenic/calque basin: `분해 항등식`, `명시적 표현`, `편극`, `미분 과정`, `기본 공변식`, `동치`.
- Mixed/contested basin: `종결식`, `결합식`, `동변 수축`, `결손`, `형식열`, `무게 감소`.
- Unresolved: `Modul \eps`, exact Korean historical naming of the general product theorem, and whether `기본 공변식` or `초등 공변식` is preferred.

## Hard-part record

The first read-only PowerShell hash command for T04 failed before output with `ParserError: Missing 'in' after variable in foreach loop`, caused by `foreach($x in$units)` lacking whitespace between `in` and `$units`. No file changed. The corrected retry `foreach ($x in $units)` succeeded. This failed attempt must remain in the P04 difficulty ledger.

No target-write failure was observed for T04--T06. This negative statement is not self-review: no target was compiled, rendered, source-checked, formula-checked, or linguistically reviewed.

## Checker priorities

1. Check the `분해/분해 가능/분해 불가능` family across §§4--5 and theorem III's exhaustiveness.
2. Resolve `Modul \eps`, `편극`, `종결식`, and `기본 공변식` from Korean specialist evidence.
3. Check every nested range in (25)--(31), the symmetric explicit form (33)--(36), and special cases (37)--(39).
4. Check theorem IV's long dependency proof and theorem V--VI generation claims without flattening quantifier scope.
5. Independently compare (40)--(45), especially the exact `\gtreqless` branch and the received (45) parameter range. Route any checker-confirmed German defect to the sole canon owner.
