# Audit note

Completed scope: Paper 35 complete, `Über Maximalbereiche aus ganzzahligen Funktionen`, in Spanish and Japanese.  The cumulative outputs now run through Paper 35.

No declared translation gaps.  The German source witness was treated as authoritative for footnotes and mathematical detail; the English control is included as an aid, but it is less complete around footnotes and bibliographic texture.

Retroactive layout cleanup: the two consecutive Paper 02 table pages in the cumulative PDFs were cleaned.  The previous landscape wrappers were removed and the tables were scaled onto standard A4 portrait pages.  Page-size verification reports A4 for cumulative pages 39 and 40 in both Spanish and Japanese.  The table cells/formulas were not rewritten; this is a layout-only cleanup.

Source normalization notes:
- In the introduction, the example is rendered as `x^2=(2x)^2/2^2`; the current German text witness reads compactly/defectively as `x^2=(2x)/2^2`.  The translation uses the mathematically correct scan/OCR reconstruction and records it here.
- In §2 the source witness mixes the total-generator index notation (`t`/`s`).  The translations use `s` for the total number of generators and `t` for the transcendence degree/Jacobian rank, which is the notation required by the proof.

Terminology audit:
- `Integritätsbereich` remains `dominio íntegro` / `整域`.
- `Integritätsbasis` remains `base íntegra` / `整基底`.
- `Maximalbereich` is `dominio maximal` / `極大領域`, not a maximal ideal.
- `ganzzahlige Funktionen` is integer-coefficient function language, not analytic entire-function language.
