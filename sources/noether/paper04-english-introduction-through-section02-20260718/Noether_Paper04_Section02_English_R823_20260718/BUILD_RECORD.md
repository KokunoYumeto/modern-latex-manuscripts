# Build record

The final TeX was compiled twice with pdfLaTeX in nonstop, halt-on-error mode after removing stale generated auxiliary, outline, log, and PDF files. Both final passes exited successfully.

The final PDF parses as two A4 pages. The final log was scanned for LaTeX and package warnings, overfull and underfull boxes, undefined controls, fatal errors, emergency stops, rerun requests, and duplicate destinations; zero hits were found.

The final PDF SHA-256 is `DD2EA57310465719D6C8E3530EB01DDAD7203EA4F484061E5D8BB19D382A11DE`.

Both pages were rendered at 180 dpi and inspected. Raw build logs are excluded because they contain machine-specific paths. This is the sanitized public build record.
