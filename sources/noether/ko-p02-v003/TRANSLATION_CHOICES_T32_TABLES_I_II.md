# Noether Paper 2 T32 / terminal Tables I--II producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja spellings and ko-KP equivalents are held pending independent Korean evidence; none is asserted here. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness scalar, and neither Chinese nor Japanese usage authorizes Korean.

- `TABELLE I/II`: `표 I/II`; `s. S. 90`: `(90쪽 참조)`.
- `oberste Horizontalreihe`: `맨 위 가로행`; `erste Vertikalreihe`: `첫 번째 세로행`.
- `Grad in den x / u`: `$x$ / $u$에 관한 차수`.
- All mathematical cell contents are preserved as notation rather than lexicalized Korean; this preservation is not table-cell or formula checking.
- Landscape, sizing, adjustment, grid, and cell macros are retained as editable TeX structure; retention is not compilation, rendering, or visual QA.

Sense window: `표` denotes the terminal classified mathematical tables, not a spreadsheet or an evidentiary truth table. `I/II` are stable table identifiers. `쪽` in `90쪽 참조` denotes the source's printed-page reference and must not be silently reinterpreted as a PDF page, whole-source line, or target-reader page. `가로행` and `세로행` identify the numeric coordinate-header rows, not arbitrary prose lines or algebraic sequences. `차수` renders source `Grad` as degree in the variables $x$ and $u$, not chronological order, tensor rank, or the local `Ordnung` label without further evidence. Numeric table headers are coordinates/degree labels, not counts of cells.

Mandarin-Simplified dominance risk/debt: the CJK shelf can over-attract Sino-xenic surfaces such as `표`, `참조`, and `차수`, while dense symbolic tables can hide the absence of Korean lexical evidence. These choices are provisional Korean producer judgment and lane continuity, not validation from Mandarin table terminology. Korean attestations, Hanja disambiguation, and ko-KP standards remain absent.

Attractor basins: Sino-xenic inherited (`표`, `참조`, `차수`, with specialized senses still needing evidence); modern Sino-xenic coinage/calque (`가로행`, `세로행` as technical header labels, if retained); global modern loan (Roman table numerals and Latin/Greek symbols are identifiers/notation, not Korean lexical evidence); native coinage (`맨 위`, `첫 번째`, `...에 관한`); mixed/contested (`쪽`, `가로행`, `세로행`, `차수`); unresolved (standard Korean table-coordinate wording, source-page reference treatment in a future reader, every Hanja mapping, and every ko-KP equivalent).

Held alternatives/review conditions: replace `표`, `쪽`, `가로행`, `세로행`, or `차수` only with local Korean evidence and a sense-preserving logged decision. Do not infer Hanja or ko-KP forms from Mandarin, Japanese, or the Hangul surface. No external Korean terminology validation exists.
