# Noether Paper 2 T21 / §19 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `System`: `계`.
- `Ordnung` in the form table: `계수차수`.
- `Faltung / tritt in Faltung ein`: `수축 / 수축되다`.
- `Irreduzible Formen`: `환원 불가능 형식들`.
- `Formenreihe`: `형식열`; `Reduzent`: `환원자`.
- `zerfallen / Zerfallen`: `분해되다 / 분해`.
- `kontragrediente Form`: `반변 형식`; `Kovariante`: `공변식`.
- `Funktionaldeterminante`: `함수행렬식`.
- `volle Überschiebungen`: `완전 전이연산`.
- `reduzibel`: `환원 가능`.

Sense window: `반변 형식` is an adjectival transformation class and remains distinct from the noun-class choice `반공변식`. `완전 전이연산` is not generic transfer or `수축`, although the source relates these operations. `형식열` is an algebraic family, not a visual table row. `분해` remains distinct from `환원 가능`.

Attractor basins: inherited Sino-xenic (`계`, `계수차수`, `공변식`, `반변`, `환원`); modern Sino-xenic coinage/calque (`형식열`, `환원자`, `완전 전이연산`, `분해 가능`); mixed/contested (`수축`, `함수행렬식`, `반변 형식`); unresolved (specialist Korean scope phrase for `als Kovariante betrachtet`). No external Korean terminology validation exists.
