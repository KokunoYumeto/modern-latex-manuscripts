# Noether Paper 2 T14 / §12 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `Formen siebenter Ordnung`: `계수차수 7의 형식들`.
- `Faltung`: `수축`.
- `Überschiebung`: `전이연산`.
- `zerfallen / zerfallende Form`: `분해되다 / 분해 가능 형식`.
- `Funktionaldeterminante`: `함수행렬식`.
- `Produktsatz`: `곱 정리`.
- `Kontravariante / kontragredient`: `반공변식 / 반변`.
- `Spezialisierung`: `특수화`.
- `Polarisation`: `편극`.
- `Ansatz`: descriptive `다음과 같이 두다`, avoiding an unattested Korean technical noun.
- `Schlußform`: `최종형`.
- `Relation`: `관계`.

Sense window: `전이연산` is not ordinary transfer and remains distinct from `수축`; `분해 가능` is not the same state as `환원 가능`. `반공변식` is a form class while `반변` is the transformation adjective. `함수행렬식` may denote a Jacobian-type determinant and remains specialist-review debt.

Attractor basins: inherited Sino-xenic (`계수차수`, `편극`, `특수화`, `관계`, `반변`); modern Sino-xenic coinage/calque (`전이연산`, `분해 가능 형식`, `최종형`); mixed/contested (`수축`, `함수행렬식`, `반공변식`); unresolved (`Ansatz` and the precise Korean functional-determinant term). No external Korean terminology validation exists.
