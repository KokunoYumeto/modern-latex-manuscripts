# Noether Paper 2 T20 / Chapter IV §18 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja forms and ko-KP equivalents are held pending independent Korean evidence. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `System $s_I$`: `계 $s_I$`; `Schema`: `도식`.
- `Faltung`: `수축`.
- `Irreduzible Formen`: `환원 불가능 형식들`; `Reduktionen`: `환원`; `Reduzent`: `환원자`.
- `Ordnung`: `계수차수`; `Grad`: `차수`.
- `Formenreihe`: `형식열`; `niedrigere Formen`: `더 낮은 형식들`.
- `kontragrediente Formen`: `반변 형식들`.
- `Funktionaldeterminante`: `함수행렬식`; `zerfallende Formen`: `분해 가능 형식들`.

Sense window: `환원 불가능`, `환원`, `환원자`, and `분해 가능` denote different roles or states and are not interchangeable. `계수차수` represents source `Ordnung`, while the bidegree components in $x$ and $u$ use `차수` for source `Grad`. `형식열` is an algebraic family rather than a displayed row. `반변` marks the local contragredient sense and is not a generic inverse. `함수행렬식` names the source's functional determinant, not an arbitrary matrix determinant. `수축` is the local `Faltung` operation, not physical folding.

Hanja/ko-KP hold: possible Hanja glosses such as 系, 還元, 次數, 反變, 函數行列式, and 分解 are not inserted and do not constitute evidence; corresponding North-Korean orthography and specialist choices are unresolved. No ko-KP standard claim is made.

Attractor basins: inherited Sino-xenic (`계`, `환원`, `차수`, `반변`, `분해`); modern Sino-xenic coinage/calque (`계수차수`, `환원자`, `환원 불가능 형식`, `형식열`, `함수행렬식`); global modern loan (none selected in this tranche); mixed/contested (`수축`, `반변 형식`, `도식`); unresolved (specialist Korean distinction among irreducible, reducible, reducer, and decomposable; ko-KP equivalents). No external Korean terminology validation exists.
