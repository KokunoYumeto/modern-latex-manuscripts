# Noether Paper 2 T10--T11 / §§8--9 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `Formen dritter/vierter Ordnung`: `계수차수 3/4의 형식들`, following the paper's explicit `Ordnung` definition.
- `Faltung`: `수축`.
- `irreduzible Formen`: `환원 불가능 형식들`.
- `Reduktion / Reduzent`: `환원 / 환원자`.
- `kontragrediente Formen`: `반변 형식들`.
- `doppelte Reduktion`: `이중 환원`.
- `Folgerungen`: `결론`.
- `adj. Modul`: `첨가 모듈`, provisional expansion of the abbreviated source label.

Sense window: `Ordnung` is coefficient degree, not variable degree; `반변` describes transformation behavior, not a merely opposite vector. `환원 불가능` and `환원자` belong to the paper's reduction order. `첨가 모듈` is a contextual expansion requiring checker confirmation.

Attractor basins: inherited Sino-xenic (`계수차수`, `환원`, `반변`, `결론`); modern Sino-xenic coinage/calque (`환원자`, `이중 환원`, `첨가 모듈`); mixed/contested (`수축`, `환원 불가능 형식`); unresolved (`adj. Modul` exact Korean specialist surface). No external Korean terminology validation exists.
