# Noether Paper 2 T09 / §7 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `relativ vollständiges System mod ...`: `...를 법으로 하는 상대 완전계`.
- `überschieben`: `전이연산하다`.
- `dualistisch gegenüberstehen`: `쌍대 관계에 있다`.
- `Grundform`: `기본형`.
- `Moduln adjungieren`: `모듈로 첨가하다`.
- `höherer Modul`: `더 높은 모듈`.
- `Ordnung in den Koeffizienten`: `계수차수`.
- `normieren`: `정규화하다`.
- `Formenreihe`: `형식열`.
- `zerfallende Form`: `분해 가능 형식`.
- `Kontrollieren der Rechnung`: `계산을 검산하다`.

Sense window: `전이연산` labels classical `Überschiebung`, whereas `수축` labels `Faltung`; the source relates them operationally but does not authorize lexical collapse. `더 높은 모듈` and `더 높은 형식` belong to different ordered structures. `계수차수` follows the paper's introduction and must not be replaced by variable degree.

Attractor basins: inherited Sino-xenic (`쌍대`, `기본형`, `계수차수`, `정규화`, `검산`); modern Sino-xenic coinage/calque (`상대 완전계`, `전이연산`, `더 높은 모듈`); mixed/contested (`형식열`, `분해 가능 형식`, `모듈로 첨가`); unresolved (specialist Korean distinction between `überschieben` and `Faltung`). No external Korean terminology validation exists.
