# Noether Paper 2 T07 / §5 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `Zurückführung des Moduls`: `모듈 ...를 ...로 환원`.
- `Reihe der Moduln`: `모듈의 열`; this is a sequence of reduction moduli, not a matrix row.
- `normalisieren`: `정규화하다`.
- `Faltung`: `수축`.
- `übernommene Formen`: `넘겨받은 형식들`.
- `Spezialisierung`: `특수화`.
- `Symbolvertauschung`: `기호의 교환`.
- `Polarisieren / Polare`: `편극하다 / 편극`.
- `mod`: `...를 법으로 하여`; source macro `\modu{...}` is retained in displays.

Sense window: `환원` acts on the paper's ordered modules and form systems; it is not automatically quotient reduction in a modern module. `열` can denote a sequence here but also appeared for symbol/variable rows elsewhere, so every occurrence needs its local German locator. `수축`, `편극`, and `전이연산` are related operations in the source but are not interchangeable labels.

Attractor basins: inherited Sino-xenic (`환원`, `정규화`, `특수화`, `편극`, `기호`); modern Sino-xenic coinage/calque (`상대 완전계`, `모듈의 열`); mixed/contested (`수축`, `넘겨받은 형식`, `...를 법으로 하여`); unresolved (`Reihe der Moduln` specialist Korean surface). No external Korean terminology validation exists.
