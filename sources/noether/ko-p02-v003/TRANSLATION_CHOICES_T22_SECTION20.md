# Noether Paper 2 T22 / §20 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `Formen siebenter Ordnung`: `계수차수 7의 형식들`.
- `Faltung`: `수축`; `Überschiebung`: `전이연산`.
- `Irreduzible Formen`: `환원 불가능 형식들`; `Reduzent`: `환원자`.
- `Zerfallen`: `분해`; `reduzibel erkannt`: `환원 가능하다고 판정된`.
- `Funktionaldeterminante`: `함수행렬식`.
- `Zurückgehen ... auf niedrigere Formen`: `더 낮은 형식들로 되돌림`.
- `doppelte Reduktion`: `이중 환원`.
- `Ansatz`: descriptive `다음과 같이 두다`.
- `mod $(i,J)$`: `$(i,J)$를 법으로 하여`.

Sense window: `전이연산` is not generic transfer or `수축`. `분해` remains distinct from `환원 가능`. `이중 환원` reports two-step reduction, not multiplication by two. `법` is a congruence surface and not the borrowed noun `모듈`. The phrase `환원 가능하다고 판정된` attributes the classification to the source, not to this producer.

Attractor basins: inherited Sino-xenic (`계수차수`, `환원`, `이중`, `공식`); modern Sino-xenic coinage/calque (`환원자`, `환원 불가능 형식`, `전이연산`, `분해`); mixed/contested (`수축`, `함수행렬식`, congruence `법`); unresolved (`Ansatz` and the specialist Korean wording for reduction “by going back” to lower forms). No external Korean terminology validation exists.
