# Noether Paper 2 Korean introduction producer choices — UNCHECKED

These are source-local Hangul-first ko-KR producer preferences, not Korean terminology certification. Hanja, ko-KP localization, and pan-Korean equivalence remain unverified. Mandarin-Simplified dominance in the surviving comparison shelf is qualitative evidence debt only, never a readiness score. Chinese and Japanese do not authorize Korean.

## Coupled sense windows

- `ternäre biquadratische Form`: `삼원 4차 형식`. `삼변수 4차 형식` is a transparent alternative; `삼항` is reserved for the invariant-theory domain adjective where appropriate.
- `Formensystem / Bildung`: `형식계 / 구성식`. `Bildung` denotes an invariant-theoretic construction, not merely the act of forming something.
- `Ordnung / Grad`: provisionally `계수차수 / 변수차수` because the source footnote defines the first through coefficients and the second through variables. Modern order/degree conventions are an adverse attractor.
- `Kovariant / Kontravariant / Invariant`: `공변식 / 반공변식 / 불변식`.
- `relativ vollständiges System / absolut vollständiges System`: `상대 완전계 / 절대 완전계`.
- `Faltung / Formenreihe / Reihenentwicklung`: `수축 / 형식열 / 급수 전개`; these are distinct operations/objects despite shared surface vocabulary around rows/series.
- `Modul` and `mod`: `모듈` and `$\cdots$를 법으로 하는`. In this introduction the word names the form(s) used for modular reduction, not automatically an algebraic `가군`.
- `Überschiebung`: provisionally `전이연산`. Classical invariant-theory Korean usage for transvection requires evidence; generic `겹침` is rejected.
- `Grundform / reduzibel`: `기본형 / 환원 가능`.
- `simultanes System / kogredient / kontragredient`: `연립계 / 동변 / 반변`.

## Provisional lexical-attractor basins

- Sino-xenic inherited or established technical basin: `형식`, `불변식`, `공변식`, `반공변식`, `수축`, `유한성`.
- Modern Sino-xenic coinage/calque basin: `상대 완전계`, `절대 완전계`, `연립계`, `계수차수`, `변수차수`.
- Mixed/contested: `형식열`, `구성식`, `모듈`, `전이연산`, `삼원 4차 형식`.
- Unresolved: exact Korean historical terminology for `Überschiebung`, `Ordnung/Grad`, and `relativ vollständiges System`.

## Adverse evidence and checker priorities

- Preserve original bibliography titles; the Korean text translates explanatory prose but does not silently retitle cited works.
- The source's `Ordnung/Grad` footnote overrides default modern intuitions locally and must be checked across later sections.
- `Modul` changes sense across Noether works and even within the corpus; no cross-paper automatic replacement is authorized.
- The long U02 footnote contains two distinct linear-dependence claims and one reported numerical-error explanation; check their subjects and contrast.
- The symbol overview was copied as TeX under the translation-only boundary and remains formula-unchecked.

No target was compiled, rendered, source-checked, formula-checked, assembled, or independently linguistically reviewed.
