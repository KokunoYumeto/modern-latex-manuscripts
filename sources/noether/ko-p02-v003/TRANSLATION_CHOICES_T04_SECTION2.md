# Noether Paper 2 T04 / §2 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative debt; Chinese/Japanese do not authorize Korean.

- `Reihenentwicklung nach Polaren`: `편극에 따른 급수 전개`.
- `zusammengesetzte Polare`: `합성 편극`.
- `Elementarkovarianten`: `기본 공변식`; `초등 공변식` remains an adverse alternative.
- `kontragrediente / kogrediente Variablenreihen`: `서로 반변인 / 서로 동변인 변수열`.
- `Spezialisierung`: `특수화`.
- `Hilfsvariablen`: `보조변수`.
- `rekurrierendes Gleichungssystem`: `재귀 방정식계`.
- `Übertragungsprinzip`: `전이 원리`, provisionally; do not conflate with classical `Überschiebung`, rendered `전이연산` in the introduction.
- `numerische Koeffizienten`: `수치 계수`.
- `höhere Formen`: `더 높은 형식`.

Attractor basins: established Sino-xenic (`편극`, `공변식`, `특수화`, `재귀`); modern calque (`합성 편극`, `수치 계수`); mixed/contested (`형식열`, `전이 원리`, `기본 공변식`). Unresolved Korean specialist evidence: compound polar terminology and the transfer/transvection distinction.

No formula, target, source, or terminology was independently checked; no compile/render occurred.
