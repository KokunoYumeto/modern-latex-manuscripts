# Noether Paper 2 T24 / §22 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `Formen neunter Ordnung`: `계수차수 9의 형식들`.
- `Faltung`: `수축`; `einmalige/zweimalige Faltung`: `한 번/두 번의 수축`.
- `Formenreihe`: `형식열`; `Reduzent`: `환원자`; `Faktor`: `인수`.
- `Zerfallen / zerfallende Form`: `분해 / 분해 가능 형식`.
- `höhere Form`: `더 높은 형식`.
- `Ansatz`: descriptive `다음과 같이 두다`.
- `doppelte Reduktion`: `이중 환원`; `reduzibles Glied`: `환원 가능 항`.
- `Überschiebung über eine Funktionaldeterminante`: `함수행렬식에 대한 전이연산`.

Sense window: `형식열` is an algebraic family, not a displayed row. `인수` names multiplicative factor status and is not a causal “factor.” `분해 가능` remains distinct from `환원 가능`. `이중 환원` is a two-stage reduction, while `두 번의 수축` is an operation count. `전이연산` is not generic transfer or `수축`.

Attractor basins: inherited Sino-xenic (`계수차수`, `환원`, `인수`, `이중`); modern Sino-xenic coinage/calque (`형식열`, `환원자`, `분해 가능 형식`, `전이연산`); mixed/contested (`수축`, `함수행렬식`, `더 높은 형식`); unresolved (`Ansatz` and the exact Korean attachment of the source's multiple reducer list). No external Korean terminology validation exists.
