# Independent-checker handoff — Noether Paper 2 T05 U20--U25

Every target is `UNCHECKED`; exact identities are in `SOURCE_CUSTODY_T05_SECTION3.md`.

Independent checker must:

1. Compare all Korean prose, theorem II, definition, cases 1--2 and a)--c), examples, and footnotes against lines 823--895.
2. Check every displayed formula token, especially the underbraced zero term, hats, contractions, coefficients, dual forms, and form-series endpoints.
3. Resolve `ganze rationale Funktion` (`정수 유리 함수`) from Korean invariant-theory evidence; do not infer its modern sense from the surface words alone.
4. Review `Reduzent` (`환원자`), `zerfallende Form` (`분해 가능 형식`), `doppelte Reduktion` (`이중 환원`), and `Funktionaldeterminante` (`함수행렬식`) as separate concepts.
5. Check the local equivalence/contrast between `Polare` and `Überschiebung`; do not collapse either into the separate Clebsch `Übertragungsprinzip` used later.
6. Return stable findings with exact unit/source locators, target and authority hashes, evidence, alternatives, and uncertainty. Route only independently checker-confirmed German defects to the sole canon owner.

No source defect, Korean approval, build, render, or assembly claim is made.
