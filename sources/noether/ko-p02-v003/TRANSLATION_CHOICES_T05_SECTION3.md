# Noether Paper 2 T05 / §3 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese terminology do not authorize Korean.

- `Reduktionstheorem`: `환원 정리`.
- `relativ vollständiges System`: `상대 완전계`.
- `ganze rationale Funktion`: `정수 유리 함수`, held for specialist review; possible senses include polynomial/integral rational expression rather than a modern integer-valued rational function.
- `reduzibel / Reduzent`: `환원 가능 / 환원자`.
- `doppelte Reduktion`: `이중 환원`.
- `zerfallende Form`: `분해 가능 형식`; `분해 형식` is an adverse shorter alternative.
- `Funktionaldeterminante`: `함수행렬식`; a possible Jacobian-specific Korean term requires evidence.
- `Polare (Überschiebung)`: `편극(전이연산)`, provisionally retaining both source labels.
- `höhere / niedere Formen`: `더 높은 / 더 낮은 형식`; the ordering is the paper's invariant-theory ordering, not ordinary polynomial degree alone.

Sense window: `환원` here concerns expressions within ordered form series and modules; `분해` concerns representation by products and higher forms. `함수행렬식` is not authorized as a generic determinant. `전이연산` is reserved for classical `Überschiebung` and must not be used as evidence for Clebsch's distinct `Übertragungsprinzip`.

Attractor basins: inherited Sino-xenic (`환원`, `함수`, `행렬식`, `편극`); modern Sino-xenic coinage/calque (`상대 완전계`, `환원자`, `이중 환원`); mixed/contested (`정수 유리 함수`, `분해 가능 형식`, `전이연산`); unresolved (`ganze rationale Funktion` and the precise Korean Jacobian term). No terminology was independently validated.
