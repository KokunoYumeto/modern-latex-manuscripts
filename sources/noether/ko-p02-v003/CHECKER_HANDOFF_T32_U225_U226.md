# Independent-checker handoff — Noether Paper 2 T32 U225--U226

Every target is `UNCHECKED`; exact pointer, authority, source, target, boundary, terminal-control, and Paper 3 cursor identities are in `SOURCE_CUSTODY_T32_TABLES_I_II.md`.

Independent checker must:

1. Compare the complete terminal Tables I--II interval, whole-source lines3480--3568, against U225--U226; verify the excluded blank line3479, excluded terminal controls3569--3572, and Paper 3 start at line3573.
2. Check Table I cell by cell across row labels 0--21 and column labels 0--21, and Table II cell by cell across row labels 0--23 and column labels 0--16. Verify every occupied and empty cell, row/column position, `\mcell` boundary, line break, exponent, subscript, prime, dot, hat, parenthesis, comma, and case-sensitive symbol.
3. Check the table titles, source-page references, numeric headers, and legends. Confirm that the horizontal headers and vertical labels encode the stated degrees in $x$ and $u$ and that producer `표`, `쪽`, `가로행`, `세로행`, and `차수` preserve their exact roles.
4. Reconcile every table entry and the number of forms represented by each system against the §26 conclusion in U223--U224, including the declared total of 331; do not infer correctness from mechanical transcription or the printed source.
5. Check all dense formula strings without silent normalization, especially distinctions among Latin `k`, `l`, Greek `\nu`, `\eta`, `\vartheta`, `\theta`, uppercase/lowercase symbols, dots, `H^2/H^3`, and all contraction tuples `(\cdot,s,u)^n`.
6. Check the retained layout structure (`landscape`, `adjustbox`, column specifications, `\hline`, font sizing, and legends) during the separately owned compile/render stage. Producer metadata does not establish that either table fits, compiles, or renders legibly.
7. Review the table-heading sense window: `TABELLE` as a classified mathematical table, `s. S. 90` as a source-page cross-reference, `Horizontalreihe/Vertikalreihe` as coordinate-header rows, and `Grad` as homogeneous degree rather than chronological order.
8. Return stable findings with exact unit/source locators, hashes, evidence, alternatives, and uncertainty. Route only independently confirmed German defects to the sole German-canon owner under pointer v005; do not treat a dense-table surprise or translator observation as a defect.

No source defect, Korean approval, table/formula/count approval, compilation, rendering, assembly, certification, or self-review is claimed.
