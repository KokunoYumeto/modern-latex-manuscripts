# Noether Paper 2 T12 / §10 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `Formen fünfter Ordnung`: `계수차수 5의 형식들`.
- `Faltung`: `수축`.
- `irreduzibel / reduzibel`: `환원 불가능 / 환원 가능`.
- `zerfallende Form`: `분해 가능 형식`.
- `Reduzent`: `환원자`.
- `niedere Formen der Formenreihe`: `형식열의 더 낮은 형식들`.
- `doppelte Reduktion`: `이중 환원`.
- `Anlaß zur Reduktion`: `환원할 계기`.
- `dualistische Rechnung`: `쌍대인 계산`.
- `als Modul adjungiert`: `모듈로 첨가됨`.

Sense window: `계수차수` is coefficient degree. `환원할 계기` describes an algebraic consequence of comparing two reductions, not causal certification. `쌍대인 계산` asserts formal duality in the source and is not an independent check. `분해 가능` and `환원 가능` remain distinct states.

Attractor basins: inherited Sino-xenic (`계수차수`, `환원`, `쌍대`, `공식`); modern Sino-xenic coinage/calque (`환원자`, `이중 환원`, `환원할 계기`); mixed/contested (`수축`, `분해 가능 형식`, `모듈로 첨가`); unresolved (specialist Korean phrase for `Anlaß zur Reduktion`). No external Korean terminology validation exists.
