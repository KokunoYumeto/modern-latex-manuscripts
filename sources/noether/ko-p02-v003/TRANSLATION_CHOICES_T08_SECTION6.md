# Noether Paper 2 T08 / §6 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja and ko-KP are unverified. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness score; Chinese and Japanese do not authorize Korean.

- `Zurückführung / Reduktionsformeln`: `환원 / 환원 공식`.
- `spezielle Faltungen`: `특수 수축`.
- `niedrigere Formen der Formenreihe`: `형식열의 더 낮은 형식`.
- `Polaren`: `편극`.
- `Reduzent`: `환원자`.
- `Reihenentwicklung`: `급수 전개`.
- `Schlußformel / Schlußwerte`: `최종 공식 / 최종값`.
- `rekurrierend`: `재귀적으로`.
- `Übertragung aus dem binären Gebiet`: `이항 영역으로부터의 전이`.
- `Überschiebungen`: `전이연산`, kept distinct from `Übertragungsprinzip` (`전이 원리`).
- `Hilfsvariable`: `보조변수`.

Sense window: `전이` in `Übertragung` describes carrying an expansion/relation from the binary domain, whereas `전이연산` labels the classical operation `Überschiebung`; neither surface authorizes the other. `환원자` is an element/form-series device, while `환원 공식` names the equations effecting reduction. `더 낮은 형식` uses the source's form-series ordering, not an unchecked modern degree comparison.

Attractor basins: inherited Sino-xenic (`환원`, `편극`, `재귀`, `보조변수`); modern Sino-xenic coinage/calque (`특수 수축`, `환원자`, `전이연산`, `최종값`); mixed/contested (`급수 전개`, `더 낮은 형식`, `이항 영역으로부터의 전이`); unresolved (specialist Korean distinction among `Übertragung`, `Übertragungsprinzip`, and `Überschiebung`). No external Korean terminology validation exists.
