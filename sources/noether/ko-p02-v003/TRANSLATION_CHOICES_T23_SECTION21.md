# Noether Paper 2 T23 / §21 producer choices — UNCHECKED

Hangul-first provisional ko-KR only. Hanja spellings and ko-KP equivalents are held pending independent Korean evidence; none is asserted here. Mandarin-Simplified shelf dominance is qualitative evidence debt, never a readiness scalar, and neither Chinese nor Japanese usage authorizes Korean.

- `Formen 8. Ordnung`: `계수차수 8의 형식들`; `System`: `계`.
- `Faltung`: `수축`.
- `Irreduzible Formen`: `환원 불가능 형식들`.
- `Reduktionen`: `환원`; `Reduzent`: `환원자`.
- `zerfällt`: `분해된다`; `Formenreihe`: `형식열`.
- `Überschiebung`: `전이연산`; `einmalige Überschiebung`: `한 번의 전이연산`.
- `Funktionaldeterminante`: `함수행렬식`.
- `doppelte Reduktion`: `이중 환원`.
- `Letzteres`: provisional `마지막 것은`, with its exact antecedent expressly held for the checker.
- `Folgerungen`: provisional heading `결론`.
- `treten nicht in Faltung mit $s^2$ ein`: descriptive `s^2과의 수축에 들어가지 않는다`.
- Mathematical arrays and formulas were retained as producer TeX; this is not a formula check.

Sense window: `수축` denotes the paper's invariant-theoretic `Faltung`, not everyday shrinking, folding, or an independently established modern convolution term. `전이연산` is reserved here for `Überschiebung` and is not generic transfer, translation, or `수축`. `환원`, `환원자`, `환원 불가능`, and `분해` remain distinct: a reducer, reducibility classification, and decomposition are not interchangeable. `형식열` means `Formenreihe`, not an arbitrary sequence of formatting objects. `계수차수` renders the local graded `Ordnung` and is not an assertion about chronological order. `함수행렬식` is the local `Funktionaldeterminante`. `이중 환원` denotes the source's twofold reduction procedure, not multiplication by two. `마지막 것은` is deliberately unresolved until a checker fixes the antecedent among the final identities in (28.). `결론` may overstate the structural force of `Folgerungen`; `따름결과`/`귀결` remain alternatives for review. The literal punctuation `§12 C))` is source-facing evidence, not a producer correction.

Mandarin-Simplified dominance risk/debt: the available CJK shelf can strongly attract the Sino-xenic-looking surfaces `계수차수`, `환원`, `환원자`, `전이연산`, `함수행렬식`, and `형식열`. Their presence here reflects provisional Korean producer judgment and continuity with this lane, not cross-language validation. Korean-native mathematical attestations, Hanja disambiguation, and ko-KP terminology remain absent.

Attractor basins: Sino-xenic inherited (`형식`, `계수`, `차수`, `함수`, `등식`, `공식`, `결론`, with modern mathematical senses still requiring evidence); modern Sino-xenic coinage/calque (`환원자`, `환원 불가능 형식`, `형식열`, `전이연산`, `함수행렬식`); global modern loan (none deliberately introduced in the Korean prose; Latin and symbolic notation is retained notation, not lexical evidence); native coinage (`한 번`, `마지막 것은`, and descriptive verbal framing); mixed/contested (`계수차수`, `수축`, `분해`, `환원`, `이중 환원`, `계`); unresolved (standard Korean `Überschiebung`, the antecedent rendering for `Letzteres`, the heading force of `Folgerungen`, all Hanja mappings, and all ko-KP equivalents).

Held alternatives/review conditions: review may replace `전이연산`, `함수행렬식`, `계수차수`, or `결론` only with local Korean evidence and an explicit sense-preserving decision. Hanja and ko-KP forms must not be inferred from Mandarin, Japanese, or the Hangul surface. No external Korean terminology validation exists.
