
\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[ngerman]{babel}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,mathtools,array,booktabs}
\usepackage{geometry}
\usepackage{microtype}
\geometry{a4paper,margin=1.45cm}
\setlength{\parindent}{1.25em}
\setlength{\parskip}{0pt}
\allowdisplaybreaks
\setlength{\emergencystretch}{20em}
\sloppy
\hbadness=10000
\vbadness=10000
\hfuzz=2pt
\vfuzz=10pt
\raggedbottom
\providecommand{\mR}{\mathfrak{R}}
\providecommand{\bR}{\overline{\mathfrak{R}}}
\providecommand{\mK}{\mathfrak{K}}
\providecommand{\mL}{\mathfrak{L}}
\providecommand{\mS}{\mathfrak{S}}
\providecommand{\mT}{\mathfrak{T}}
\providecommand{\mZ}{\mathfrak{Z}}
\providecommand{\mO}{\mathfrak{O}}
\providecommand{\mo}{\mathfrak{o}}
\providecommand{\mA}{\mathfrak{A}}
\providecommand{\mB}{\mathfrak{B}}
\providecommand{\mC}{\mathfrak{C}}
\providecommand{\mD}{\mathfrak{D}}
\providecommand{\mE}{\mathfrak{E}}
\providecommand{\mP}{\mathfrak{P}}
\providecommand{\ma}{\mathfrak{a}}
\providecommand{\mb}{\mathfrak{b}}
\providecommand{\mc}{\mathfrak{c}}
\providecommand{\mf}{\mathfrak{f}}
\providecommand{\mpideal}{\mathfrak{p}}
\providecommand{\mq}{\mathfrak{q}}
\providecommand{\mt}{\mathfrak{t}}
\providecommand{\mX}{\mathfrak{X}}
\providecommand{\mOmega}{\mathfrak{\Omega}}
\providecommand{\bP}{\overline{P}}
\providecommand{\bq}{\overline{\mathfrak{q}}}
\providecommand{\bp}{\overline{\mathfrak{p}}}
\providecommand{\ba}{\overline{\mathfrak{a}}}
\providecommand{\bb}{\overline{\mathfrak{b}}}
\providecommand{\tr}{\operatorname{Tr}}
\providecommand{\Norm}{\operatorname{N}}
\newcommand{\class}[1]{(#1)}
\begin{document}
\setcounter{footnote}{0}

\section*{31. Der Diskriminantensatz für die Ordnungen eines algebraischen Zahl- oder Funktionenkörpers}
\emph{Fortsetzung: §§3--8.}

\subsection*{§3. Vollständig reduzible Ringe}

1. Der Ring $\mR$ heißt \emph{vollständig reduzibel}, wenn in der Produktdarstellung seines Nullideals (§2, 2) alle Primärkomponenten Primideale werden; oder -- was nach §2, 1 und 2 damit identisch ist -- wenn $\mR$ sich darstellen läßt als direkte Summe von endlich vielen Körpern. Der vollständig reduzible Ring heißt vollständig reduzibel erster Art, wenn alle Primideale von erster Art sind (§2, 4); im entgegengesetzten Fall vollständig reduzibel zweiter Art.

Ist $P$ ein vollkommener Körper, so gibt es also nur vollständige Reduzibilität erster Art; insbesondere ist dies stets der Fall bei algebraisch-abgeschlossenem Körper. Im folgenden bedeute speziell $\bP$ den aus $P$ abgeleiteten algebraisch-abgeschlossenen Körper; und $\bR$ sei gleich $\mR[\bP]$.

2. \textbf{Satz.} \emph{Der Ring $\mR$ ist dann und nur dann vollständig reduzibel erster Art, wenn der Erweiterungsring $\bR=\mR[\bP]$ mit algebraisch-abgeschlossenem $\bP$ vollständig reduzibel ist.}

Der Beweis erfolgt in drei Schritten.

2a. Aus vollständiger Reduzibilität von $\bR$ -- allgemeiner irgendeines Erweiterungsringes -- folgt vollständige Reduzibilität (erster oder zweiter Art) von $\mR$. Denn nach Voraussetzung gilt für das Nullideal von $\bR$ eine Darstellung als kleinstes gemeinsames Vielfaches von Primidealen,
\[
        (0)=[\bp_1,\ldots,\bp_s].
\]
Durch Durchschnittsbildung kommt in $\mR$ als Darstellung des Nullideals
\[
        (0)=[[\mR,\bp_1],\ldots,[\mR,\bp_s]],
\]
und die Ideale $[\mR,\bp_i]$ sind nach §1, 3 Primideale.

2b. Aus vollständiger Reduzibilität erster Art von $\mR$ folgt vollständige Reduzibilität erster Art von $\bR$, allgemeiner vollständige Reduzibilität erster Art jedes Zwischenrings von $\mR$ und $\bR$.

Da nach §2, 3 der Darstellung von $\mR$ als direkter Summe eine solche von $\bR$ entspricht, derart daß jede Komponente $\bR_i$ Erweiterungsring $\mR_i[\bP_i]$ wird -- wo $\bP_i$ ein zu $\bP$ isomorpher Erweiterungskörper des zu $P$ isomorphen Unterkörpers $P_i$ der Komponente $\mR_i$ ist -- so genügt die Betrachtung der einzelnen Komponente $\mR_i$. Es kann also ohne Beschränkung der Allgemeinheit angenommen werden, daß $\mR$ Körper, und zwar endlicher Erweiterungskörper erster Art seines Unterkörpers $P$, ist. Somit wird $\mR$ erzeugt durch Adjunktion eines primitiven Elementes erster Art zu $P$; anders ausgedrückt: es wird $\mR$ isomorph dem Restklassenring $P[x]/f(x)$, wo $f(x)$ eine Primfunktion erster Art im Polynombereich $P[x]$ einer Unbestimmten $x$ bedeutet. Ist $Q$ ein Erweiterungskörper von $P$, so wird $Q[x]/f(x)$ isomorph zu $\mR[Q]$; denn beim Übergang von $P[x]$ zu $Q[x]$ bleibt der Rang des Restklassenrings erhalten, gleich dem Grad von $f(x)$, so daß es sich genau um die in §1, 2 angegebene Konstruktion des Erweiterungsringes handelt. In $Q[x]$ zerfällt $f(x)$ in paarweise teilerfremde Primfunktionen erster Art, deren jede ein Primideal in $Q[x]$ erzeugt; das heißt, daß $Q[x]/f(x)$ und damit wegen des Isomorphismus auch $\mR[Q]$ vollständig reduzibel erster Art wird. Da dies für jeden Erweiterungskörper $Q$, insbesondere auch für den algebraisch-abgeschlossenen Körper $\bP$ gilt, ist damit 2b bewiesen.

2c. Ist $\mR$ vollständig reduzibel zweiter Art, so ist $\bR$ nicht vollständig reduzibel.

Wie unter 2b genügt es, $\mR$ als Körper und zwar als endlichen Erweiterungskörper zweiter Art von $P$ anzunehmen. Es ist zu zeigen, daß in der Darstellung von $\bR$ als direkter Summe primärer Ringe mindestens eine eigentlich primäre Komponente auftritt. Dazu genügt der Nachweis eines von Null verschiedenen Elementes aus $\bR$, von dem eine Potenz verschwindet. Denn wenn $\bar\beta\ne0$, muß mindestens eine Komponente $\bar\beta_i\ne0$ sein; wenn $\bar\beta^\rho=0$, muß $\bar\beta_i^\rho=0$ sein (§2, 2, Homomorphismus von $\bR$ zu $\bR_i$), und also wird $\bR_i$ kein Körper.

Sei $\gamma$ ein Element zweiter Art aus $\mR$, vom Exponenten $f\ge 1$; sei
\[
        F(\gamma)=\gamma^{kp^f}+c_1\gamma^{(k-1)p^f}+\cdots+c_k=0
\]
die Gleichung niedrigsten Grades, der $\gamma$ in bezug auf $P$ genügt, wo $p$ die Charakteristik von $P$ bedeutet. Die Elemente
\[
        e,\gamma,\gamma^2,\ldots,\gamma^{kp^f-1}
\]
aus $\mR$ sind also linear unabhängig in bezug auf $P$ und bleiben folglich nach der Gleichheitsdefinition auch in $\bR$ linear unabhängig in bezug auf $\bP$. Setzt man daher
\[
        G(\gamma)=\gamma^{kp^{f-1}}+\sqrt[p]{c_1}\,\gamma^{(k-1)p^{f-1}}+\\cdots+\sqrt[p]{c_k},
\]
so wird $G(\gamma)$ ein von Null verschiedenes Element aus $\bR$; denn wegen $p>1$ und $f\ge1$ wird $kp^{f-1}$ stets kleiner als $kp^f$, es kann also keine Abhängigkeit zwischen den in $G(\gamma)$ auftretenden Potenzen von $\gamma$ bestehen. Es wird aber $G(\gamma)^p=F(\gamma)$ und verschwindet somit; damit ist 2c bewiesen.

Aus 2a und 2c folgt, daß vollständige Reduzibilität erster Art von $\bR$ die vollständige Reduzibilität erster Art von $\mR$ nach sich zieht; aus 2b folgt die Umkehrung. Damit ist der Satz bewiesen.

3. \textbf{Folgerung.} \emph{Ist $\mR$ vollständig reduzibel erster Art, so wird $\bR$ direkte Summe von Ringen vom Range eins, also von zu $\bP$ isomorphen Körpern. Eine solche Zerlegung in Ringe vom Rang eins existiert schon in einem Ring $\mR[Q]$, wo $Q$ endlicher Erweiterungskörper von $P$ ist.}

Denn $\bR$ wird nach 2b direkte Summe von Körpern, die als endliche Erweiterungen von zu $\bP$ isomorphen Unterkörpern -- wegen der algebraischen Abgeschlossenheit von $\bP$ -- mit diesen Unterkörpern identisch werden. Es kommt somit
\[
        \bR=\bP\bar e_1+\cdots+\bP\bar e_n,
        \qquad e=\bar e_1+\cdots+\bar e_n;
\]
die Komponenten $\bar e_i$ von $e$ bilden zugleich eine linear unabhängige Modulbasis von $\bR$.

Werden die $\bar e_i$ nach §1, 2 als lineare Kombinationen der $\gamma$ aus $\mR$ mit Koeffizienten aus $\bP$ dargestellt, so bestimmt das System aller Koeffizienten einen endlichen Erweiterungskörper $Q$ von $P$; es gehören also die $\bar e_i$ zu $\mR[Q]$ und aus der Eigenschaft der Modulbasis kommt
\[
        \mR[Q]=Q\bar e_1+\cdots+Q\bar e_n.
\]
Es gilt somit schon in $\mR[Q]$ eine Zerlegung in Ringe vom Range eins, und in jedem Zwischenring von $\mR[Q]$ und $\bR$ entstehen die unzerlegbaren Komponenten als Erweiterungsringe von $Q\bar e_i$.\footnote{Man kann den kleinsten derartigen Erweiterungskörper $Q$ charakterisieren als Vereinigungskörper der durch die Komponenten $\mR_i=\mR e_i$ von $\mR$ bestimmten Galoisschen Körper. Ist nämlich $f_i(x)$ nach 2b so gewählt, daß $P[x]/f_i(x)$ jeweils zu der Komponente $\mR_i$ isomorph wird, und ist $Q^{(i)}$ der in $\bP$ gelegene Galoissche Erweiterungskörper, der zur Zerlegung von $f_i(x)$ in Linearfaktoren gerade hinreicht, so ist $Q$ der Vereinigungskörper aller $Q^{(i)}$. Der Zerfällung von $f_i(x)$ in Linearfaktoren entspricht eine Darstellung von $Q^{(i)}[x]/f_i(x)$ als direkte Summe von Körpern vom Rang eins; $Q^{(i)}$ ist dabei der kleinste Erweiterungskörper, in dem dies statthat. Wegen der Isomorphie gilt das gleiche für $\mR_i[Q^{(i)}\bar e_i]$ und damit für $\mR[Q]$.}

\subsection*{§4. Matrizendarstellung, Spuren und Diskriminanten bei Ringen endlichen Ranges}

Die Voraussetzungen über den zugrunde liegenden Ring seien in diesem und dem nächsten Paragraphen etwas allgemeiner als bisher, um auch die Ordnungen in Zahl- und Funktionenkörpern zu umfassen. Es handelt sich um eine allgemeine und gleichmäßige Formulierung von im wesentlichen bekannten Dingen.

\emph{Voraussetzung.} $\mR$ sei Erweiterungsring eines Ringes $\mZ$ ohne Nullteiler; das Einheitselement von $\mR$ sei schon in $\mZ$ gelegen. Für jeden $\mZ$-Modul aus $\mR$ -- also insbesondere für $\mR$ selbst -- existiere wenigstens eine endliche Modulbasis, die aus in bezug auf $\mZ$ linear unabhängigen Elementen besteht. Neben $\alpha_1,\ldots,\alpha_s$ wird $\beta_1,\ldots,\beta_s$ dann und nur dann linear unabhängige Modulbasis desselben $\mZ$-Moduls, wenn die Transformationsdeterminante eine Einheit aus $\mZ$ ist.

Die Voraussetzung ist offenbar erfüllt für die bis jetzt betrachteten Ringe endlichen Ranges, wo $\mZ$ zum Körper $P$ wird. Sie ist ebenfalls erfüllt für die Ordnungen in Zahl- und Funktionenkörpern, wo $\mZ$ der Ring der ganzen rationalen Zahlen, bzw. der Funktionalbereich wird.\footnote{Vgl. \emph{Idealtheorie}, §3. Elemente aus $\mZ$ werden mit lateinischen Buchstaben bezeichnet.}

1. \emph{Zu $\mR$ homomorphe Matrizenringe.} Sei $\alpha_1,\ldots,\alpha_s$ eine linear unabhängige $\mZ$-Modulbasis des Ideals $\ma$, und sei $\gamma$ ein beliebiges Element aus $\mR$. Dann gehört auch $\gamma\alpha_i$ zu $\ma$, und es bestehen Gleichungen mit Koeffizienten aus $\mZ$:
\[
        \gamma\alpha_i=c_{i1}\alpha_1+\cdots+c_{is}\alpha_s,
        \qquad i=1,\ldots,s.
\]
Oder in Matrizenform, wenn jeweils $(\tau_1,\ldots,\tau_s)$ die aus diesen Elementen bestehende einzeilige Matrix bedeutet:
\[
 (\gamma\alpha_1,\ldots,\gamma\alpha_s)
 = (\alpha_1,\ldots,\alpha_s)
   \begin{pmatrix}
      c_{11}&\cdots&c_{1s}\\
      \vdots&&\vdots\\
      c_{s1}&\cdots&c_{ss}
   \end{pmatrix}
 = (\alpha_1,\ldots,\alpha_s)C.
\]
Durchläuft $\gamma$ alle Elemente aus $\mR$, so bildet die Gesamtheit der vermöge der Basis $\alpha_i$ zugeordneten Matrizen $C$ einen homomorphen Ring $R_\ma$; es wird $R_\ma$ isomorph dem Restklassenring $\mR/((0):\ma)$, wo $(0):\ma$ den Idealquotienten des Nullideals durch $\ma$ bedeutet. Denn der Differenz $\beta-\gamma$ ist die Differenz $B-C$ zugeordnet, und dasselbe gilt vom Produkt, da
\[
(\beta\gamma\alpha_1,\ldots,\beta\gamma\alpha_s)
=(\gamma\alpha_1,\ldots,\gamma\alpha_s)B
=(\alpha_1,\ldots,\alpha_s)CB.
\]
Den Elementen $c$ aus $\mZ$ sind insbesondere die Diagonalmatrizen $cE$ zugeordnet. Allen und nur den Elementen $\gamma$, für die $\gamma\ma$ gleich dem Nullideal wird, ist die Nullmatrix zugeordnet.

Eine andere Basis $\bar\alpha_1,\ldots,\bar\alpha_s$ desselben Ideals erzeugt einen zu $R_\ma$ isomorphen, und zwar äquivalenten, Matrizenring:
\[
        R_{\bar\ma}=P^{-1}R_\ma P.
\]
Ist nämlich $(\bar\alpha_1,\ldots,\bar\alpha_s)=(\alpha_1,\ldots,\alpha_s)P$, so wird für die zugehörigen Matrizen $C'$ und $C$ stets $C'=P^{-1}CP$.

2. \emph{Idealklassen und Darstellungsklassen.} Zwei Ideale $\ma$ und $\mb$ aus $\mR$ gehören derselben Idealklasse an, wenn sie -- aufgefaßt als $\mR$-Moduln -- isomorph sind, wenn also ihre Elemente sich derartig eineindeutig zuordnen lassen, daß Differenz und Multiplikation mit demselben Element aus $\mR$ sich entsprechen. Zwei durch Matrizendarstellung nach 1 erzeugte Ringe $R_\ma$ und $R_\mb$ gehören derselben Darstellungsklasse an, wenn sie äquivalent sind, wenn es also eine unimodulare Matrix $P$ gibt, so daß
\[
        R_\mb=P^{-1}R_\ma P
\]
wird.

Die Idealklassen und Darstellungsklassen entsprechen sich eineindeutig. Verschiedene Modulbasen desselben Ideals liefern äquivalente Matrizenringe; verschiedene Ideale derselben Idealklasse liefern ebenfalls äquivalente Matrizenringe, weil zueinander gehörige Basiselemente durch Multiplikation mit jedem $\gamma\in\mR$ dieselben linearen Koeffizienten liefern. Gehören umgekehrt $R_\ma$ und $R_\mb$ derselben Darstellungsklasse an, so liefert die Transformationsmatrix eine Basis von $\mb$, bei der jedem Element $a=c_1\alpha_1+\\cdots+c_s\alpha_s$ aus $\ma$ das Element $b=c_1\beta_1+\cdots+c_s\beta_s$ aus $\mb$ entspricht; aus der Gleichheit der Matrizenringe folgt $\gamma a\leftrightarrow\gamma b$. Als Hauptklasse wird die Klasse des Einheitsideals bezeichnet.

3. \emph{Spur eines Elementes bezüglich einer Klasse.} Sei $C$ die dem Element $\gamma$ aus $\mR$ in $R_\ma$ zugeordnete Matrix; der Übergang von $R_\ma$ zu einem äquivalenten Ring zeigt, daß $|C|$ und allgemeiner $|tE-C|$, wo $t$ eine Unbestimmte bedeutet, eine Invariante der Darstellungsklasse ist. Damit handelt es sich zugleich um eine Invariante der Idealklasse. Der Koeffizient von $(-t^{s-1})$ in $|tE-C|$ soll als Spur von $\gamma$ in bezug auf die Klasse bezeichnet werden:
\[
        S_{(\ma)}(\gamma)=c_{11}+c_{22}+\cdots+c_{ss}.
\]
Der von $t$ freie Koeffizient $\pm |C|$ wird als Norm von $\gamma$ in bezug auf die Klasse bezeichnet.

Die Spuren $S_{(\ma)}(\gamma)$ in bezug auf eine feste Klasse bilden einen $\mZ$-Modul, zu dem $\mR$, aufgefaßt als $\mZ$-Modul, homomorph wird. Denn nach dem unter 1 bewiesenen Ringhomomorphismus kommt
\[
        S_{(\ma)}(\beta-\gamma)=S_{(\ma)}(\beta)-S_{(\ma)}(\gamma),
        \qquad
        S_{(\ma)}(c\beta)=cS_{(\ma)}(\beta).
\]
Ist $\mR$ ein Ring ohne Nullteiler, so ist die Spur eines Elementes unabhängig von der Klasse definiert; man kann also von Spur schlechthin reden; dasselbe gilt von der Norm. Denn jedes vom Nullideal verschiedene Ideal hat dann den Rang des Rings, und jede Basis eines Ideals ist zugleich Basis des Einheitsideals im Quotientenkörper.

4. \emph{Diskriminante eines Ideals bezüglich einer Klasse.} Nicht die Diskriminante selbst, sondern erst das daraus in $\mZ$ abgeleitete Hauptideal ergibt sich als Invariante von Ideal und Klasse. Seien $\omega_1,\ldots,\omega_s$ und $\bar\omega_1,\ldots,\bar\omega_s$ zwei linear unabhängige $\mZ$-Modulbasen eines Ideals $\mt$ aus $\mR$. Die Determinanten
\[
        |S_{(\ma)}(\omega_i\omega_j)|,
        \qquad
        |S_{(\ma)}(\bar\omega_i\bar\omega_j)|
\]
unterscheiden sich nur um das Quadrat einer Einheit aus $\mZ$ als Faktor. Dies gilt für die beiden Determinanten der Basisprodukte; wegen des Modulhomomorphismus bestehen zwischen den Spuren dieselben linearen Beziehungen wie zwischen den Produkten, also nimmt die Determinante bei der kogredienten Transformation denselben quadratischen Faktor an.

Als bis auf das Quadrat einer Einheit bestimmte Diskriminante von $\mt$ bezüglich der Klasse $(\ma)$ wird jede Determinante $|S_{(\ma)}(\omega_i\omega_j)|$ bezeichnet; als Diskriminantenideal das daraus in $\mZ$ abgeleitete Hauptideal. Ist $\mR$ ein Ring ohne Nullteiler, so wird die Diskriminante eines Ideals unabhängig von der zugrunde gelegten Klasse.

\subsection*{§5. Direkte Summen der Klassen}

1. \emph{Direkte Summe der Idealklasse oder Darstellungsklasse.} Ist das Ideal $\ma$ direkte Summe, $\ma=\mb+\mc$, so folgt aus dem Isomorphismus, daß jedes Ideal der Klasse direkte Summe wird, $\bar\ma=\bar\mb+\bar\mc$. Dabei wird jeweils $\bar\mb$ zu $\mb$ und $\bar\mc$ zu $\mc$ isomorph, die Ideale als $\mR$-Moduln aufgefaßt; man kann also von direkter Summe der Idealklasse und von den Komponentenklassen reden.

Ist ein Ring $R_\ma$ einer Darstellungsklasse direkte Summe von Unterringen, die zugleich Ideale in $R_\ma$ sind, so gilt das wegen des Isomorphismus für jeden Ring $R_\mb$ der Darstellungsklasse, und zwar werden die Komponenten äquivalente Ringe. Man kann also von direkter Summe der Darstellungsklasse und von den Komponentenklassen reden. Der direkten Summe der Idealklasse entspricht die direkte Summe der Darstellungsklasse; in diesem Sinn soll von direkter Summe der Klasse gesprochen werden.\footnote{Auf die Frage, inwieweit alle direkten Summen der Darstellungsklassen durch direkte Summen der Idealklassen erzeugt werden können, soll hier nicht eingegangen werden.}

Sei $\beta_1,\ldots,\beta_m$ eine Basis von $\mb$ und $\gamma_1,\ldots,\gamma_n$ eine Basis von $\mc$; wegen der direkten Summe bilden die $\beta$ und $\gamma$ zusammen eine linear unabhängige Basis von $\ma$. Ist $R_\ma$ der vermöge dieser Basis erzeugte Matrizenring, so wird die einem beliebigen Element $\delta$ aus $\mR$ in $R_\ma$ zugeordnete Matrix von der Form
\[
        \begin{pmatrix}D^{\mb}&0\\0&D^{\mc}\end{pmatrix}.
\]
Die Systeme der Matrizen mit nur dem ersten bzw. nur dem zweiten Block bilden Ideale $R^{\mb}$ bzw. $R^{\mc}$ in $R_\ma$; ihr Produkt verschwindet, und $R_\ma=R^{\mb}+R^{\mc}$ ist direkte Summe. Die Zuordnung der Matrix $D^{\mb}$ zu der Blockmatrix aus $R^{\mb}$ liefert einen Isomorphismus mit dem aus $\mb$ erzeugten Ring $R_\mb$, entsprechend für $\mc$.

2. \emph{Verhalten von Spur und Diskriminante bei direkter Summe der Klassen.} Ist $\ma=\mb+\mc$ und legt man zur Bestimmung der Spur $S_{(\ma)}(\delta)$ die Blockmatrix zugrunde, so liest man ab: Die Spur eines Elementes, genommen in bezug auf eine Klasse, ist die Summe der Spuren, genommen in bezug auf die Komponentenklassen. Wegen des Verschwindens des Produktes $\mb\mc$ liest man weiter ab: Ist $\beta$ Element aus $\mb$, so wird $S_{(\ma)}(\beta)=S_{(\mb)}(\beta)$; die Spur von $\beta$ in bezug auf die Klasse $(\ma)$ ist gleich der Spur in bezug auf die Klasse $(\mb)$.

Daraus folgt: Das Diskriminantenideal des Komponentenideals $\mb$, genommen in bezug auf die Klasse $(\ma)$, ist gleich dem Diskriminantenideal von $\mb$, genommen in bezug auf die Komponentenklasse $(\mb)$. Schließlich ist das Diskriminantenideal von $\ma$, genommen in bezug auf $(\ma)$, gleich dem Produkt der Diskriminantenideale der Komponenten, genommen in bezug auf die Komponentenklassen. Denn bei der Basis der direkten Summe ist die Diskriminantenmatrix blockdiagonal:
\[
\left|
\begin{array}{cc}
S_{(\mb)}(\beta_i\beta_j)&0\\
0&S_{(\mc)}(\gamma_i\gamma_j)
\end{array}
\right|
=|S_{(\mb)}(\beta_i\beta_j)|\,|S_{(\mc)}(\gamma_i\gamma_j)|.
\]

3. \emph{Übergang zu Erweiterungsringen.} Sei $\mX$ ein Erweiterungsring ohne Nullteiler von $\mZ$ derart, daß der Durchschnitt von $\mR$ und $\mX$ durch $\mZ$ gegeben ist; sei der durch Adjunktion von $\mX$ entstandene Erweiterungsring $\bR=\mR[\mX]$ als Ring gleichen Ranges definiert. Sind $\ma$ und $\mt$ Ideale in $\mR$ und $\ba$ und $\bar\mt$ ihre Erweiterungsideale in $\bR$, so wird auch das Diskriminantenideal von $\bar\mt$, genommen in bezug auf $(\ba)$, Erweiterungsideal in $\mX$ des von $\mt$ in bezug auf $(\ma)$ genommenen. Denn jede Basis von $\ma$ oder $\mt$ ist auch $\mX$-Modulbasis von $\ba$ oder $\bar\mt$. Insbesondere wird das Diskriminantenideal von $\bR$ in bezug auf die Hauptklasse Erweiterungsideal desjenigen von $\mR$ in bezug auf die Hauptklasse.

\subsection*{§6. Diskriminantenkriterium der vollständigen Reduzibilität erster Art}

Von jetzt an sei $\mR$ wieder als Erweiterungsring eines Körpers $P$ vorausgesetzt und $\bP$ sei Erweiterungskörper von $P$. Als notwendige und hinreichende Bedingung für vollständige Reduzibilität erster Art wird sich das Nichtverschwinden der Diskriminante ergeben. Nach §5, 3 läßt sich das Diskriminantenideal von $\mR$ vermöge des Erweiterungsringes $\bR=\mR[\bP]$ bestimmen; und zwar genügt es nach §5, 2, sich auf die primären Ringe zu beschränken, aus denen sich $\bR$ additiv zusammensetzt.

1. \emph{Spezielle $P$-Modulbasen.} Sind $\ma$ und $\mt$ Ideale aus $\mR$ und ist $\ma$ teilbar durch $\mt$, so läßt sich jede Basis von $\ma$ durch Hinzufügung von Elementen zu einer linear unabhängigen Modulbasis von $\mt$ ergänzen. Ist $\mR$ primärer Ring, $\mpideal$ das zum Nullideal gehörige Primideal, $\rho$ der Exponent, also $\mpideal^\rho=(0)$ und $\mpideal^{\rho-1}\ne(0)$, so ergibt sich eine spezielle $P$-Modulbasis von $\mR$, indem man sukzessive eine Basis von $\mpideal^{\rho-1}$ zu einer solchen von $\mpideal^{\rho-2}$ ergänzt, allgemein eine solche von $\mpideal^i$ zu einer Basis von $\mpideal^{i-1}$, wobei $\mR$ als $\mpideal^0$ zu zählen ist.

Vermöge einer solchen Basis läßt sich zeigen: Ist $\mR$ primärer Ring, so verschwindet die in bezug auf die Hauptklasse genommene Spur jedes durch das zugehörige Primideal $\mpideal$ teilbaren Elementes. Sei nämlich
\[
\eta_1,\ldots,\eta_s
\]
eine solche spezielle Basis mit $\eta_j\equiv0\pmod{\mpideal^i}$ in den entsprechenden Stufen. Aus $\pi\equiv0\pmod{\mpideal}$ folgt $\pi\eta_j\equiv0$ in der jeweils nächsthöheren Stufe; also enthält die zugeordnete Matrix in der Diagonale nur Nullen, und die Spur von $\pi$ verschwindet.\footnote{Die von einer solchen Basis vermittelte Matrizendarstellung hat die zu den Idealfaktoren passende Block-Dreiecksform. Da bei vollständiger Reduzibilität die unzerlegbaren Komponenten kein vom Null- oder Einheitsideal verschiedenes Ideal besitzen, kann in der Darstellungsklasse dieser Komponentenideale eine solche nichttriviale Darstellung nicht auftreten.}

2. \emph{Diskriminantenideale primärer Ringe mit algebraisch-abgeschlossenem Unterring $P$.} Ist das Nullideal von $\mR$ Primideal und $P$ algebraisch abgeschlossen, so wird $\mR$ vom Range eins, also mit $P$ identisch (§3, 3); das Einheitselement $e$ kann als Modulbasis genommen werden. Damit kommt
\[
        S_{(\mR)}(e^2)=S_{(\mR)}(e)=e,
\]
und das Diskriminantenideal wird gleich dem Einheitsideal.

Das Diskriminantenideal eines eigentlich primären Rings mit algebraisch abgeschlossenem Unterring $P$ wird gleich dem Nullideal. Legt man die unter 1 gegebene spezielle Modulbasis zugrunde -- oder ergänzt man nur irgendeine Basis von $\mpideal$ zu einer Basis von $\mR$ --, so kann als erstes Basiselement das Einheitselement $e$ genommen werden, und alle von $e^2$ verschiedenen Basisprodukte sind durch $\mpideal$ teilbar; ihre Spur verschwindet. Damit verschwindet die Diskriminante als mindestens zweireihige Determinante, für die alle von $S(e^2)$ verschiedenen Elemente verschwinden.

3. \textbf{Satz.} \emph{Der Ring $\mR$ von endlichem Rang in bezug auf einen Unterkörper $P$ ist dann und nur dann vollständig reduzibel erster Art, wenn sein Diskriminantenideal das Einheitsideal in $P$ ist; mit anderen Worten, wenn die bis auf das Quadrat einer Einheit bestimmte Diskriminante von Null verschieden ist.}

Die vollständige Reduzibilität erster Art ist nach §3, 2 identisch damit, daß der Erweiterungsring $\bR=\mR[\bP]$ vollständig reduzibel ist, daß also dessen unzerlegbare Komponenten zu $\bP$ isomorphe Körper werden. Nach §5, 3 ist das Diskriminantenideal von $\mR$ und gleichzeitig das von $\bR$ Null- bzw. Einheitsideal; und nach §5, 2 wird das Diskriminantenideal von $\bR$ das Produkt der Diskriminantenideale der Komponenten, genommen in bezug auf diese Komponenten. Die einzelnen Diskriminantenideale entstehen aus den unter 2 betrachteten, indem jeweils der zu $\bP$ isomorphe Körper $P_i=\bP e_i$ durch $\bP$ selbst ersetzt wird. Daraus folgt, daß bei vollständiger Reduzibilität von $\bR$ das Diskriminantenideal als Produkt von Einheitsidealen selbst das Einheitsideal wird, während, wenn $\bR$ nicht vollständig reduzibel ist, für mindestens eine Komponente das Diskriminantenideal gleich dem Nullideal wird, wodurch auch für das Diskriminantenideal von $\bR$ sich das Nullideal ergibt. Damit ist der Satz bewiesen.

4. \emph{Darstellung von Spur, Norm und Diskriminante durch konjugierte Elemente im Fall der vollständigen Reduzibilität erster Art.} Sei nach §3, 3 bei vollständiger Reduzibilität erster Art
\[
        \mR[Q]=Qe_1+\cdots+Qe_n
\]
eine Darstellung als direkte Summe von Ringen vom Rang eins, und sei $Q$ der kleinste Erweiterungskörper, in dem eine solche Darstellung möglich. Die $n$ Komponenten von $\mR$ seien gegeben durch $K_i e_i$, wo jeweils $K_i$ Unterkörper von $Q$ ist und $\mR$ homomorph zu $K_i$ wird.

Legt man $e_1,\ldots,e_n$ als Modulbasis zugrunde, so wird jedem Element
\[
        \gamma=c_1e_1+\cdots+c_ne_n
\]
die Diagonalmatrix mit Diagonale $c_1,\ldots,c_n$ zugeordnet. Somit kommt
\[
        S(\gamma)=c_1+\cdots+c_n,
        \qquad
        N(\gamma)=c_1\cdots c_n,
\]
unter $S(\gamma)$ die Spur und $N(\gamma)$ die Norm in bezug auf die Hauptklasse verstanden. Bedeutet weiter $\alpha_1,\ldots,\alpha_n$ eine aus Elementen aus $\mR$ bestehende Modulbasis von $\mR$, und ist
\[
        \alpha_j=\alpha_j^{(1)}e_1+\cdots+\alpha_j^{(n)}e_n,
\]
so kommt
\[
        |S(\alpha_i\alpha_j)|=
        \left|\alpha_i^{(k)}\right|^2,
\]
d.h. die Diskriminante wird als Quadrat der Determinante der konjugierten Basiselemente dargestellt. Ist insbesondere $\mR$ selbst Körper, also Erweiterungskörper erster Art des Unterkörpers $P$, so gehen die Homomorphismen von $\mR$ zu den $K_i$ in Isomorphismen über, und die $K_i$ sind in bezug auf $P$ konjugierte Körper im Galoisschen Erweiterungskörper $Q$. Die Darstellung von Spur, Norm und Diskriminante geht dann in die bekannte Darstellung durch konjugierte Elemente über. Dabei ist zu beachten, daß $\mR$ nur als zu den $K_i$ äquivalenter und nicht selbst im Galoisschen Erweiterungskörper $Q$ gelegener Körper vorausgesetzt ist.

\subsection*{§7. Der Diskriminantensatz für die Ordnungen eines algebraischen Zahl- oder Funktionenkörpers}

1. Die in Betracht kommenden Zahl- und Funktionenkörper ordnen sich alle dem folgenden Körpertypus mit Festlegung der ganzen Elemente unter. Sei $\mo$ ein Hauptidealring ohne Nullteiler und mit Einheitselement (Ring der ganzen rationalen Zahlen, Polynombereich einer Unbestimmten mit Koeffizienten aus einem Körper, Funktionalbereich) und $\Omega$ sein Quotientenkörper; sei $K$ eine endliche Erweiterung erster Art von $\Omega$ und $\mO$ der Ring aller in bezug auf $\mo$ ganzen Größen aus $K$. Jeder Unterring von $\mO$, der $\mo$ umfaßt, wird eine Ordnung genannt; $\mO$ selbst wird als Hauptordnung bezeichnet.

Jeder $\mo$-Modul in $\mO$, insbesondere alle Ideale einer Ordnung und die Ordnung selbst, besitzt eine linear unabhängige Modulbasis in bezug auf $\mo$. Ist $K$ vom Grade $n$ in bezug auf $\Omega$, so genügt es, sich auf Ordnungen vom Range $n$ in bezug auf $\mo$ zu beschränken. Jede Ordnung $\mT$ ist also ein Ring, für den die Voraussetzungen von §4 und §5 zutreffen, und zwar ein Ring ohne Nullteiler; somit ist die Diskriminante $D_\mT$ der Ordnung bis auf das Quadrat einer Einheit aus $\mo$ als Faktor eindeutig definiert. Diese Diskriminante ist zugleich bis auf eine Einheit aus $\Omega$ als Faktor mit der Diskriminante des Körpers $K$, aufgefaßt als $\Omega$-Modul, identisch und somit von Null verschieden, da $K$ als Erweiterung erster Art vorausgesetzt war (§6, 3). Die Diskriminante läßt zugleich die in §6, 4 gegebene Darstellung durch das Determinantenquadrat konjugierter Basiselemente zu.

Die Idealtheorie in $\mT$ ist gegeben durch den Satz: In $\mT$ läßt sich jedes vom Nullideal verschiedene Ideal eindeutig darstellen als Produkt von endlich vielen paarweise teilerfremden Primäridealen, deren zugehörige Primideale keinen vom Einheitsideal verschiedenen echten Teiler besitzen.

2. \emph{Übergang von der Ordnung zu den Ringen endlichen Ranges in bezug auf einen Körper.} Sei $p$ eine Primgröße aus $\mo$, also das aus $p$ in $\mo$ abgeleitete Hauptideal ein von Null- und Einheitsideal verschiedenes Primideal $\mo p$ aus $\mo$; bezeichne entsprechend $\mT p$ das aus $p$ in $\mT$ abgeleitete Hauptideal. Der Restklassenring
\[
        \mR=\mT/\mT p
\]
wird ein Ring vom Range $n$ in bezug auf einen zu dem Körper $\mo/\mo p$ isomorphen Unterkörper $P$; es wird $\mT$ homomorph zu $\mT/\mT p$.

Daß $\mR$ einen zu $\mo/\mo p$ isomorphen Unterkörper enthält, folgt aus dem zweiten Isomorphiesatz; der Unterring $(\mo,\mT p)/\mT p$ wird isomorph zu $\mo/[\mo,\mT p]=\mo/\mo p$. Sei $\alpha_1,\ldots,\alpha_n$ eine linear unabhängige Modulbasis von $\mT$ in bezug auf $\mo$, und seien $(\alpha_i)$ die durch die Elemente $\alpha_i$ bestimmten Restklassen nach $\mT p$. Jede lineare Abhängigkeit zwischen den $(\alpha_i)$ in bezug auf $P$ entstünde aus einer Kongruenz
\[
        c_1\alpha_1+\cdots+c_n\alpha_n\equiv0\pmod{\mT p},
\]
also aus einer Relation $c_1\alpha_1+\cdots+c_n\alpha_n=py$ mit $y\in\mT$. Aus der Darstellbarkeit von $y$ durch die Basis der $\alpha_i$ und der Eindeutigkeit dieser Darstellung folgt, daß jedes $c_i$ durch $p$ teilbar ist; damit verschwindet das Element $(c_i)$ aus $P$. Also bilden die $(\alpha_i)$ eine Basis von $\mR$ über $P$.

Durch den Homomorphismus geht die Diskriminante $D_\mT$ von $\mT$ in die Diskriminante von $\mR$ über, die Idealzerlegung von $\mT p$ in die Zerlegung des Nullideals von $\mR$. Ist
\[
        \mT p=\mq_1\cdots\mq_s=[\mq_1,\ldots,\mq_s]
\]
die Darstellung von $\mT p$ in $\mT$, so geht diese durch den Homomorphismus über in
\[
        (0)=[(\mq_1),\ldots,(\mq_s)]
\]
in $\mR$. Nach dem ersten Isomorphiesatz wird $\mR/(\mq_i)$ isomorph zu $\mT/\mq_i$; es sind also gleichzeitig $\mq_i$ und $(\mq_i)$ eigentliche Primärideale oder Primideale, und wegen des Homomorphismus sind die $(\mq_i)$ paarweise teilerfremd.

3. \textbf{Diskriminantensatz.} \emph{Eine Primgröße $p$ aus $\mo$ geht dann und nur dann in der Diskriminante $D_\mT$ einer Ordnung $\mT$ auf, wenn in der Zerlegung des Hauptideals $\mT p$ in paarweise teilerfremde Primärkomponenten aus $\mT$ mindestens eine eigentliche Primärkomponente oder mindestens ein Primideal zweiter Art auftritt.}

Ist der Restklassenkörper $\mo/\mo p$ ein vollkommener Körper, so besitzt also jede in $D_\mT$ aufgehende Primgröße $p$ und nur diese mindestens eine eigentliche Primärkomponente. Ist $\mo/\mo p$ vollkommen und wird für $\mT$ die Hauptordnung $\mO$ gewählt, so geht $p$ dann und nur dann in der Diskriminante auf, wenn es mindestens durch das Quadrat eines Primideals aus $\mO$ teilbar ist.

Denn wegen des Entsprechens der Idealzerlegung von $\mT p$ in $\mT$ und des Nullideals in $\mR=\mT/\mT p$ bedeutet das Auftreten einer eigentlichen Primärkomponente oder eines Primideals zweiter Art von $\mT p$, daß $\mR$ nicht vollständig reduzibel erster Art ist. Das ist aber nach §6, 3 dann und nur dann der Fall, wenn die Diskriminante von $\mR$ verschwindet, was wegen des Homomorphismus nach 2 die Teilbarkeit von $D_\mT$ durch $p$ aussagt.\footnote{Ein Beispiel für das Auftreten von Primidealen zweiter Art: Sei $\mo$ der abgeleitete Funktionalbereich, $\Omega$ der Quotientenkörper und $K$ der durch Adjunktion von $\sqrt{x}$ entstehende Erweiterungskörper. Betrachtet wird die Ordnung $\mT$ mit der Modulbasis $1,\sqrt{x}$ in bezug auf $\mo$. Die Diskriminante ist $\left|\begin{smallmatrix}1&\sqrt{x}\\1&-\sqrt{x}\end{smallmatrix}\right|^2$. Das Hauptideal $\mT2$ bleibt Primideal in $\mT$, aber zweiter Art, denn $\mT/\mT2$ ist isomorph zu $P[t]/(t^2-x)$, wo $P$ der entsprechende Restklassenkörper ist. Dagegen ist $\mT\sqrt{x}$ eigentlich primär. Ersetzt man eine Unbestimmte durch zwei, so erhält man Beispiele, in denen erst durch Adjunktion von zwei Elementen zu $P$ erzeugt wird.}

\subsection*{§8. Der Diskriminantensatz für die Ordnungen von Relativkörpern}

Legt man statt des Hauptidealrings $\mo$ schon als Ausgangsring die Hauptordnung eines Zahl- oder Funktionenkörpers zugrunde -- allgemeiner einen Multiplikationsring, d.h. einen Ring, in dem die Idealtheorie der Hauptordnung gilt --, so tritt an Stelle der Diskriminante $D_\mT$ das Diskriminantenideal von $\mT$ in bezug auf $\mo$; der Diskriminantensatz überträgt sich vollständig vermöge der folgenden Überlegungen.

1. Sei $\mo$ ein Multiplikationsring, also ein Ring ohne Nullteiler und mit Einheitselement, in dem jedes von Null- und Einheitsideal verschiedene Ideal sich eindeutig als Potenzprodukt von Primidealen darstellen läßt, und wo diese Primideale keinen vom Einheitsideal verschiedenen echten Teiler besitzen. Seien weiter $\Omega,K,\mO,\mT$ wie unter §7, 1 definiert; dann gilt in $\mT$ die dort angegebene Idealtheorie.

Besitzt insbesondere ein Multiplikationsring $\mo$ nur ein vom Null- und Einheitsideal verschiedenes Primideal, so wird $\mo$ Hauptidealring. Denn wählt man ein Element $p$, dessen Hauptideal genau durch die erste Potenz dieses Primideals teilbar ist, so erzeugt $p$ das Primideal selbst; aus der Potenzdarstellung folgt dann, daß auch alle übrigen Ideale Hauptideale sind.

2. \emph{Spur und Diskriminantenideal.} Die Spur jedes Elements $\gamma$ aus $\mT$ ist definiert als Spur von $\gamma$ aus $K$, wobei $K$ als $\Omega$-Modul aufgefaßt ist. Jedes System von $n$ in bezug auf $\Omega$ linear unabhängigen Elementen aus $K$ kann als Basis zur Erzeugung der Spur gewählt werden. Ist $\alpha_1,\ldots,\alpha_n$ ein solches System, so wird, da $K$ als Erweiterung erster Art vorausgesetzt ist,
\[
        |S(\alpha_i\alpha_j)|
\]
ein von Null verschiedenes Element aus $\Omega$, und zwar das Determinantenquadrat der konjugierten Basiselemente. Aus der ganzen Abgeschlossenheit von $\mo$ in $\Omega$ folgt, daß diese Determinante Element aus $\mo$ ist, sobald alle $\alpha_i$ zu $\mO$ gehören.

Es durchlaufen $\tau_1,\ldots,\tau_n$ alle Systeme von je $n$ Elementen aus $\mT$; zu jedem System sei die Determinante $|S(\tau_i\tau_j)|$ gebildet. Das aus der Gesamtheit dieser Determinanten in $\mo$ abgeleitete Ideal wird als das Diskriminantenideal der Ordnung $\mT$ in bezug auf $\mo$ bezeichnet. Besitzt insbesondere die Ordnung eine linear unabhängige Modulbasis $\alpha_1,\ldots,\alpha_n$, so kann die Determinante $|S(\alpha_i\alpha_j)|$ als Basis des Diskriminantenideals genommen werden; die Definition deckt sich mit der in §7, 1.

3. \emph{Übergang zum Quotientenring.} Ist $\ma$ ein beliebiges Ideal aus $\mo$, so ist der Quotientenring $\mT_\ma$ definiert als Gesamtheit der Elemente des Quotientenkörpers von $\mT$, die ein zu $\ma$ primes Element aus $\mT$ im Nenner haben. Der Ring $\mT_\ma$ besitzt außer Null- und Einheitsideal keine weiteren Primideale als die Erweiterungsideale der zu $\ma$ gehörigen Primideale. Es besteht eineindeutiges Entsprechen und Isomorphismus der Restklassenringe zwischen den nichttrivialen Idealen aus $\mT_\ma$ und denjenigen Idealen aus $\mT$, deren zugehörige Primideale unter den zu $\ma$ gehörigen enthalten sind; außerdem entsprechen Null- und Einheitsideal sich eineindeutig.

Dieselben Überlegungen gelten für den Multiplikationsring $\mo$. Ist insbesondere $\mpideal$ ein Primideal, so besitzt $\mo_\mpideal$ nur ein von Null- und Einheitsideal verschiedenes Primideal; wegen des Isomorphismus der Restklassenringe ist mit $\mo$ auch $\mo_\mpideal$ Multiplikationsring, und nach 1 wird somit $\mo_\mpideal$ Hauptidealring. Die Basis des aus $\mpideal$ abgeleiteten Primideals wird eine Primgröße $p$ aus $\mo_\mpideal$. Das Erweiterungsideal eines beliebigen Ideals $\mc$ aus $\mo$ wird erzeugt durch die zu $\mpideal$ gehörige Primärkomponente von $\mc$, wird also gleich $(p)^\alpha$ oder dem Einheitsideal, je nachdem $\mpideal$ in $\mc$ aufgeht -- und zwar zur $\alpha$-ten Potenz -- oder nicht aufgeht. Stimmen die Erweiterungen zweier Ideale in allen Quotientenringen $\mo_\mpideal$ überein, so sind die Ideale identisch.

4. \emph{Das Diskriminantenideal beim Übergang zum Quotientenring.} Sei $\mpideal$ ein Primideal aus $\mo$ und bezeichne $\mP=\mT\mpideal$ das Erweiterungsideal in $\mT$. Der Quotientenring $\mT_\mP$ besitzt eine aus linear unabhängigen Elementen bestehende Modulbasis in bezug auf den Hauptidealring $\mo_\mpideal$; zugleich ist $\mT_\mP$ eine endliche $\mpideal$-Ordnung in dem Erweiterungskörper $K$ des Quotientenkörpers von $\mo_\mpideal$. Somit ist in $\mT_\mP$ in bezug auf $\mo_\mpideal$ der Diskriminantensatz (§7, 3) erfüllt.

Die Diskriminante $D_{\mT_\mP}$ bildet dabei eine Basis des in $\mo_\mpideal$ genommenen Erweiterungsideals des Diskriminantenideals von $\mT$ in bezug auf $\mo$. Man nehme nämlich als $\mo_\mpideal$-Modulbasis von $\mT_\mP$ Elemente $\alpha_1,\ldots,\alpha_n$ aus $\mT$; dann gehört $|S(\alpha_i\alpha_j)|$ zum Diskriminantenideal, und jede Determinante $|S(\tau_i\tau_j)|$ wird in $\mo_\mpideal$ durch $|S(\alpha_i\alpha_j)|$ teilbar.

5. \textbf{Allgemeiner Diskriminantensatz.} \emph{Ein Primideal $\mpideal$ aus einem Multiplikationsring $\mo$ geht dann und nur dann in dem Diskriminantenideal einer Ordnung in bezug auf $\mo$ auf, wenn in der Idealzerlegung von $\mT\mpideal$ in paarweise teilerfremde Primärkomponenten aus $\mT$ mindestens eine eigentliche Primärkomponente oder mindestens ein Primideal zweiter Art auftritt.}

Aus 3 und 4 folgt, daß ein Primideal $\mpideal$ aus $\mo$ dann und nur dann in dem Diskriminantenideal von $\mT$ aufgeht, wenn $p$ aus $\mo_\mpideal$ in der Diskriminante $D_{\mT_\mP}$ aufgeht. Das ist aber identisch damit, daß der Restklassenring $\mT_\mP/\mT_\mP p$ nicht vollständig reduzibel erster Art ist; da dieser Restklassenring isomorph ist zu $\mT/\mT\mpideal$, ist damit der allgemeine Diskriminantensatz bewiesen.\footnote{Hieraus ergibt sich -- wenn $\mo$ die Hauptordnung eines algebraischen Zahlkörpers ist -- die Übereinstimmung mit der von Hilbert definierten Relativdiskriminante, die mit der von Hecke definierten übereinstimmt.}

Als Spezialisierung auf Relativdiskriminanten von Zahlkörpern kommt: Ein Primideal $\mpideal$ der Hauptordnung eines Zahlkörpers geht dann und nur dann in der Relativdiskriminante eines Erweiterungskörpers auf, wenn $\mpideal$ in der Hauptordnung des Erweiterungskörpers durch das Quadrat eines Primideals teilbar wird.

Göttingen, 30. März 1926.

\end{document}
