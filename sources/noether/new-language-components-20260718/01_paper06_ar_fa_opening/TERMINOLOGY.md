# Terminology note

This tranche contains independent Arabic and Iranian-Persian choices. Similar
scripts and shared loanwords are not used as authority transfer.

All listed forms are working candidates. The two `field` decisions have
machine-readable records validated against the research department's
operational-decision schema. The recovered counts are concept-shelf routing
evidence, not support rows, truth probabilities, readiness scores, or external
review.

| German | Arabic working choice | Iranian Persian working choice | Status |
| --- | --- | --- | --- |
| `Körper` | `حقل` | `میدان` | Supported by recovered algebra anchors; still subject to domain review in this historical context. |
| `System` | `نظام` | `دستگاه` | Contextual working choices, not cross-lane equivalents. |
| `rationale Funktionen` | `الدوال النسبية` | `توابع گویا` | Standard-looking working register; external mathematical review pending. |
| `ganze rationale Funktionen` | `الدوال النسبية الصحيحة` | `توابع گویای صحیح` | Deliberately close to the historical German. Review whether modern exposition should instead gloss these as polynomial functions. |
| `Rationalfunktionenkörper` | `حقول الدوال النسبية` | `میدان‌های توابع گویا` | Transparent compound retained. |
| `Basisfragen` | `مسائل الأساس` | `مسائل مربوط به پایه` | Provisional because the paper uses several distinct basis notions later. |
| `Modulbasis` | `أساس المودول` | `پایهٔ مدولی` | Held working forms. Arabic `مودول` is explicitly the module sense and must never be counted as modulus evidence. |
| `Rationalbasis` | `أساس نسبي` | `پایهٔ گویا` | Working historical terms; keep distinct from module basis and later minimal/involution bases. |
| `rationale Darstellbarkeit` | `قابلية التمثيل النسبي` | `نمایش‌پذیری گویا` | Working renderings; review against native expository usage. |
| `Fundamentalpunkte` | `نقاط أساسية` | `نقاط بنیادی` | Historical geometric sense; compare modern base-point terminology before promotion. |

The current unit now invokes the module concept. Recovered Persian module
counts remain concept-shelf evidence, while the recovered Arabic material says
module terminology is variable. The corresponding forms are therefore held
rather than promoted.

## Review questions carried forward

1. Decide whether Arabic `نظام` or a more specific local mathematical term is
   preferable for Noether's broad use of *System*.
2. Decide whether Persian `دستگاه` should remain primary or be paired with
   `سیستم` in a first-occurrence gloss.
3. Resolve the historical phrase *ganze rationale Funktionen* consistently
   across all later units before promoting either wording to a lane glossary.

Standing Arabic F14 adverse controls are maintained in the lane-level
`ADVERSE_LEDGER.csv`: `مودول` is not modulus evidence in the probed context,
and `شكل أساسي` is not established evidence for the intended invariant-theory
ground-form sense.
