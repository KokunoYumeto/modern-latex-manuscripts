# Source check and review - SGA 1 Exposé I §I.8

The complete bounded §I.8 unit, French lines 1493--1653, passed independent
line-by-line source, formula, hierarchy, reference, page-marker, and TeX
review. The exact next cursor is excluded line 1654 (§I.9).

Material decisions are disclosed in the TeX and ledgers: the shared printed
`A_0` target is corrected to type-correct `B_0`; the shared unlocalized `C`
reading is corrected to `B=C_n`; `défini pas` is read as `défini par`; the
source-comment locator is expanded only to EGA Chapter I; and the source's
`J`/calligraphic-`J` alternation is normalized to one calligraphic ideal with
an explicit note. Current corrected French branches control every `S_n`,
calligraphic `B_n`, tensor denominator, map direction, and hierarchy choice.

Public machine evidence adds 10 comparison rows, 12 formula/structure rows,
14 adverse-choice rows, 10 terminology rows, 10 provenance/coverage rows,
11 graph records, and 16 difficulty/failure/revision records. The cumulative
public surface validates as 23 CSV files / 400 data rows and 10 JSONL files /
145 records, with stable IDs, rectangularity, formula safety, schema/status,
hierarchy, supersession, closure, and target-hash checks. Artifact-tool import,
inspection, error scanning, and rendering passed all 23 CSVs / 400 rows;
receipt 2530 bytes, SHA-256 `F1A6D375F38EF4EF2E53DAC7B177511023449E449680F8649C35697A2571696F`.