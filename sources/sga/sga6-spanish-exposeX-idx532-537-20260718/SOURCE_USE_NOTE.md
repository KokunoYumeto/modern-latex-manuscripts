# Source-use note

Mathematical authority is the six-page scan packet and its current French
rescribe at:

`C:\IL_GitHub\00_main_current\sources\sga\sga6-claude-workpass-source-rescribe-20260704\english_source_checked_restart\working_exposeX_idx532_537_en`

The existing English source-checked tranche in that directory was consulted
only as a comparison control. It is not evidence of Spanish usage. Independent
Spanish TeX bodies under the Romance B3 package were checked for the target
register of `haz`, `noetheriano`, `subesquema cerrado`, `soporte`, and
`longitud`; these row checks do not certify the whole translation.

The source scan reads coherent `\mathcal O_X`-module at current index 536. The
French rescribe's `\mathcal C_X` is a known typo and is not propagated.

The live French-control file is LF-normalized, while the packet's checksum row
describes a CRLF copy. Both have 391 identical logical lines; the difference is
line-ending normalization, not source-content drift. Both hashes are recorded
in `SOURCE_MANIFEST.json`.
