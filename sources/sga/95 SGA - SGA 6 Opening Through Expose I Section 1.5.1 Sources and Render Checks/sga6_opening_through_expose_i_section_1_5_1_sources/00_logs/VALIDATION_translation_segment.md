# Validation - SGA Translation working draft section

The main TeX file was compiled with `pdflatex` in two passes.

The resulting PDF has 51 pages. All 51 pages were rendered to PNG with `pdftoppm` at 100 DPI in two page-range chunks, and representative pages were visually checked: title/table-of-contents page, Expose I opening page, and final continuation-anchor page.

Known status: this is a working English LaTeX draft translation. It is compile-checked and render-checked, but it is not a final line-by-line editorial proof.
