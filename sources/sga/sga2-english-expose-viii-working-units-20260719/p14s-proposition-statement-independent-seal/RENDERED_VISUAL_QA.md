# Rendered visual QA

- Source physical pages 76-77 were independently rendered at 300 dpi and
  inspected at original resolution. Fresh 600-dpi critical crops isolate the
  proposition opening and the diagram/proof boundary.
- The source diagram has nodes `X^bullet`, `CX^bullet`, `Y^bullet`, and
  `CY^bullet`; horizontal arrows `x` and `y`; downward arrows `u` and `Cu`; its
  continuation and closing sentence occur on running page 69.
- Target page 1 was independently rendered at 300 dpi after the final two-pass
  build, with a fresh 600-dpi critical crop, and inspected at original
  resolution.
  The authority box, proposition, formulas, and diagram are unclipped and
  legible. The four nodes match the source; `x` and `y` point right; `u` and
  `Cu` point down; label placement matches the printed square. All 14 reported
  font rows are embedded, subsetted, and Unicode mapped.
- Spatial PDF extraction interleaves the vertical-arrow labels, but the complete
  map data is already present linearly in the proposition prose and displays;
  no mathematical content depends on diagram extraction alone.

Status: source and target independent visual QA pass at bounded-unit seal.
