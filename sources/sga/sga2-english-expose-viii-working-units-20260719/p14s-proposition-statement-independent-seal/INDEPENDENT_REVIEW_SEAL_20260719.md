# Independent review seal - SGA2-VIII-P14S

This bounded internal unit is independently source-reviewed and sealed.

## Exact scope

- Corrected French lines 2573-2581.
- Original printed page 86.
- Physical source-PDF pages 76-77.
- Recomposed running pages 68-69.
- Blank line 2582 excluded; continuation cursor line 2583 at the proof comment.

## Substantive result

Independent comparison against the corrected French TeX and direct PDF
confirms the left-bounded hypothesis, quantifier `q` in `Z`, strict inequality
`p<q`, existence of an injective resolution, and the complete types of `u`,
`x`, `y`, and `Cu`. Both complexes governed by `u` are left-bounded; `x` and
`y` resolve the stated source complexes; `Cu` has source `CX` and target `CY`.

The target preserves uniqueness of `Cu` only up to homotopy, the four square
nodes, the rightward `x` and `y` arrows, the downward `u` and `Cu` arrows, and
commutativity only up to homotopy. No English mathematical correction was
required. The proof-left-to-the-reader sentence and its footnote begin at line
2583 and remain outside the unit.

The comparison candidate's `C_X`, `C_Y`, and `C_u` notation is an unprinted
normalization and remains rejected; its ASCII diagram is comparison-only. The
source-aligned target retains `CX`, `CY`, and `Cu` and an editable AMSCD square.

## Gates

- Fresh two-pass build: pass, zero diagnostics.
- PDF/text/font inspection: pass; 1 A4 page; 14/14 font rows embedded,
  subsetted, and Unicode mapped; zero forbidden text-control bytes.
- Source and target visual QA: pass at 300 dpi full-page and 600 dpi critical
  crops.
- CSV/JSONL validation: pass for stable IDs, rectangularity, formula safety,
  schema, revision links, and reference closure.
- Authority hashes, privacy scan, Artifact Tool imports/renders, and exact
  manifest closure: pass.

This seal is internal and bounded. Cumulative Expose VIII integration, release
review, and publication remain open.
