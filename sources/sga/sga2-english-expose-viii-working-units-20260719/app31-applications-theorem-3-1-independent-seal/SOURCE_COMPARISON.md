# Source, symbol, and formula comparison

Corrected French lines 2874–2886 were checked clause by clause against the
authority TeX and visually against same-edition physical PDF p. 84. The check
preserves the following exact controls:

- visible §3 / “Applications” and Theorem 3.1 numbering;
- the bridge's nonproper-morphism application, while retaining the theorem's
  separate hypothesis that `f` is proper;
- `f: X -> Y`, locally Noetherian `Y`, local embeddability of `X` in a
  regular prescheme, `n in Z`, open `U`, and coherent `O_U`-module `F`;
- both occurrences of the closure of `{x}` in the codimension-one condition,
  followed by upright `prof F_x >= n`;
- `R^p(f o g)_*(F)` for `p<n` and `g` as the canonical immersion;
- Leray abutment `R^*(f o g)_*(F)` and
  `E_2^{p,q}=R^p f_*(R^q g_*(F))`;
- coherent extension `G|_U ~= F` and EGA I, 9.4.3;
- corrected-branch “condition (iv) on p. 74,” where 74 is the recomposed
  running page and the referenced statement is line 2811 / printed p. 93 /
  physical p. 82;
- printed-p. 97 token immediately after `vérifiée` / “satisfied”;
- `R^q g_*(G|_U)` coherent for `q<n`, then EGA III, 3.2.1 applied to `f`
  and the source's `R^q g_*(F)`, concluding that `E_2^{p,q}` is coherent for
  `q<n`;
- no QED before the boundary and exact continuation at line 2888,
  Proposition 3.2.

The displayed target uses ordinary parentheses, not enlarged delimiter
commands, in two formulas. This is a text-extraction repair only: it preserves
the source grouping and closes the two-U+0001 failure documented append-only.

The target's calligraphic `F/G` are retained under manager-closed Option A,
decision `EG-SGA2-FG-NOTATION-ADJUDICATION-20260719-0001`: they are an explicit
established-English normalization from corrected-source upright operators,
never literal source preservation, and the target is unchanged. The controlling
3,093-byte addendum SHA-256 is
`B6870E65A6A36DD6B4A6291CF38A36AC95BF4903541BF65147C0CB60D6E7858D`;
the canonical MD/CSV/JSONL/validation and adjudication-prose hashes are
`C6573262...`, `7C960386...`, `CC896683...`, `C3ED69FA...`, and `8ED4C58A...`,
with the full identities retained in `AUTHORITY_ARTIFACT_HASHES.csv` and the
terminal JSONL revisions. The direct PDF is same-edition evidence, and the
jcreinhold Markdown is comparison-only. This comparison is production
self-review, not an independent seal.

The symbol audit also distinguishes three additional nonliteral target
normalizations:

- source `\ZZ = \mathbf Z` versus target `\mathbb Z`;
- source upright operator `\R`/`\E` versus target ordinary math-italic `R`/`E`;
- source plain italic `F` in the French-2879 conclusion amid surrounding
  upright `\Fa`, versus a uniformly calligraphic target `F` under closed
  Option A.

The F/G target choice is manager-closed Option A; only the Z and R/E target
choices remain separately provisional. Rejected choices are to call any of
them literal source-glyph preservation, silently flatten their provenance,
conflate the closed F/G policy with the provisional Z or R/E policies, or
mutate this bounded target. Formula roles, indices, bounds, and sheaf identity
remain exact.
