# SGA2 Fa/Ga glyph-normalization adverse delta - public projection

Status: append-only cross-unit correction control. No sealed target or payload
is silently mutated by this note.

The controlling 3,494-byte prose file has SHA-256
`C6573262CC02336CB7A215D2C0D91FA7C1F222A361DEBD74D02DDD6CBA7EEA75`.
This public projection replaces its private absolute style-file locator with
`source_control/french_arxiv/sga2-smf.sty` and states the later closed Option A
disposition explicitly. The exact companion CSV, JSONL, and validation remain
unchanged beside this projection.

## Source fact

The corrected arXiv authority style `source_control/french_arxiv/sga2-smf.sty`
is 19,155 bytes with SHA-256
`5895C022FAF0FC1294C7B8ECFE4E74C47329F02FF317B19170C52FA855224884`.
Line 352 declares `\Fa` as upright operator `F`; line 354 declares `\Ga` as
upright operator `G`. The corrected branch therefore does not render those
macros as calligraphic glyphs. The same-edition French PDF is layout evidence,
not independent source corroboration.

## Closed target policy

Manager decision `EG-SGA2-FG-NOTATION-ADJUDICATION-20260719-0001` approves
Option A. Established English may retain calligraphic
`\mathcal F/\mathcal G` as a deliberate target-language normalization from
upright French `F/G`. It must not be described as literal source-glyph
preservation.

Sealed mathematical targets remain unchanged. Inaccurate historical
provenance descriptions are retained and superseded through append-only
controls. The exact machine-readable companion records govern the affected
stable IDs, including NGT1.

## Remaining independent issue

This glyph policy does not adjudicate the separate French line-2865 `F` versus
preceding `F''` ambiguity. NGT1 preserves printed `F`, alerts the source owner,
and makes no silent mathematical emendation.
