# SGA2 Expose VIII fourth step: n greater than one

Scope is corrected French lines 2856--2872 inclusive, original printed pages
95--96, physical source-PDF pages 83--84, and recomposed running pages 75--76.
The physical page break is between lines 2864 and 2865. The corrected TeX's
`pageoriginale` token is separately located inside line 2865 after `il
existe`. Blank line 2873 is excluded; Section 3, `Applications`, begins at
French line 2874 and is the exact continuation cursor.

Authority is the corrected arXiv math/0511279v1 French TeX
`smf_doc-math_4_01.tex`, 586,789 bytes, SHA-256
`C2F899E92A904E312B550C6452A117FF23D30AF984B2254A0961D2DF0DACD042`.
The directly compiled 216-page French reader, SHA-256
`41AD02C57321A8D2200FF32A929BC93ADBC3DE0D59DCD5A284D28D859FB87A90`,
is same-edition page, layout, and symbol evidence, not independent
corroboration. The jcreinhold e7a259f Markdown is comparison-only.

The English target preserves the induction step, the immediate-specialization
condition, the literal dependency on diagram (2.7), EGA III 1.4.15, both
higher-direct-image isomorphisms, the italic local reduction, underlined
`Gamma_Y`, point-versus-ideal typography, Expose III 2.1, the arrow label
`g'`, tilde `N`, both depth bounds, and the proof close. The diagram reference
is literal in the standalone file and therefore produces no unresolved TeX
reference.

Two source issues remain visible rather than silently corrected:

- line 2859 underlines the named prime ideal but prints plain `x` in
  `dim(A/x)=1`; the target retains plain `x` and discloses the oddity in the
  gray authority box and machine ledgers;
- line 2865 proves the associated-prime statement for `F''` and then writes
  `F=tilde M`, `M=F(X)`. This may be an implicit renaming or an `F''` typo.
  The English body preserves source `F`. Silent normalization to `F''` is
  rejected pending source-owner response or an independent cumulative proof
  audit. The durable source-owner alert filename and hash are recorded in the
  source-defect and difficulty ledgers.

One deliberate English normalization is also disclosed: the corrected source
macro renders upright roman `F`, while the P2-SETUP precedent provisionally
uses calligraphic `F` pending manager consolidation. Formula roles, primes, subscripts,
and operators remain source-aligned; the glyph delta is not described as
literal source preservation and is revisitable under an SGA-wide notation
policy change.

The comparison candidate's literal `x follows y`, running-page 76 labeled as
an original page, plain Gamma, `g`-dot arrow, corrupted tilde `N`, and raw
code-block displays were rejected. The bounded unit subsequently passed
independent source/math review and is now independently sealed pending archive
custody. No archive handoff, publication, or public readback is claimed here.
