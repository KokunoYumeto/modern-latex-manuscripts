# Production source audit

Scope: corrected French lines 2820--2826, original printed page 94, physical
source-PDF page 82, and recomposed running page 74. Printed marker 94 occurs
before this unit at French line 2813. Blank line 2827 is excluded; French line
2828 begins the `n=1` case and is the exact substantive continuation cursor.

The target preserves `(iv) => (ii)`, the complete restatement of condition
`(ii)`, the universal quantifier, `x\in U`, `c(x)=1`, upright `prof`, the
corrected calligraphic `F_x`, the weak inequality `\geq n`, induction on `n`,
the `n=0` base case, and the source's assertion that both conditions are
vacuous.

The corrected French theorem allows integer `n`, while this proof explicitly
starts its induction at zero. Negative cases are left implicit; the target
does not add an emendation. The jcreinhold candidate is comparison-only. Its
plain `F`, raw `prof`, `\geqslant`, and literal “empty” wording are rejected in
favor of the corrected French authority and established SGA English register.

Independent review subsequently confirmed every source/formula/terminology/
page/boundary control, rebuilt the target twice in isolation, and reproduced
the extraction and both 300-dpi renders byte-for-byte. No target repair or
French emendation was required. The sealed target identity is recorded in
`REVIEW_FREEZE.md`.
