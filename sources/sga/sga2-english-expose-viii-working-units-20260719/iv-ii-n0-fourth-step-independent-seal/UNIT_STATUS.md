# SGA2 Exposé VIII `(iv) => (ii)`, induction base case

Status: independently sealed bounded unit with no target repair; exact-set/
hash/privacy manifest closure passes and archive-owner handoff is ready.

Scope: corrected French lines 2820--2826, printed page 94, physical source-PDF
page 82, and recomposed running page 74. The preceding printed-page marker is
at line 2813. Blank line 2827 is excluded; French line 2828 begins the `n=1`
case and is the exact cursor.

Authority is the corrected arXiv `math/0511279v1` French TeX. The directly
compiled 216-page French reader is same-edition layout evidence, not an
independent witness. The jcreinhold `e7a259f` Markdown is comparison-only.

Thirty-two substantive CSV rows and 14 JSONL records / 12 stable IDs validate
with zero errors. All five CSVs passed Artifact Tool import,
inspection, ID rendering, and visual review. The one-page PDF builds cleanly;
all 13 font records are embedded, subset, and Unicode-mapped; extraction and
target/source visual checks pass. A fresh independent rebuild reproduced the
extraction and both 300-dpi renders byte-for-byte.

This bounded target is not a complete exposé or volume. Publication and remote
readback are unclaimed. The exact public set is 34 files: 32 descriptor rows
plus `ZENODO_PAYLOAD_MANIFEST.csv` and `UNIT_HASHES.csv`.
