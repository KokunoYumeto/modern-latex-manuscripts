# Artifact Tool visual QA - independent seal

The full-table and primary-ID previews for all four final substantive ledgers
and the final self-excluding manifest were inspected at original resolution.
Headers are visible, rows and columns are populated, stable IDs are distinct,
and no blank or broken import, clipped primary ID, shifted row, accidental
formula evaluation, or missing final column is visible.

Wide full-table previews are intentionally compact; the separate ID previews
provide a clear one-row-per-record identity check. The CSV files themselves
remain the machine source of truth. Preview files and the NDJSON tool receipt
are local-only QA support and are not part of the public manifest.

Status: final Artifact Tool visual pass.
