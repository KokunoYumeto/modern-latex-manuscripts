# Independent review seal - SGA2-VIII-Q15A

Exact scope: corrected French TeX lines 2611--2616; original printed page 88;
physical direct-source PDF pages 77--78; recomposed running pages 69--70.
Line 2617 is blank. The next source cursor is line 2618, where the initial-term
calculation begins.

Independent review compared every sentence, formula, map, hypothesis,
cross-reference, page locator, and boundary against the corrected French TeX
and the direct compiled SMF/arXiv PDF. The PDF is a compilation of the same
French edition and is not represented as an independent original-scan witness.
The current jcreinhold translation is comparison-only.

Two target defects were corrected during review:

1. The draft defined the underlined functor with italic `F`; the French style
   authority defines `cF` as `underline{mathrm F}`. All target occurrences now
   preserve the upright roman source glyph.
2. The standalone manual `(1.5)` tag was replaced by the source-matching
   equation counter initialized to 4. The visible number is unchanged and the
   PDF now has the durable unique destination `equation.5`.

No prose, mathematical object, sign, exponent, arrow, hypothesis, or result
changed. The source's stray `Proposition 1.4.,` punctuation remains recorded;
the target uses a grammatical English comma.

Final gates passed: two independent clean builds and two final unit builds;
one-page PDF; 11/11 embedded/subsetted/Unicode fonts; zero forbidden extracted
controls; 300/600-dpi source and target inspection; CSV rectangularity and
formula safety; JSONL parse/schema/hierarchy/revision/reference closure;
authority hashes; privacy scan; and exact artifact manifest validation.

Seal: independently source-reviewed bounded internal unit. This is not a
cumulative Expose VIII edition, a complete SGA2 translation, a critical
edition, or a standalone Zenodo publication claim. Continuation cursor: French
line 2618.
