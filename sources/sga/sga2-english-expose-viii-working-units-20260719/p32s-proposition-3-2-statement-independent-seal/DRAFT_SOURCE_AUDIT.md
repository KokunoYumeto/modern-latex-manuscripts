# Production source audit — SGA2-VIII-P32S

This bounded production unit covers corrected French lines 2888–2899: the
complete statement of Proposition 3.2 and no proof. Blank 2900 is excluded;
French line 2901 begins the proof and is the exact continuation cursor.

The corrected arXiv TeX is authority. The same-edition 216-page compiled
French reader checks physical p. 84, recomposed running p. 76, original printed
p. 97, emphasis, formula layout, and the visible bar over `S`. It is not
independent corroboration. No `pageoriginale` token occurs inside the unit; the
printed-p. 97 token occurs immediately before it in source line 2886.

The scan-backed reading is “the closure overline S of the support S.” One PDF
text-extraction presentation can lose that bar, but the corrected TeX and
visual page both show it. The unbarred reading is therefore rejected as an
extraction artifact, not treated as a source variant.

The jcreinhold e7a259f Markdown at comparison lines 632–646 is comparison-only.
It agrees broadly on the content but is one LLM lineage; its “an open of X”
calque and fenced plain-text codimension display are not promoted.

Four nonliteral target glyph choices are explicit. Decision
`EG-SGA2-FG-NOTATION-ADJUDICATION-20260719-0001` closes Option A: source
upright `F` becomes the established English calligraphic `F`, without any
claim of literal glyph preservation. Source upright-bold `Z` becomes
blackboard-bold `Z`; source upright operator `R` becomes ordinary math-italic
`R`; and source `X-U` becomes `X setminus U`. Those three remaining deltas are
still disclosed separately. Formula roles, degrees, closure, argument order,
and strict inequalities are unchanged.

This is production self-review only. Independent source/formula, visual,
privacy, and machine-evidence review is still required before any seal.
