# Source, symbol, and formula comparison

Corrected French lines 2888–2899 were checked clause by clause against the
authority TeX and visually against same-edition physical PDF p. 84.

The target preserves:

- visible Proposition 3.2;
- locally Noetherian prescheme, locally embeddable in a regular prescheme;
- open `U`, canonical immersion `i: U -> X`, and `n in Z`;
- coherent Cohen–Macaulay `O_U`-module `F`, including “(on U!)”;
- equivalence of conditions (a) and (b);
- `R^p i_*(F)` coherent for strict `p<n`;
- every irreducible component `S'` of the closure `overline S` of the support
  `S` of `F`;
- `codim(S' intersect (X setminus U), S') > n`, with exact argument order and
  strict inequality;
- closure at line 2894 confirmed visually against the false unbarred
  extraction artifact;
- close at line 2899, exclusion of blank 2900 and proof 2901, and no QED.

The same-edition PDF is visual/layout evidence only. The external English
Markdown is comparison-only. The four target-glyph normalizations are listed
in the formula and terminology ledgers. Calligraphic `F` is closed Option A,
an established English normalization from upright source `F`; the other glyph
deltas remain explicitly disclosed. None is described as literal source-glyph
preservation.
