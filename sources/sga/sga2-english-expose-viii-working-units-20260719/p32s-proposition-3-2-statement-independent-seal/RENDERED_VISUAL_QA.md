# Rendered visual QA

The final target page was rendered at 180 dpi and inspected at original
resolution:

- `target-page-1.png`: 189,054 bytes, SHA-256
  `6ECDB656FFF547C49F528CEDB243A1A00F18939E153AEC80A39A60748B246210`.

The page is complete and unclipped. The authority box, closed Option-A `F`
disclosure, visible Proposition 3.2 label, locally Noetherian and embedding
hypotheses, canonical immersion, `n` clause, Cohen–Macaulay hypothesis,
conditions (a)/(b), strict `p<n`, visibly barred `overline S`, codimension
argument order, `X setminus U`, and strict `>n` are legible. No proof or QED
appears.

The same-edition French page was independently rendered before this unit and
inspected again at original resolution:

- `source-physical-084.png`: 630,497 bytes, SHA-256
  `F032FE89F7927CA35D3767E391DDCE4A9A0DB2103141CFE9D3B0315335672B75`.

It visibly confirms running p. 76, printed p. 97, complete Proposition 3.2,
upright source `F/R/Z` glyph classes, the bar over `S`, both strict bounds, and
the proof beginning below the target boundary. This image is from the same
corrected edition, not an independent source witness. Visual QA does not seal
the translation.
