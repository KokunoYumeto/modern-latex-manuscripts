# Independent review

An independent source, formula, locator, build, render, machine-ledger, and
privacy review passed the frozen `SGA2-VIII-P32S` target without requiring a
target repair.

The reviewed boundary is corrected French lines 2888--2899, printed p. 97,
physical same-edition source-PDF p. 84, and running p. 76. Blank line 2900 is
excluded; French line 2901 begins the proof and is the exact continuation
cursor. The source PDF is generated from the corrected edition and therefore
supplies layout/glyph evidence only, not independent textual corroboration.

The reviewer separately checked:

- the locally Noetherian prescheme and local embeddability hypotheses;
- the open subset, canonical immersion `i:U->X`, and `n` in the integer ring;
- coherent Cohen--Macaulay `O_U`-module status and the emphatic `(on U!)`;
- condition (a), including every index and the strict inequality `p<n`;
- condition (b), including universal quantification, irreducibility,
  `S'`, visual and textual `overline S`, support dependency, codimension
  argument order, and the strict inequality `>n`;
- closure at French line 2899, exclusion of line 2900, proof exclusion at
  line 2901, and absence of a spurious QED mark.

The jcreinhold `e7a259f` Markdown was inspected only as one external LLM
comparison lineage. Its `an open of X` and fenced plain-text formula were not
promoted. Corrected French TeX remains authority.

Closed Option A is represented honestly: calligraphic English `F` is an
established target normalization from upright source `F`, not literal glyph
preservation. Upright-bold source `Z` to blackboard-bold target `Z`, upright
source `R` to italic target `R`, and source `X-U` to target `X setminus U`
remain three separate disclosed deltas. The independent pass found no
unledgered normalization.

Frozen target identities:

- TeX: 2,215 bytes, SHA-256
  `0CA96E488EEA989BD02230DC1047705A0A85F3F6DFE826EE0F0F2F94F6DFFFE3`;
- PDF: 252,594 bytes, one A4 page, SHA-256
  `2F2C1049289DC9400DEAAA7F29B9D2AF2A9376859B5D6F269A17B66A3C4C8E85`;
- layout extraction: 1,662 bytes, one formfeed and zero forbidden controls,
  SHA-256
  `827C69AD6B761954F05A9FF90B48BE7AF0FBF8B535DAECD812C66D28247A5ACC`;
- target render: 189,054 bytes, SHA-256
  `6ECDB656FFF547C49F528CEDB243A1A00F18939E153AEC80A39A60748B246210`.

The independent isolated build reproduced identical extracted layout and an
identical 150-dpi render; all 17 PDF fonts passed embedded/subset/Unicode
checks. The five substantive CSV ledgers contain 58 rectangular,
formula-safe rows. The two append-only JSONL ledgers contain 32 records across
15 stable IDs, with parse/schema/hierarchy/reference/revision closure and
15/15 sealed terminal records. Artifact Tool receipts and rendered previews
were re-created after the seal rows. Public-text and raw-PDF privacy scans
found zero private-path hits.

This seals only the bounded statement unit. It does not claim a complete
Expose VIII, complete SGA2 volume, critical edition, archive custody,
publication, DOI update, or public readback.
