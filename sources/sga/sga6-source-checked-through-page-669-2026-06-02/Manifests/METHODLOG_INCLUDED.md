# Methodlog included

A cumulative `Methodology/` directory is included in this package. It records special-character conventions, transcription/rendering failure modes, and reusable Python scripts for extracting source slices, rendering PDF checks, compiling LaTeX, and generating SHA256 manifests.
