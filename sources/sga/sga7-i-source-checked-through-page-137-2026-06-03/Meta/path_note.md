# Path-name policy

This package uses short internal folders (`En`, `Fr`, `Src`, `PDFs`, `Logs`, `Meta`, `Meth`) and short file names (`sga7i_121_137_en.pdf`, etc.) to reduce Windows path-length problems while preserving human readability.
