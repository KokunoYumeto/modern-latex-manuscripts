# Before/after snippets

These are concise diagnostic excerpts. The production TeX remains the authority.

## Source-PDF 277 / printed 270

Before: `assume E strictly perfect and zero outside the relevant degrees`; unnamed “stalk functor”; Corollary 5.6 had `E\in\Ob D(X)`.

After: `E^i=0 for i<p (respectively for i<p-1)`; the functor `M -> H^p(E_x tensor^L M)` and the two cycle/stalk identifications are explicit; Corollary 5.6 has `E\in\Ob D^{-}(X)`.

## Source-PDF 286 / printed 279

Before: the sentence ended after `the ambient abelian category being Mod(A_X)`.

After: the source footnote follows that sentence: the index `A` may be omitted when unambiguous; “naive” may be dropped only when explicitly signalled.

## Source-PDF 347 / printed 340

Before: formulas (5.4.1) and (5.4.2) appeared as conclusions with a terse “corresponding finite-rank hypotheses.”

After: both universal-ring computations are shown, including multiplication by `lambda_{-1}(N)`, the `i+j=p` expansion, finite-rank bounds for `N`, `N'`, `N+N'`, and cancellation in an integral domain before substitution.

## Source-PDF 350 / printed 343

Before: `with \lambda^k(N'_0)=0 for k>d`.

After: `subject to \gamma^k(N'_0)=0 for k>d`; the weights, citations (4.12), (4.15), (5.5.4), 4.7, augmentations, and filtration compatibility are explicit.

## Source-PDF 377 / printed 370

Before: Corollary 1.4 had no proof; the next result was called Proposition 1.5; there was no Lemma 1.5.1; later results were numbered Propositions 1.7 and 1.8; generation and relation were compressed into the wrong section sequence.

After: Corollary 1.4 has its proof; Lemma 1.5.1 and its Bourbaki argument are present; the next result is Lemma 1.5.2; the later results are Propositions 1.6 and 1.7; generation is §1.8 and the relation is §1.9; downstream references use the corrected labels.

## Source-PDF 431 / printed 424

Before/after: no change. Repair105's expanded Proposition 1.8 is present byte-for-byte under normalized LF extraction, with SHA-256 `6641F4AF065BF59773756A251002BCC115DAD0243E4E23CE26D882F129564F7F`.

