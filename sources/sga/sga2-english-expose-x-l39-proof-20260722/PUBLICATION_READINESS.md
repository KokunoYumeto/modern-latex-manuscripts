# Publication readiness

Status: `GITHUB_CUSTODY_READY_ZENODO_CUMULATIVE_DEFERRED`.

All 24 enumerated producer/review inputs match the handoff hashes. Producer
and independent source, formula, two-component diagram, terminology,
rejected-choice, build, extraction, font, render, rights, machine, reference,
and privacy gates pass. The target is one editable TeX file and one
unencrypted A4 PDF page; all 16 font rows are embedded, subset, and
Unicode-mapped. Producer and independent PDFs are byte-identical.

The append-only handoff correction is bound by SHA-256
`17BC1A7AE8A9D68A7D8301545D637E1EA9DB637B7F9129DF160A209D4A7ED498`.
It corrects only the producer privacy-scan count to 55 durable text files;
the independent count is 43, and both scans report zero private-path hits.

The source body, comparison body, source PDFs, source rasters, transient build
files, scripts, and raw logs are excluded. Target/source render pixels were
reviewed but are omitted because the handoff's exact public selection does not
enumerate them; the public PDF is the durable target surface.

This package is ready for bounded public GitHub custody. A standalone Zenodo
mutation is deferred to authenticated cumulative curation under existing
concept DOI `10.5281/zenodo.20410947`; no duplicate deposition is authorized.
