# D031 maintained public source

This directory contains the maintained 43-page French and English readers and self-contained native TeX/Markdown under `normalized/`, together with the12-page apparatus. Compile each TeX file with pdfLaTeX twice from `normalized/`. The cumulative entrypoints at the source-tree root include these normalized PDFs. Exact original gate-normalized files are preserved under `gate_normalized_witness/`.

A fresh enhanced visual audit found one label/arrow collision on physical33 printed279. The maintained derivative adds only `{pos=0.75}` to the right vertical-arrow label in the six-node diagram. No mathematical text, symbols, arrows or graph incidence changed. Every page has exact PyMuPDF raw extracted text and independently exact whitespace-normalized pypdf text; pypdf changes one inter-label whitespace on page33. The other42 pages have exact raster/text identity. `D031_PRESENTATION_VALIDATION_RECEIPT.json` binds original and current native/cumulative identities, the literal TeX diff and fresh independent visual validation. The earlier archive/gate derivative graph describes the initial literal-only publication transformation; where a normalized reader differs subsequently, its original bytes remain at `gate_normalized_witness/` and this separate presentation receipt governs the current derivative.

`README.md`, `prepare_input.py`, historical manifests and historical receipts describe the original private gate workflow, not the directory layout of this maintained public derivative. Do not interpret their original-byte hash assertions as assertions about substituted public derivatives. The authoritative archive correspondence is `D031_ARCHIVE_DERIVATIVE_RECEIPT.json`: it records separate original and initial-public identities for every included gate file and recursively traversed archive member. Only matching literal local-account substrings in ordinary text were substituted with `[LOCAL_ACCOUNT]`. The separate reader label-position derivative is documented above; unaffected archive member bytes remain unchanged.

The original six-member outer archive is represented by one deterministic public-safe derivative under `source_archive/`. Its original unredacted filename, size and SHA-256 remain explicitly bound in the derivative receipt; the original itself remains private and untouched. The large inner full-state ZIP and its ZERO_ACCEPTED prior-work ZIP are retained inside that outer derivative, not duplicated as separate filesystem copies. The extracted `input_state/` supplies the ordinary source records, tools, controlling authority and comparison-only witness. Its large seed30 ZIP is intentionally accessed through the nested archive rather than duplicated locally. No unique evidentiary object has been dropped.

The old input capture tools with substituted historical local paths are evidence, not portable build entrypoints. Self-contained normalized native TeX is the maintained reader rebuild route. All215 inherited salvage rows retain ZERO_ACCEPTED status. The controlling scan spans43 physical pages, printed247-289; printed290 is absent and is not invented. Comparison and raw inherited editions have no controlling authority over the accepted normalized readers.
