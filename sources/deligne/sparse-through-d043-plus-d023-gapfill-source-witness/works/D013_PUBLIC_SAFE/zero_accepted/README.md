# D013 ZERO_ACCEPTED evidence

These files are exact members of the original complete prompt bundle:

- `15_S00.json` is the explicit initial state with zero accepted pages.
- `16_GOVERNING_INSTRUCTIONS.md` identifies the sole authority, warns that inherited material is untrusted, and requires pagewise replay.
- `17_S00_CUMULATIVE_FULL_STATE.zip` is the exact initial cumulative state container.
- `92_BUILD_RECEIPT.json` records the initial zero-accepted templates and their hashes.

The historical return envelope was not copied because it contains a local-account path. Its omission changes no source or acceptance evidence.
