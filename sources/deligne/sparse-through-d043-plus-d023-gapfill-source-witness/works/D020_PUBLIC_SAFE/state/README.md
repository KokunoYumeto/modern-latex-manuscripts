# Deligne — La conjecture de Weil. I

STATUS: IN_PROGRESS. V6 is a bounded-copy hygiene repair derived only from the frozen V5 audit subject. V5 closed the sealed V4 dynamic-audit finding A11 and passed every substantive scholarly-content check, but failed candidate-surface hygiene/current-description checks H01–H02. V6 is not accepted until a new independent, memory-bounded, nonpatching audit returns PASS.

Authority: `source/20_AUTHORITY_DELIGNE_D020_WEIL_I_NUMDAM_36PP.pdf`, 36 physical pages; SHA-256 `8392B345D4854E6DC55FB42CFC0B616D941935983723627237239A87348F42E5`. Physical page 1 is a vector repository cover and is excluded from both scholarly bodies. Physical pages 2–36 are 35 article pages, printed 273–307. The article ends at the manuscript-received date, 20 September 1973; no author-address block follows.

The three required offline HTML readers are `readers/source_language.html`, `readers/english_standalone.html`, and `readers/apparatus.html`. They use native MathML. The corresponding independent editable mathematical TeX is in `tex/`. Each scholarly layer contains exactly one `P0019-A01` lacets figure after paragraph `(5.2)`; apparatus contains none. The PDFs under `readers/pdf/` are the verified promoted V5 outputs: source-language 35 pages, 268,838 bytes, SHA-256 `F7A98D3A5CA65C3039D2E897FB536B238631FFCE8F91F535C760839C8B95A267`; English 35 pages, 263,602 bytes, SHA-256 `E14213313974780608285C04BDDD5F1A89108CCBE5533186BA1B186C89194741`; apparatus 36 pages, 130,165 bytes, SHA-256 `B07A34FABF9709AD62EBCD4A54EF1F0C05DC34597F616B13F4D918D7AA1FC694`. V6 changes no TeX, asset, HTML, or PDF input, so no TeX rebuild is required. The article TeX resolves its single conservative figure from `../assets/presentation_derivatives/` or the TeX file's own folder for a flat delivery.

Current source-first receipt: `audit/FINAL_SOURCE_FREEZE_V7.tsv`; each French page record is frozen before its translated derivative. Historical freezes and adverse candidates are retained but are not acceptance evidence. All 272 inherited prior-work members remain ZERO_ACCEPTED. Comparator PDFs are non-authoritative.

Native builders: `tools/build_math_readers.py` (TeX and span ledger), `tools/build_readers.py` (offline MathML), and `tools/validate_state.py` (deterministic structural checks). V5 made asset emission a post-paragraph `finally` path, so it cannot be bypassed by the numbered-prose, formula, heading, bibliography, or other paragraph branches. `tools/test_asset_insertion_v5.py` exercises the actual page-19 French and English records and confirms that the apparatus does not receive the scholarly image. `tools/vendor/latex2mathml` is the retained vendored 3.81.0 Python package, with its license metadata retained; the two unused generated Windows executable wrappers are deliberately excluded from the V6 candidate-public surface. The MathML builder imports the Python package directly and uses scoped upright font commands to retain explicit normal mathematical letters.

Memory-bounded audit primitives are `tools/bounded_streaming_audit.py` and `tools/run_low_memory_guard.py`. File identity, exact equality, copy verification, and term searches use fixed 65,536-byte streaming blocks (hard maximum 1,048,576 bytes), never whole-large-file or edit-distance comparison. Harmless tests run in a fail-closed Windows Job created suspended and assigned before resume, with no breakaway, kill-on-close, and an exact aggregate 268,435,456-byte memory limit.

The historical one-shot PDF launcher is `tools/rebuild_v5_pdfs_guarded.py`. Its completed fixed receipt is `audit/V5_TEX_REBUILD_RECEIPT.json`: the named TeX mutex was held over all six XeLaTeX trees, each tree had the exact aggregate 2,147,483,648-byte Job limit, all exited zero and accounted through zero active descendants, and the three PDFs were promoted as one verified set. The existing production receipt makes the launcher fail closed against retry or overwrite. V6 does not invoke it.

The raw p19 lossless authority crop and separately named presentation derivative are retained with exact geometry and operations in `edition/asset_ledger.tsv`. Only the presentation derivative appears once in each article body. All cover annotations, running heads, folios and the terminal rule are separately disposed in provenance/apparatus rather than silently promoted into article text.

This task does not modify a source master, shared queue, Git destination, or public record. The final outer packet and cumulative checkpoint may be marked COMPLETE only after the final clean audit and deterministic byte/inventory checks.
