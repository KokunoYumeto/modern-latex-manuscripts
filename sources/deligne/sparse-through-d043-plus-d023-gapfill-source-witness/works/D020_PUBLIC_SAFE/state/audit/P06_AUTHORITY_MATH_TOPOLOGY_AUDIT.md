# P06 authority-only mathematical and topology audit

Scope: authority physical pages 31-36, printed pages 302-307, checked against the 200 dpi authority renders and targeted magnified crops. The audit did not consult comparator or prior-work text as authority.

The independent residual pass checked theorem and equation labels, local-system notation, arrow direction and type, the diagrams on printed pages 304-305, the bibliography terminus, the SGA block, and the received date. It found one remaining v3 omission on printed page 306: the Kunneth formula had lost the superscript stars on both cohomology groups. The current v4 restores `H^*(A,ℱ_j) = ⊗ H^*(A¹,ℱ_j^1)`.

Confirmed details include `ε(a)^{-1}` and `Miyake` on printed page 302; the local-system paragraph on 303; hook arrows and `j_!σ*` on 304; `R^*(fu)_! = ⊕_j R^*a_!ℱ_{j,S}` on 305; and `u_!ℱ_j = u_*ℱ_j` on 306. Running heads and folios remain excluded from article text; printer signature `39` and the terminal horizontal rule remain provenance-only objects.

Post-v4 verdict: PASS.
