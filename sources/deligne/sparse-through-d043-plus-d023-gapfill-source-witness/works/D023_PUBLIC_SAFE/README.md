# D023 public-safe exact work

This tree contains the controlling 30-page authority, semantic source-language
English and standalone French editions, restrained apparatus, editable Markdown/TeX,
math-typeset PDFs, the comparison-only collected-volume split, ZERO_ACCEPTED prior-work
ledger, and fresh independent paper-complete audits. The comparator never overrides the
controlling authority. Raw session packets remain locally preserved and are bound by
immutable identities rather than nested into the maintained public source tree.
