# D027 publication-safe source and audit packet

This subtree contains the canonical source-language English edition, faithful
French edition, restrained apparatus, editable TeX, the complete controlling
authority, exact authority-addressed records, and the independent audit needed
to reproduce and review D027.

The 60-page JSTOR file in `witnesses/authority/` is controlling.  Its physical
page 1 is copy matter retained only for provenance; physical pages 2-60 map to
printed article pages 103-161 and to the 59 scholarly pages in each edition.

The collected-volume split in `witnesses/comparator/` is comparison-only.  It
does not override the controlling authority and was not used as canonical
reader input.  Earlier returned HTML and inherited PDFs remain ZERO_ACCEPTED
evidence in the consolidated provenance archive, not canonical source.

No image fallback is promoted for this work: the exact asset ledger is empty
because all mathematical displays and structural diagrams are represented
semantically in LaTeX.  The repair log records parser-boundary repairs applied
only at the render layer; frozen NDJSON records are unchanged.
