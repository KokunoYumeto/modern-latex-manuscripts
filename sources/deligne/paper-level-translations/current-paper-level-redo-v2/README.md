# Deligne complete redo - upload-ready bilingual package

This package contains the current rebuilt bilingual TeX/PDF deliverables for the previously touched Deligne papers.

Layout:

```text
papers/<paper>/<EN|FR>/*.tex
papers/<paper>/<EN|FR>/*.pdf
combined/
reports/manifest.csv
```

No source-slice PDFs, scan images, render images, aux/log files, or OCR-only source text are included in this upload package.
