# Path-name convention

From this package onward, root folders and file names use short human-readable identifiers such as `D010_HD2_041_050_EN.pdf` to reduce Windows path-length failures while keeping paper number, title abbreviation, page range, language, and role visible.
