# D080 descending cumulative package

Standardized format: pdflatex + lmodern, standard LaTeX math, compact readable layout, short paths.

Clean deliverables are under `New/` and `Cum/`; QA/methodology files remain outside clean PDFs.

Visual artifact policy: audit includes diagrams, illustrations, tables, matrices, and large display blocks. Diagrams are rebuilt in TeX rather than pasted from source scans.
