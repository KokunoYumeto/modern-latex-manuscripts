# OLP-0198 accepted reader payloads

The cumulative PDF is the human-readable accepted-prefix checkpoint through OLP-0198. It deliberately stops before OLP-0199. Each `*.source.tex` / `*.isv.tex` pair below preserves exact upstream and installed target bytes; TeX backslashes are not rewritten by the privacy projection.

The PDF is a partial working reader, not a claim that all 722 units are translated. See the top-level Interslavic-first README and the machine handoff for the exact frontier, inherited partial-reader reference markers, licence, canon and validation boundaries.
