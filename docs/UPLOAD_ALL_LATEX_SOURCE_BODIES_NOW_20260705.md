# UPLOAD ALL LATEX SOURCE BODIES NOW

Date: 2026-07-05

> **Historical directive:** this page preserves the 2026-07-05 source-body
> intake policy. It is not an active machine-to-machine instruction. Current
> contributors should use the repository issue/PR routes and bind each bounded
> source checkpoint by exact path, bytes, hash, provenance, and state.

The durable requirement is to preserve every real source body admitted for an
interlanguage, Noether, source-canon, or language-baseline checkpoint.

Required upload scope:

- `.tex`, `.ltx`, `.sty`, `.cls`, `.bib`, `.bbl`, `.dtx`, `.ins`, MetaPost, diagrams, and any other TeX-adjacent source files.
- PDFs, ZIPs, TARs, downloaded source archives, and repository snapshots when those are the real provenance carrier.
- Source-canon language baselines, not only generated Noether drafts.
- All active language families and lanes: Slavic, Interslavic, Ukrainian, Russian, Polish, Czech, Slovak, South Slavic, Belarusian, Macedonian, Bulgarian, Slovenian, Croatian/Serbian/Bosnian, Romance, CJK, Arabic/RTL, Persianate/Farsi/Dari/Tajik, Turkic, African/Horn/West, Malay/SEA/Pacific, Indigenous/Creole/Sign, and any other lane touched.

Do not submit only manifests, checksum ledgers, access-boundary notes, omission
ledgers, or run logs when source bodies exist. Those are useful, but they are not
substitutes for inspectable source files.

If a source file cannot be preserved, record the exact reason, upstream URL or
repository identity, filename, size, checksum if known, and next recovery action.

Historical finding at that source-intake generation:

- `origin/codex/noether-pc-20260629` contains many older TeX payloads, including `noether-slavic-source-canon/20260704/.../latex_payload/...` and some `source_canon_repo_text_probe/...` material.
- Latest packages 620-626 added no `.tex`, `.pdf`, `.zip`, `.bib`, or other source-body payloads.
- The next bounded intake therefore needed the actual source corpora, not only coordination artifacts.

The public repository needs the real admitted source bodies in inspectable form
so later contributors can reuse them without rediscovery.

## Critical clarification

Do not provide an index instead of the corpus. Do not provide a summary instead of the corpus. Do not provide only source-canon tables, hashes, manifests, or URL lists.

Preserve a coherent bounded corpus as exact bytes, with its original relative
structure and a manifest. Large checkpoints may use multiple containers, but a
description of unpreserved source bytes is not a substitute for custody.
