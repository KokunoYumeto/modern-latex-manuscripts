# Noether CJK Draft Corpus Translation Slices: Continuation 28

Generated UTC: `2026-07-04T09:54:21.057313+00:00`

Status: `draft/non-canonical/not native reviewed/not approved/not gate-promoted`.

Draft/non-canonical Japanese and Simplified Chinese corpus slices. Not native reviewed. Not approved. No gate promotion.

Korean remains source-discovery/crosswalk only in this continuation.

## Metadata

- Baseline SHA256: `C0ACCB2D4EB98F54B41BC3977DFA0CB57A349C74B7B35E06453343D15ACAB4ED`
- Coverage: Continuation 28 covers German baseline 22846-23140, completing Deuring lecture Chapter V §25 before §26 begins at 23141.
- Slice count: `7`

## Slices

### cjk-continuation-28-001-galois-maximal-subfield-and-inner-automorphisms

- German anchor: `22846-22858`
- Source summary: Begins §25. The maximal commutative subfield is Galois; its automorphisms are inner automorphisms of R_r generated by elements u_i, unique up to nonzero factors from the subfield. The target theorem is R_r as a sum of u_i times the subfield.
- Japanese title: Galois 的極大可換部分体と内的自己同型
- Simplified Chinese title: Galois 极大交换子域与内自同构

**Japanese Draft**

§25 は、\(\Re_r\) が Galois 的な極大可換部分体を持つ場合の正規表示を扱う。この部分体の \(n\) 個の自己同型 \(\mathsf H_i\) は、前の定理により \(\Re_r\) の内的自己同型として実現される。

すなわち各 \(\mathsf H_i\) について、適当な元 \(u_i\) が存在し、すべての \(z\) に対して \(\mathsf H_i z=u_i^{-1}zu_i\) となる。元 \(u_i\) は、極大可換部分体の非零因子を掛ける自由度を除いて定まる。

この節で示す主張は、\(\Re_r=u_1\Im+\cdots+u_n\Im\) という形の分解である。従って本質的には、\(u_i\) たちが \(\Im\) 上一次独立であることを示せばよい。

**Simplified Chinese Draft**

§25 讨论在 \(\Re_r\) 有 Galois 极大交换子域时的正规表示。该子域的 \(n\) 个自同构 \(\mathsf H_i\)，依前面的定理，都可由 \(\Re_r\) 的内自同构实现。

也就是说，对每个 \(\mathsf H_i\)，存在元素 \(u_i\)，使得对所有 \(z\) 都有 \(\mathsf H_i z=u_i^{-1}zu_i\)。元素 \(u_i\) 除了可乘以该子域中的非零因子外是确定的。

本节要证明的命题是 \(\Re_r=u_1\Im+\cdots+u_n\Im\)。因此关键在于证明这些 \(u_i\) 关于 \(\Im\) 线性无关。

**Script/TeX Notes**

- Preserve \(\mathsf H_i z=u_i^{-1}zu_i\) and \(\Re_r=u_1\Im+\cdots+u_n\Im\).
- The baseline line uses OCR/OCR-normalization oddities for the maximal commutative subfield symbol; draft keeps the structural role rather than forcing a canonical glyph.
- Normaldarstellung is drafted as 正規表示 / 正规表示 and requires native/domain review.

**Unresolved Flags**

- Lines 22848-22853 contain OCR substitutions for the field symbol, including `3`/`\beta`-like forms; source-image review needed.
- The proof reference to Satz 7, Seite 25 is retained as source context but not independently verified here.

### cjk-continuation-28-002-idempotent-decomposition-and-automorphism-indexing

- German anchor: `22859-22883`
- Source summary: Sets up a direct construction of u_i. Decomposes the scalar extension with idempotents e_i, chooses pseudomatrix units p_ik, identifies automorphisms through Theta_i and S_i, and reindexes p_ik by the associated automorphisms.
- Japanese title: 冪等元分解と自己同型による添字付け
- Simplified Chinese title: 幂等元分解与自同构指标化

**Japanese Draft**

証明は \(u_i\) を直接与えることで進む。まず \(\mathfrak Z_{\mathsf Z}=e_1\mathsf Z+\cdots+e_n\mathsf Z\) と分解し、\(\mathfrak R_{\mathsf r\mathsf Z}\) を左イデアル \(\mathfrak l_i=\mathfrak R_{\mathsf r\mathsf Z}e_i\) の和として書く。

この分解に対応する擬行列単位 \(p_{ik}\) を取り、表現加群 \(e_i\mathbb Z\) が与える同型を \(\Theta_i\) と表す。そこから \(S_i=\Theta_i\Theta_1^{-1}\) が、体 \(Z\) 上の \(n\) 個の自己同型を与える。

以後、\(\Theta_i\) のかわりに \(\Theta_{S_i}\) と書き、擬行列単位の添字も対応する自己同型で記す。すなわち \(p_{ik}=p_{S_iS_k}\) と置く。さらに \(\mathsf H_S=\varTheta_E^{-1}\varTheta_{S^{-1}}\) と番号付けする。

**Simplified Chinese Draft**

证明通过直接给出 \(u_i\) 来进行。先写成 \(\mathfrak Z_{\mathsf Z}=e_1\mathsf Z+\cdots+e_n\mathsf Z\)，并把 \(\mathfrak R_{\mathsf r\mathsf Z}\) 分解为左理想 \(\mathfrak l_i=\mathfrak R_{\mathsf r\mathsf Z}e_i\) 的和。

取与该分解相应的伪矩阵单位 \(p_{ik}\)，并把表示模 \(e_i\mathbb Z\) 给出的同构记为 \(\Theta_i\)。于是 \(S_i=\Theta_i\Theta_1^{-1}\) 给出 \(Z\) 上的 \(n\) 个自同构。

随后用 \(\Theta_{S_i}\) 代替 \(\Theta_i\) 的记法，并把伪矩阵单位的指标也换成相应的自同构，即 \(p_{ik}=p_{S_iS_k}\)。再取编号 \(\mathsf H_S=\varTheta_E^{-1}\varTheta_{S^{-1}}\)。

**Script/TeX Notes**

- Preserve \(\mathfrak Z_{\mathsf Z}=e_1\mathsf Z+\cdots+e_n\mathsf Z\), \(p_{ik}=p_{S_iS_k}\), and \(\mathsf H_S=\varTheta_E^{-1}\varTheta_{S^{-1}}\).
- The baseline alternates Z/mathbb Z/mathsf Z forms; draft keeps TeX source tokens and gives prose-level meaning.
- Pseudomatrizeneinheiten remains 擬行列単位 / 伪矩阵单位.

**Unresolved Flags**

- Lines 22861-22882 need source-image review for exact Z, fraktur, and Theta glyphs.
- The note at line 22883 points to a later simpler proof; not translated into a canonical footnote here.

### cjk-continuation-28-003-construction-and-properties-of-u-t

- German anchor: `22885-22918`
- Source summary: Defines u_T as a sum over indexed pseudomatrix units. Verifies that u_T has an inverse, is an R_r element because it is invariant, and generates the automorphism H_T through z u_T = u_T H_T(z).
- Japanese title: 元 \(u_T\) の構成と基本性質
- Simplified Chinese title: 元素 \(u_T\) 的构造及基本性质

**Japanese Draft**

この記法のもとで、本文は \(u_T=\sum_S p_{S,ST}=\sum_S S(p_{E,T})\) と定める。この元が必要な性質を持つことを順に確認する。

第一に、\(u_T^{-1}\) は明示的な和として与えられ、\(\bar\gamma\) と \(\alpha\) を含む係数により逆元が存在する。第二に、\(R(u_T)=\sum_S RS(p_{E,T})=u_T\) なので、\(u_T\) は不変であり \(\Re_r\) の元である。

第三に、\(u_T\) は自己同型 \(\mathsf H_T\) を生成する。すなわち \(z\in\mathfrak Z\) に対し、\(u_T^{-1}zu_T=\mathsf H_T(z)\)、同値に \(z u_T=u_T\mathsf H_T(z)\) が成り立つ。

**Simplified Chinese Draft**

在上述记法下，文本定义 \(u_T=\sum_S p_{S,ST}=\sum_S S(p_{E,T})\)。接着逐项验证该元素具有所需性质。

第一，\(u_T^{-1}\) 由一个显式求和给出，其系数含有 \(\bar\gamma\) 与 \(\alpha\)，从而逆元存在。第二，由于 \(R(u_T)=\sum_S RS(p_{E,T})=u_T\)，\(u_T\) 是不变的，因此是 \(\Re_r\) 的元素。

第三，\(u_T\) 生成自同构 \(\mathsf H_T\)。也就是说，对 \(z\in\mathfrak Z\)，有 \(u_T^{-1}zu_T=\mathsf H_T(z)\)，等价地 \(z u_T=u_T\mathsf H_T(z)\)。

**Script/TeX Notes**

- Preserve \(u_T=\sum_S p_{S,ST}\), the inverse formula line `22898`, and \(z u_T=u_T\mathsf H_T(z)\).
- The inverse formula is symbol-dense and likely OCR-sensitive; leave the exact fraction in source anchors rather than normalizing in prose.
- Invarianz is translated as 不変 / 不变 in the Galois-action sense.

**Unresolved Flags**

- Line 22898 inverse formula requires source-image verification for alpha/gamma indices.
- Line 22917 uses the same OCR-unstable field symbol as the §25 opening.

### cjk-continuation-28-004-verification-of-commutation-relation-and-linear-independence

- German anchor: `22920-22963`
- Source summary: Checks z u_T = u_T H_T(z) through Theta-index calculations, then proves the u_T are linearly independent over the maximal commutative subfield by expanding a relation sum z_T u_T = 0 and forcing every coefficient z_T to vanish.
- Japanese title: 交換関係の確認と一次独立性
- Simplified Chinese title: 交换关系的验证与线性无关性

**Japanese Draft**

交換関係の確認では、\(z=\sum_S\varTheta_S(z)e_S\) と \(u_T=\sum_S S(e_Ep_{E,T})\) を用いる。これにより \(z u_T\) は \(\sum_S\Theta_S(z)S(p_{E,T})\) の形に整理される。

一方、\(\Theta_S\mathsf H_T=\Theta_{ST^{-1}}\) という関係を用いると、\(u_T\mathsf H_T(z)\) も同じ和になる。従って \(z u_T=u_T\mathsf H_T(z)\) が得られる。

最後に、\(\sum_T z_Tu_T=0\) と仮定する。展開して係数を比較すると、すべての \(S,T\) について \(\Theta_S(z_T)=0\) が従うので、各 \(z_T=0\) である。これで \(u_T\) たちの一次独立性が示される。

**Simplified Chinese Draft**

验证交换关系时，使用 \(z=\sum_S\varTheta_S(z)e_S\) 与 \(u_T=\sum_S S(e_Ep_{E,T})\)。这样 \(z u_T\) 可整理成 \(\sum_S\Theta_S(z)S(p_{E,T})\)。

另一方面，由关系 \(\Theta_S\mathsf H_T=\Theta_{ST^{-1}}\)，可知 \(u_T\mathsf H_T(z)\) 也化为同一求和。因此得到 \(z u_T=u_T\mathsf H_T(z)\)。

最后假设 \(\sum_T z_Tu_T=0\)。展开并比较系数后，对所有 \(S,T\) 都有 \(\Theta_S(z_T)=0\)，于是每个 \(z_T=0\)。这证明了这些 \(u_T\) 的线性无关性。

**Script/TeX Notes**

- Preserve \(\Theta_S\mathsf H_T=\Theta_{ST^{-1}}\), \(z u_T=u_T\mathsf H_T(z)\), and \(\sum_T z_Tu_T=0\).
- The proof is an index calculation; exact displayed sums should remain TeX and be PDF-checked before canonicalization.
- Linear independence is 一次独立性 / 线性无关性.

**Unresolved Flags**

- Lines 22922-22942 are formula-heavy and need source-image review for S, T, and inverse indices.
- The draft compresses the coefficient-comparison step; domain review needed before promotion.

### cjk-continuation-28-005-small-factor-systems-and-associated-classes

- German anchor: `22965-23012`
- Source summary: Introduces symbolic power notation z^S. Derives relations u_R u_T = u_RT a_R,T, calls a_R,T a small factor system, relates small and large factor systems, and describes how replacing u_S by u_S r_S gives associated small factor systems and a group isomorphism between associated classes.
- Japanese title: 小因子系と同伴類
- Simplified Chinese title: 小因子系与同伴类

**Japanese Draft**

ここから \(\mathsf H_S(z)\) を \(z^S\) と書き、本文はこれを「象徴的冪」と呼ぶ。元 \(u_S\) には \(u_Ru_T=u_{RT}a_{R,T}\) という形の関係が成り立ち、\(a_{R,T}\) は部分体の非零元である。

擬行列単位の積を計算すると、\(\Theta_{SRT}(a_{R,T})=\alpha_{S,SRT}^{(SR)}\) が得られ、さらに \(a_{R,T}\) は大きな因子系 \(\alpha_{BC}^{(A)}\) と互いに決定し合う。この \(a_{R,T}\) を小因子系と呼ぶ。

二つの大きな因子系の積に対応する小因子系は、対応する小因子系の積 \(f_{R,T}=a_{R,T}b_{R,T}\) である。さらに \(u_S\) を \(u_Sr_S\) に置き換えると、\(a'_{R,T}=a_{R,T}(r_R^Tr_T/r_{RT})\) となり、同伴な小因子系が同伴な大因子系に対応する。これにより同伴類の対応は群同型になる。

**Simplified Chinese Draft**

从这里起把 \(\mathsf H_S(z)\) 记作 \(z^S\)，文本称为“符号幂”。元素 \(u_S\) 满足形如 \(u_Ru_T=u_{RT}a_{R,T}\) 的关系，其中 \(a_{R,T}\) 是子域中的非零元。

计算伪矩阵单位的乘积可得 \(\Theta_{SRT}(a_{R,T})=\alpha_{S,SRT}^{(SR)}\)，并且 \(a_{R,T}\) 与大的因子系 \(\alpha_{BC}^{(A)}\) 相互决定。这个 \(a_{R,T}\) 称为小因子系。

两个大因子系的乘积所对应的小因子系，是对应小因子系的乘积 \(f_{R,T}=a_{R,T}b_{R,T}\)。若把 \(u_S\) 换成 \(u_Sr_S\)，则 \(a'_{R,T}=a_{R,T}(r_R^Tr_T/r_{RT})\)，同伴小因子系对应同伴大因子系。因此同伴类之间的对应是群同构。

**Script/TeX Notes**

- Preserve \(z^S\), \(u_Ru_T=u_{RT}a_{R,T}\), \(f_{R,T}=a_{R,T}b_{R,T}\), and \(a'_{R,T}=a_{R,T}r_R^Tr_T/r_{RT}\).
- Line 22987 has ordinary factor-system product language; it is not Tensorprodukt evidence.
- Kleines/Großes Faktorensystem is drafted as 小因子系 / 大因子系 in both JP/zh-Hans.

**Unresolved Flags**

- Lines 22971-22985 have dense mixed indices; source-image review needed before canonical TeX.
- Associated-factor-system terminology needs domain/native review.

### cjk-continuation-28-006-twisted-matrix-representation-and-equivalence

- German anchor: `23014-23083`
- Source summary: Represents u_S by n-row matrices A_S over the maximal commutative subfield. The representation covers elements u_S r_S generating automorphisms, but is not homomorphic; products map through A_T A_S^T. Basis change gives B_S = M^{-1}A_S M^S, leading to equivalence classes from right ideals.
- Japanese title: 歪表示としての行列表現と同値性
- Simplified Chinese title: 作为扭表示的矩阵表示及等价性

**Japanese Draft**

つぎに、これまでの表現と本質的に異なる形で、\(u_S\) を極大可換部分体上の \(n\) 次行列 \(A_S\) によって表す。基底 \((k_{S_1},\ldots,k_{S_n})\) に右から \(u_S\) を掛け、その変換行列を \(A_S\) と定める。

この表示の対象は、同じ自己同型を生成するすべての元 \(u_Sr_S\) である。これらは乗法群をなし、部分体の乗法群を正規部分群として含む。しかし \(u_S\mapsto A_S\) は準同型でも反準同型でもなく、積は \(u_Su_T\mapsto A_TA_S^T\) となる。

本文はこの表示を「歪表示」と呼ぶ。基底を \(M\) で変えると、対応する行列は \(M^{-1}A_SM^S\) へ変わる。したがって右イデアルから得られる表示は、この形の行列変換による同値類として扱われる。

**Simplified Chinese Draft**

接着，文本用一种不同于先前表示的方式，把 \(u_S\) 表示为极大交换子域上的 \(n\) 阶矩阵 \(A_S\)。对基 \((k_{S_1},\ldots,k_{S_n})\) 右乘 \(u_S\)，所得变换矩阵定义为 \(A_S\)。

该表示作用于所有生成同一自同构的元素 \(u_Sr_S\)。这些元素形成一个乘法群，并把子域的乘法群作为正规子群包含在内。但映射 \(u_S\mapsto A_S\) 既不是同态，也不是反同态；乘积对应为 \(u_Su_T\mapsto A_TA_S^T\)。

文本称这种表示为“扭表示”。若基通过矩阵 \(M\) 改变，相应矩阵变为 \(M^{-1}A_SM^S\)。因此由右理想得到的表示，按这种矩阵变换给出等价类。

**Script/TeX Notes**

- Preserve \((k_{S_1},\ldots,k_{S_n})A_S\), \(u_Su_T\mapsto A_TA_S^T\), and \(B_S=M^{-1}A_SM^S\).
- Verschränkte Darstellung is drafted as 歪表示 / 扭表示; this is provisional and should be reviewed.
- The group notation in lines 23041-23042 is OCR-damaged; draft uses prose rather than promoting exact symbols.

**Unresolved Flags**

- Lines 23041-23053 have OCR corruption in group symbols and the negative phrase `nicht`; source-image review needed.
- The equivalence formula at 23067-23069 has repeated k-basis wording and needs source check.

### cjk-continuation-28-007-general-twisted-representations-and-unique-irreducible-class

- German anchor: `23085-23140`
- Source summary: Defines a general m-degree twisted representation by assigning m-row matrices A_S satisfying the twisted product rule. Builds a right module, extends it to a finite R_r-right module, and concludes there is one irreducible twisted representation class, of degree t, by a rank count.
- Japanese title: 一般の歪表示と一つの既約表示類
- Simplified Chinese title: 一般扭表示与唯一既约表示类

**Japanese Draft**

右イデアルから生じる表示に限らず、本文は \(\Im^*\) の \(m\) 次歪表示を一般に定義する。各 \(u_S\) に \(m\) 次の \(\Im\)-行列 \(A_S\) を対応させ、積規則 \(u_Su_T\mapsto A_TA_S^T\) を満たすものとする。

\(\mathfrak M=x_1\mathfrak Z+\cdots+x_m\mathfrak Z\) を右 \(\mathfrak Z\)-加群とし、\((x_1u_S,\ldots,x_mu_S)=(x_1,\ldots,x_m)A_S\) によって \(\mathfrak Z^*\)-右加群にする。交換関係 \((mz)u_S=(mu_S)z^S\) から、\(\mathfrak M\) は有限 \(\Re_r\)-右加群になる。

既知の結果により、任意の有限単純 \(\Re_r\)-加群は単純右イデアルと作用素同型である。したがって \(\mathfrak G^*\) の既約歪表示類は一つだけで、単純右イデアルに属するものになる。その次数は、階数計算から \(\Re\) の指数、すなわち絶対指数数 \(t\) に等しい。

**Simplified Chinese Draft**

不再局限于由右理想产生的表示，文本一般地定义 \(\Im^*\) 的 \(m\) 次扭表示：给每个 \(u_S\) 指派一个 \(m\) 阶 \(\Im\)-矩阵 \(A_S\)，并满足乘法规则 \(u_Su_T\mapsto A_TA_S^T\)。

取右 \(\mathfrak Z\)-模 \(\mathfrak M=x_1\mathfrak Z+\cdots+x_m\mathfrak Z\)，通过 \((x_1u_S,\ldots,x_mu_S)=(x_1,\ldots,x_m)A_S\) 把它变成 \(\mathfrak Z^*\)-右模。由交换关系 \((mz)u_S=(mu_S)z^S\)，\(\mathfrak M\) 成为有限 \(\Re_r\)-右模。

根据已有结果，每个有限单 \(\Re_r\)-模都与某个单右理想算子同构。因此 \(\mathfrak G^*\) 的既约扭表示类只有一个，即属于单右理想的那一类。其次数由秩计算得出，等于 \(\Re\) 的指数，也就是绝对指数数 \(t\)。

**Script/TeX Notes**

- Preserve \(\mathfrak M=x_1\mathfrak Z+\cdots+x_m\mathfrak Z\), \((mz)u_S=(mu_S)z^S\), and rank equations at `23131-23139`.
- Endlicher Rechtsmodul is rendered as 有限右加群 / 有限右模.
- Index (absolute Exponentenzahl) is drafted as 指数（絶対指数数） / 指数（绝对指数数）.

**Unresolved Flags**

- Line 23129 references M.Z. §18 and Satz 2, Seite 22; source lineage should be reviewed.
- The rank formulas at 23131-23139 use OCR variants of field/ring symbols and need source-image review.
