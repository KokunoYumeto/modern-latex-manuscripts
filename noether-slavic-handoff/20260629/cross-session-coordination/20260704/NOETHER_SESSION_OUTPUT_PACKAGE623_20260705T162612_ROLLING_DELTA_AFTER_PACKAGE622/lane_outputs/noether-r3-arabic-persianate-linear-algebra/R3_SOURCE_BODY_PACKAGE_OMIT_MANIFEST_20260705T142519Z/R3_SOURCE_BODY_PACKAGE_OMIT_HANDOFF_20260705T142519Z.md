# R3 Source-Body Package Omit Manifest 20260705T142519Z

Status: source-canon/provenance packaging-safety sidecar only. This is not a package, not a push, not a license clearance, and not a completion claim.

Rows indexed: 216

This manifest identifies R3 raw source bodies, source archives, PDFs, HTML snapshots, TeX files, and local probe cache payloads that should remain manifest/hash/URL provenance by default. B3 may use it as an omit/filter surface for rolling packages. It does not authorize raw payload publication.

Current rule: omit raw bodies from rolling packages unless B3 creates a dedicated gated source-canon payload artifact and license/access review supports it.

Boundaries preserved: no translation, no term promotion, no native review, no canonical approval, no license clearance, no gate promotion, no completion claim, and no Git push from R3.
