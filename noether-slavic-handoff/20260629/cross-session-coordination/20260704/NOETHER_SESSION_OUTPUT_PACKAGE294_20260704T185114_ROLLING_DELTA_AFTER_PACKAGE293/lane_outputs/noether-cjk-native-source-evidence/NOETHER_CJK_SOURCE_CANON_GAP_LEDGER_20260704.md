# Noether CJK Source Canon Gap Ledger

Generated UTC: `2026-07-04T16:41:42Z`

Status: `gap-ledger / draft / non-canonical / not native reviewed / not approved / not gate-promoted`.

This ledger records exact gaps left open after creating the target-language source witness catalog. It is not a translation artifact, glossary, native-review return, or approval packet.

## Open Source-Witness Gaps

| Gap id | Scope | Current evidence | Required closure |
| --- | --- | --- | --- |
| `CJK-SRC-GAP-001` | Simplified Chinese invariant form theory | Exact `不变式理论` accepted `0` in the codepoint redo. | Find a target-language source witness with exact representation, local hash, URL, and license signal, or keep as unattested in this shelf. |
| `CJK-SRC-GAP-002` | Simplified Chinese invariant form + Hilbert | `不变式 + Hilbert` accepted `0`; `不变式 + 第十四` accepted `1` but is source-status only. | Dedicated TeX/source publication or vetted PDF fallback for the intended concept. |
| `CJK-SRC-GAP-003` | Japanese standalone representation theory | Retry for exact `表現論` had raw `20` but accepted `0`; group-context `表現論 + 群` accepted `20`. | Accept only content-confirmed standalone evidence or cite group/Lie-context witness with caveat. |
| `CJK-SRC-GAP-004` | arXiv exact phrase source archives | zh/ja exact phrase arXiv checks returned `0` entries in hard-term/codepoint logs. | Re-run only if new query strategy or date/source change exists; otherwise use TeX/source archive shelves. |
| `CJK-SRC-GAP-005` | GitHub license normalization | `T2sp/rep`, `naoki-cpp/physics`, `A-H4NU/kaist-math-notes`, `calofmijuck/algebra`, and several bulk-codepoint repositories expose no license through the GitHub API. | Row-by-row license review before redistribution or public source packaging. |
| `CJK-SRC-GAP-006` | PDF fallback clearance | Public institutional PDFs often lack explicit open-license signals in the local shelf. | Treat as internal provenance only until license/access review closes. |
| `CJK-SRC-GAP-007` | Korean addendum boundary | Korean TeX evidence exists for route-only support, including `국소화` and `텐서곱`, but no Korean CJK lane approval exists. | Dedicated Korean source/native/domain lane and explicit edition decision before stronger use. |

## Retained Corpus Blockers

| Blocker id | Scope | Current source result | Required closure |
| --- | --- | --- | --- |
| `CJK-CORPUS-BLOCK-001` | Tensor product | No usable German `Tensor` / `Tensorprodukt` / lowercase `tensor` prose anchor; noisy `\otimes`, Kronecker, crossed-product, direct-product, and product-ring contexts are insufficient. | Direct German/source anchor or explicit reviewer bridge before Japanese `テンソル積` or Simplified Chinese `张量积` corpus prose. |
| `CJK-CORPUS-BLOCK-002` | Localization | No `Lokalis` / `lokalis` German corpus anchor; quotient-ring, quotient-field, local-ring, prime-ideal, and `Idealquotienten` contexts are not localization by themselves. | Direct source anchor before Japanese `局所化` or Simplified Chinese `局部化` corpus prose. |
| `CJK-CORPUS-BLOCK-003` | Harish-Chandra | No `Harish` / `Chandra` German corpus anchor. | Direct source anchor or reviewer bridge; keep Japanese source-shelf/proper-name evidence separate. |
| `CJK-CORPUS-BLOCK-004` | Abstract algebra | Chinese source shelves support `抽象代数`, but no German `abstrakte Algebra` course/category anchor exists. | Direct source anchor or reviewer bridge. |
| `CJK-CORPUS-BLOCK-005` | Modern algebra | Chinese shelves support `近世代数` / `现代代数`, but `Moderne Algebra II` is bibliographic title evidence only. | Direct source anchor or reviewer bridge. |

## Boundary Decisions

- Source-canon catalog rows are source witnesses, not glossary approvals.
- Codepoint redo counts are citation support only at the content-confirmed-download tier; raw GitHub counts remain telemetry.
- Korean rows remain addendum/source-discovery routing only.
- No public/native signoff, no gate promotion, and no Git push occurred in this pass.
