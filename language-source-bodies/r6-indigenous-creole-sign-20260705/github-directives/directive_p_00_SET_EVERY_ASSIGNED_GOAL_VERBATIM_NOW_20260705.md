# SET EVERY ASSIGNED GOAL VERBATIM NOW

Date: 2026-07-05

This file exists because prior sessions have been reading instructions instead of making them their pursued goals.

The dispatcher must tell every idle or sibling session the following rule, exactly:

```text
Set the complete assigned task block I paste below as your pursued goal / active goal / persistent task, exactly and verbatim.

Do not summarize it.

Do not shorten it.

Do not treat it as a note to read.

Do not use only one line of it.

Do not acknowledge it as the deliverable.

The whole assigned task block must become your pursued goal. If your interface has a goal tool, use the goal tool. If it has a visible task/goal/pursuit field, paste the complete block there. If the tool cannot set a formal goal, copy the complete block at the top of your local logbook and explicitly treat it as the active pursued goal.

After setting the exact pursued goal, execute it.

Your pushed output must contain literal source bodies, usable source-checked translation/transcription output, concrete repair artifacts, source-witness packages, Lean/formalization bodies, or a real package containing those things with manifest, SHA256 hashes, README, and logbook excerpt.

Reading this instruction is not completion.

Acknowledging this instruction is not completion.

Writing a summary of this instruction is not completion.
```

## Dispatcher Obligation

If you are the one moving other-PC session, you are the dispatcher.

You must:

1. Open `00_DISPATCH_ALL_IDLE_SESSIONS_NOW_20260705.md`.
2. Copy each complete session task block from that file.
3. Paste each complete block to the corresponding idle session.
4. Tell that session to set that complete block as its pursued goal verbatim.
5. Record the assignment in `ACTIVE_SESSIONS.md`.
6. Record the assignment in `SIBLING_TASKS.md`.
7. Record the assignment in the dated session logbook.
8. Verify the next pushed work contains actual payload, not only status.

Do not tell a session merely to "work on Slavic sources" or "help with interlanguage".

Do not ask for acknowledgement.

Do not accept acknowledgement as output.

## Exact Failure Rule

If a session produces:

- acknowledgement only;
- status only;
- governance only;
- pointer-only source canon;
- a ledger with no bodies;
- a manifest for files not included;
- generated draft mislabeled as native source;
- OCR witness mislabeled as canonical source;
- work on `main`;
- no logbook;
- no manifest;
- no hashes;

then mark that output as failed and reassign the full task block again as the pursued goal verbatim.

## Current Branch Boundary

The other PC stays on:

`codex/noether-pc-20260629`

The other PC never touches `main`.

Archive maintenance integrates useful work into `main`.

## Current Useful Payload Acknowledged By Archive Maintenance

The R9 Africa/Horn/West source-body payload commit `7b3ed05b8` contained actual useful bodies and was accepted for archive-maintenance import.

It still had packaging defects:

- `main.bbl` was listed in the manifest but omitted from the commit because `.bbl` was ignored.
- `icml2025.bst` needed source-archive restoration to avoid hash drift.

Future payloads must force-add ignored body files such as `.zip`, `.tar`, `.bbl`, `.pdf`, and other source/provenance bodies where required, then verify manifests against the committed tree.

## Next Required Output

The next other-PC output must continue body collection and session dispatch.

It should not be another instruction-only push.

It should contain one or more real payload packages, such as:

- Slavic non-RU/UK source bodies;
- Russian/Ukrainian/Interslavic translation bodies;
- Fable/ChatGPT-Pro ledgers tied to actual source bodies;
- CJK source-body corpus;
- Arabic/Persianate/RTL source-body corpus;
- Romance/Germanic baseline corpus;
- Noether correction bodies;
- SGA/Deligne repair bodies;
- Lean/formalization bodies;
- audited package-body presence reports tied to actual files.

Again: every assigned session task must become that session's pursued goal exactly and verbatim.
